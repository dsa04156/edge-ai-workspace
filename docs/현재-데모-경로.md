# 현재 데모 경로

## 상태 요약

2026-07-21 현재 중앙 EdgeX는 `etri-ser0002-cgnmsb`에 유지되고, 기존 커스텀 edge Agent 두 개는 퇴역했다. Jetson에는 정식 EdgeX Device SDK 기반 `device-serial-jetson`이 배포되어 Arduino Uno의 실제 Serial telemetry를 중앙 Core Data와 대시보드까지 전달한다.

```text
[현재 배포]

etri-ser0002-cgnmsb
  └─ edgex-system
      ├─ Core Keeper
      ├─ Core Metadata
      ├─ Core Data
      ├─ Core Command
      ├─ internal MessageBus (ClusterIP)
      ├─ PostgreSQL
      └─ ingest gateway (유지, 현재 edge client 없음)

edgex-edge
  └─ device-serial-jetson @ etri-dev0001-jetorn
      ├─ /dev/arduino-001 (exact CharDevice mount)
      ├─ etri-arduino-serial profile
      ├─ arduino-001 device
      └─ 최소 ingress/egress NetworkPolicy

KubeEdge
  └─ node/workload 관리
```

삭제된 운영 리소스:

- `edgex-edge-agent-jetson`
- `edgex-edge-agent-sensehat`
- 두 Agent의 2GiB SQLite outbox PVC
- Agent contract ConfigMap과 전용 egress NetworkPolicy
- `edge-telemetry-agent`, `etri-sensehat`, `sensehat-001`을 재등록하던 Metadata bootstrap manifest

Agent 이름이 붙은 기존 Secret과 중앙 gateway 인증 설정은 중앙 구성 호환을 위해 남아 있지만 현재 Serial 경로에는 참여하지 않는다. 이 Secret이 남아 있다는 사실은 Agent가 배포되어 있다는 뜻이 아니다.

## 현재 서비스 데모 경로

첫 수직 슬라이스는 Jetson의 Arduino 센서 1종이다.

```text
Arduino sensor
  -> /dev/serial/by-id/... on etri-dev0001-jetorn
  -> /dev/arduino-001 in device-serial-jetson
  -> EdgeX async Event / Reading
  -> 중앙 internal MessageBus
  -> Core Data / PostgreSQL
  -> state-aggregator / dashboard
```

역할은 다음처럼 분리한다.

| 구성 | 책임 |
|---|---|
| EdgeX Device Service | 115200 baud Serial 연결, JSON line 검증, 재연결, 값 표준화, 상태, read-only command |
| Device Profile | resource 이름, value type, 단위, read/write 계약 |
| Device | 실제 또는 가상화된 장비 identity와 protocol binding |
| Core Metadata | Device Service/Profile/Device inventory와 관리 상태 |
| Core Data | Event/Reading 저장과 deviceName 기반 조회 |
| Core Command | `temperature`, `light`, `magnetic`, `acceleration` read-only GET을 담당 Device Service로 전달 |
| KubeEdge | Device Service와 AI workload 배치·상태 관리 |
| dashboard | Metadata 상태와 최신 Event freshness를 통합 표시 |

## MQTT와 MessageBus 경계

- 중앙 `edgex-messagebus`는 stock EdgeX Core 내부 통신용 ClusterIP Service이며 Device Service의 async Event를 Core Data로 전달한다.
- MQTT-only 센서가 아니면 Jetson에 Mosquitto를 기본 배치하지 않는다.
- Serial/Modbus/OPC-UA/I2C는 해당 Device Service가 직접 endpoint를 연다.
- 고정 ClusterIP, PodIP, node IP로 endpoint를 우회하지 않는다. 서비스 접근은 Kubernetes/EdgeMesh DNS와 정상 서비스 디스커버리를 사용한다.
- EdgeMesh 설정과 리소스는 이번 전환에서 변경하지 않는다.

## 데이터 조회 기준

Device Service가 연결된 뒤 워크플로우와 대시보드는 동일한 EdgeX device identity를 사용한다.

```text
최신/과거 Event:
GET /api/v3/event/device/name/{deviceName}?limit={n}

inventory/state:
GET /api/v3/device/name/{deviceName}
GET /api/v3/deviceservice/name/{serviceName}

현재값 command:
GET /api/v3/device/name/{deviceName}/{commandName}
```

고주기 실시간 데이터와 카메라 frame을 Core Data GET polling으로 소비하지 않는다. 실시간 AI 입력은 Device Service의 검증된 stream 경로나 RTSP 직접 구독을 별도 설계하고, EdgeX에는 inventory, 상태, command와 필요한 Event/Reading을 남긴다.

## 현재 완료와 미완료

| 항목 | 상태 |
|---|---|
| 중앙 EdgeX Core/MessageBus/PostgreSQL 배치 | 유지 |
| 기존 custom edge Agent/PVC 운영 배치 | 퇴역 |
| Jetson Arduino Serial Device Service | 배포·Ready, 재시작 0회 확인 |
| Device Profile/Device 등록 | `etri-arduino-serial` / `arduino-001` 등록 |
| Core Data 실제 sensor Event/Reading | 4 source, 6개 `Int32` resource live readback 확인 |
| dashboard device freshness | `available`, `connected`, `fresh` 확인 |
| read-only command | 4개 command GET 확인 |
| write command/actuator | Profile에 없음; 별도 승인과 안전 검증 전까지 비활성 |
| 물리 케이블 분리·복구 | 단위 테스트 외 실제 시연은 남음 |
| 동적 workflow/offloading | 후속 고도화 |

## 완료 판정

정상 수집 경로는 다음 1, 2, 3, 5와 read-only command까지 같은 배포에서 확인했다. 완전한 장애·복구 데모를 PASS로 선언하려면 4를 실제 장치에서 추가 확인한다.

1. Device Service Pod가 지정 edge node에서 Ready다.
2. Core Metadata에 Device Service/Profile/Device가 의도한 이름과 protocol로 존재한다.
3. 실제 Arduino sample이 typed Event/Reading으로 Core Data에 저장된다.
4. 연결 단절·복구 시 Device Service 상태와 재연결 동작이 검증된다.
5. dashboard가 같은 device identity, 상태와 최신 Event freshness를 표시한다.
6. command가 범위에 포함되면 read-only와 write/actuator를 구분하고 허용 목록과 실패 동작까지 별도로 검증한다.
