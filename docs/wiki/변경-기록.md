# 위키 변경 기록

이 파일은 append-only로 유지한다.
ingest, query, lint 과정에서 wiki가 어떻게 바뀌었는지 기록한다.

## [2026-08-03] ingest | 옥동 데이터 계약·Replay·AI 결과 저장 기준선

- 근거: `edge-orch/sensor-anomaly-demo/app/contracts.py`, `replay.py`,
  `model_adapter.py`, `storage.py`, PVC manifest, `state-aggregator` 결과·알림 BFF와 테스트
- 갱신: `docs/옥동-데이터-계약.md`, `docs/문서-안내.md`, `docs/프로젝트-범위.md`,
  `docs/저장소-구조.md`, `docs/현재-구현-상태.md`, 지식 지도
- 메모: 옥동 현장 field를 임의로 고정하지 않고 서비스 2종의 versioned schema를 만들었다.
  펌프 JSONL Replay는 기존 Local Data 계약으로 흘러가며 파생 결과와 발생·복귀 알림은
  EdgeX 원시 저장소와 분리된 SQLite/PVC에 idempotent하게 저장한다. 현재 모델은 통계
  기준선이고 생산품질 실행, 중앙 HA 결과 저장과 원시 telemetry offline replay는 미구현이다.

## [2026-07-30] ingest | 웹 단일 연결과 MQTT 개발 package 반영

- 근거: `edge-orch/state-aggregator/app/config/adapter_catalog.json`,
  `app/static/device-management.js`, `edgex/adapter-controller/app/runtime_settings.py`,
  `config/runtime_templates.json`, MQTT simulator development overlay와 자동 테스트
- 갱신: `docs/문서-안내.md`, `docs/프로젝트-범위.md`, `docs/저장소-구조.md`,
  `docs/디바이스-서비스-연결.md`, `docs/디바이스-카탈로그.md`,
  `docs/디바이스-등록-API.md`, `docs/현재-구현-상태.md`, 관련 Ops·Wiki 문서
- 메모: 신규 등록의 보이는 진입점을 `장비 연결` 하나로 두고 Runtime mode 선택과 직접
  후보 등록 UI를 숨겼다. `검증하고 연결`은 validation 성공 뒤 Device Service
  재사용/검증 설치, bundled Profile·Device 등록과 first Event를 이어서 처리한다.
  첫 신규 설치 package는 공식 `device-mqtt:4.0.2` 기반 개발 경로다. MQTT discovery
  subscriber와 운영 TLS/auth broker는 완료로 주장하지 않으며 broker/publisher fixture는
  root 운영 render 밖에 둔다.

## [2026-07-30] ingest | 물리 장비 그룹과 클릭형 상세정보 UI 반영

- 근거: `edge-orch/state-aggregator/app/static/index.html`,
  `device-management.js`, `dashboard.js`, `dashboard-responsive.css`와
  375/768/1024px Playwright 검증
- 갱신: `docs/문서-안내.md`, `docs/프로젝트-범위.md`, `docs/저장소-구조.md`,
  `docs/현재-구현-상태.md`, `docs/대시보드-정보-구조.md`,
  `docs/대시보드-판단-정책.md`, 관련 Ops·Wiki 문서
- 메모: 전역 간편/전체 보기 모드를 제거하고 핵심 상태는 기본 화면, 기술 근거는 카드
  상세이나 선택 항목 drawer로 제공한다. live 13개 EdgeX identity는 API/KPI에서 유지하고
  관리 목록에서는 Arduino와 Sense HAT 두 물리 source로 그룹화한다. 분리된 MPU6050은
  `STALE`/`ignored` 감사 이력으로 정리했고 활성 Runtime은 없다. 개발용 Modbus endpoint
  simulator Deployment는 남아 있지만 Controller가 만들었던 Modbus Device Service
  Runtime은 퇴역한 상태로 구분했다.

## [2026-07-23] ingest | 프로토콜 우선 등록과 다중 Serial binding 계약 반영

- 근거: `edge-orch/state-aggregator/app/config/adapter_catalog.json`,
  `edge-orch/state-aggregator/app/static/device-management.js`,
  `edgex/adapter-controller/app/models.py`, `edgex/adapter-controller/app/planner.py`
- 갱신: `docs/프로젝트-범위.md`, `docs/저장소-구조.md`,
  `docs/디바이스-서비스-연결.md`, `docs/대시보드-정보-구조.md`,
  `docs/대시보드-화면-설계.md`,
  `docs/ops/어댑터-런타임-디바이스-연결-관리.md`
- 메모: 등록 1단계를 protocol/Device Service 우선 흐름으로 바꾸고 지원 예정 protocol을
  비활성 상태로 함께 노출한다. 승인 binding의 protocol tuple을 서버에서도 검증하며, 하나의
  Serial Device Service가 복수 승인 USB 포트를 별도 reader로 처리할 수 있게 runtime
  `hardwareBindingIds` 계약과 표준 baud allowlist를 추가했다. 서로 다른 두 연결의 reader와
  Event route는 Go 테스트로 검증했다. 현재 실기기 binding은 한 개이므로 가상의 두 번째
  포트를 live manifest에 추가하지 않았다.

## [2026-07-23] ingest | Dashboard Adapter Runtime·Device 연결 관리 반영

- 근거: `edgex/adapter-controller/`, `edgex/k8s/crds/edge-ai.io_adapterruntimes.yaml`,
  `edge-orch/state-aggregator/app/*management*.py`, dashboard Device Management와 운영
  manifest
- 갱신: `docs/wiki/운영-진입점.md`, `docs/wiki/지식-지도.md`,
  `docs/wiki/대시보드와-kpi.md`, `docs/wiki/운영-모델.md`
- 메모: 새 장비 연결의 운영 진입점을 dashboard로 고정했다. Serial/I2C 기존 Argo runtime은
  조회·재사용하고 Controller 소유 runtime만 승인 template로 배포·재시작·consumer-safe
  퇴역한다. EdgeX Profile/Device와 첫 Event 확인까지 connection operation으로 추적하며
  Modbus/OPC-UA/MQTT/RTSP는 실장비 검증 전 `BLOCKED`로 유지한다.

## [2026-06-16] ingest | LLM wiki 계층 추가

- 근거: `docs/시스템-구축-목표.md`, `docs/프로젝트-배경.md`, `docs/프로젝트-범위.md`, `docs/현재-데모-경로.md`, `docs/디바이스-서비스-연결.md`, `docs/대시보드-정보-구조.md`, `docs/물리-디바이스-상태-정책.md`, `docs/단계별-추진계획.md`
- 갱신: `docs/wiki/운영-규칙.md`, `docs/wiki/지식-지도.md`, `docs/wiki/운영-모델.md`, `docs/wiki/현재-데모-흐름.md`, `docs/wiki/상태와-텔레메트리.md`, `docs/wiki/대시보드와-kpi.md`, `docs/wiki/운영-진입점.md`, `docs/wiki/2차년도-설계-트랙.md`
- 메모: Active/Ops 원본문서를 유지하면서 Karpathy식 Markdown wiki 계층을 추가했다.

## [2026-06-16] lint | 한국어 중심 문서 정리

- 근거: `docs/문서-안내.md`, `docs/문서-정리-계획.md`, `docs/wiki/운영-규칙.md`
- 갱신: `docs/wiki/*.md`
- 메모: wiki 제목과 본문을 한국어 중심으로 정리하고, 원본문서와 wiki의 역할 경계를 더 명확히 했다.

## [2026-07-21] ingest | Jetson Arduino Serial 수직 슬라이스 반영

- 근거: `edgex/device-serial/`, `edgex/k8s/base/device-serial-jetson/`, 중앙 EdgeX와 `state-aggregator` live readback
- 갱신: `docs/wiki/운영-모델.md`, `docs/wiki/현재-데모-흐름.md`, `docs/wiki/상태와-텔레메트리.md`, `docs/wiki/대시보드와-kpi.md`, `docs/wiki/운영-진입점.md`, `docs/wiki/지식-지도.md`
- 메모: Agent 없는 Serial Device Service의 정상 수집·저장·read-only 조회·dashboard 경로를 현재 기준으로 승격하고, offline replay와 실제 분리·복구를 미검증으로 분리했다.

## [2026-07-21] ingest | Jetson Local Data API와 recent cache 반영

- 근거: `edgex/device-serial/internal/driver/recent_cache.go`, `edgex/device-serial/internal/driver/local_data_api.go`, Go race 테스트와 `edgex/k8s/tests/test_device_serial_manifest.py`
- 갱신: `docs/문서-안내.md`, `docs/프로젝트-범위.md`, `docs/원시-텔레메트리-데이터-플레인.md`, `docs/디바이스-서비스-연결.md`, `docs/현재-데모-경로.md`, `docs/ops/현재-데모-운영-절차.md`, `docs/wiki/운영-모델.md`, `docs/wiki/상태와-텔레메트리.md`
- 메모: Device/resource별 최근 10분·최대 10,000 sample의 재시작 비보존 cache와 label-gated latest/recent GET을 현재 구현 계약으로 반영했다. SQLite outbox, offline replay, 중앙 history fallback과 동적 Workflow 실행은 포함하지 않는다.

## [2026-07-21] verify | Local Data API live 검증과 NetworkPolicy 한계 정정

- 근거: ARM64 digest 배포, 6개 Local Data API/Metadata/Event/Command readback, state-aggregator 동시 조회, Flannel 환경의 label 유무 비교
- 갱신: `docs/현재-데모-경로.md`, `docs/원시-텔레메트리-데이터-플레인.md`, `docs/ops/현재-데모-운영-절차.md`, `docs/wiki/운영-진입점.md`, `docs/일일-기록.md`
- 메모: 6개 가상 Device의 cache/API와 중앙 회귀는 PASS로 승격했다. 현재 Flannel CNI가 NetworkPolicy를 강제하지 않으므로 `edge-ai.io/local-data-client=true`는 보안 경계가 아니며 Local Data API는 unauthenticated read-only라는 실측 한계를 명시했다.

## [2026-07-21] ingest | Raspberry Pi Sense HAT 직접 I2C 수직 슬라이스 반영

- 근거: `edgex/device-sensehat/`, `edgex/k8s/base/device-sensehat-raspi/`, Raspberry Pi 실제 I2C와 중앙 EdgeX/dashboard live readback
- 갱신: 현재 Active/Ops 문서와 `docs/wiki/운영-규칙.md`, `docs/wiki/운영-모델.md`, `docs/wiki/현재-데모-흐름.md`, `docs/wiki/상태와-텔레메트리.md`, `docs/wiki/대시보드와-kpi.md`, `docs/wiki/운영-진입점.md`, `docs/wiki/지식-지도.md`
- 메모: MQTT/Agent 없이 `/dev/i2c-1`을 직접 읽는 Device Service와 환경/IMU 가상 Device 6개를 current scope로 승격했다. Serial 6개와 합친 live inventory는 12개이며 Modbus/OPC-UA와 물리 분리·복구는 미검증으로 유지한다.

## [2026-07-22] ingest | Local Data Cache v2 bounded ring 반영

- 근거: `edgex/local-data-cache/`, 두 Device Service의 cache adapter·stats API·custom metrics, Go race 테스트와 Kustomize manifest 테스트
- 갱신: `docs/문서-안내.md`, `docs/원시-텔레메트리-데이터-플레인.md`, `docs/디바이스-서비스-연결.md`, `docs/ops/현재-데모-운영-절차.md`, `docs/wiki/상태와-텔레메트리.md`, `docs/일일-기록.md`
- 메모: 기존 slice cache를 공통 series별 ring으로 교체하고 Device Service별 sample slot 64MiB 상한, `/api/v3/localdata/stats`와 custom metrics를 추가했다. Pod 재시작 비보존, SQLite outbox·offline replay 부재와 중앙 history 책임은 그대로 유지한다.

## [2026-07-22] verify | Local Data Cache v2 실기기 배포 검증

- 근거: immutable ARM64 image 2개, Argo CD revision `0a8a761bf05051178074c5d18accdaa6a1d4bc0d`, Jetson/Raspberry Pi Local Data API와 중앙 Core Data/dashboard/AI service readback
- 갱신: `docs/일일-기록.md`, `docs/wiki/변경-기록.md`
- 메모: Serial 6 series와 Sense HAT 11 series가 각각 64MiB budget 이하에서 증가하고, 11개 I2C source·중앙 Event·AI demo 회귀가 모두 PASS였다. EdgeMesh는 generation/observed 14와 5/5 Ready로 변경하지 않았다.

## [2026-07-22] verify | Device Service 장애 복구와 중앙 유실 경계 검증

- 근거: Jetson Arduino `cdc_acm` detach/rebind, Sense HAT reader process 재시작, 중앙 MessageBus 0 replica와 Local Data/Core Data 비교
- 갱신: `docs/프로젝트-배경.md`, `docs/물리-디바이스-상태-정책.md`, `docs/단계별-추진계획.md`, `docs/현재-데모-경로.md`, `docs/문서-안내.md`, `docs/ops/현재-데모-운영-절차.md`, `docs/wiki/상태와-텔레메트리.md`, `docs/wiki/운영-진입점.md`, `docs/일일-기록.md`, `tools/verify_e2e.sh`
- 메모: 두 physical source의 6개 Device 단위 `DOWN → UP`과 새 Event 재개를 확인했다. 중앙 단절 중 local ring과 AI service는 지속됐지만 Core Data는 멈췄고 복구 뒤 미전송 구간은 replay되지 않았다. 실제 케이블·보드 손분리는 현장 gate로 남긴다.

## [2026-07-22] verify | Core Data 1,000-device 저장 기준선

- 근거: `tools/edgex-core-data-loadtest/`, `tools/run_edgex_core_data_loadtest.sh`, 중앙 Core Data/PostgreSQL live 실행과 Pod 종료 상태
- 갱신: `docs/문서-안내.md`, `docs/프로젝트-범위.md`, `docs/저장소-구조.md`, `docs/현재-데모-경로.md`, `docs/단계별-추진계획.md`, `docs/ops/엣지엑스-코어데이터-1000-디바이스-부하검증.md`, `docs/wiki/지식-지도.md`, `docs/wiki/운영-진입점.md`
- 메모: 1,000 × 1/60Hz는 1,000/1,000과 p95 16.17ms로 PASS했고 1,000 × 1Hz는 현재 Core Data 300m/256Mi에서 OOMKilled로 FAIL했다. 안전한 기본 profile, 전용 count API, bounded maintenance와 exact cleanup 경계를 문서화했다.

## [2026-07-22] fix | Dashboard API 검증기 EdgeX 계약 동기화

- 근거: `tools/check_dashboard_api.py`, `tests/test_check_dashboard_api.py`, live `/state/dashboard`
- 갱신: `tools/verify_e2e.sh`, `docs/ops/대시보드-검증.md`
- 메모: 퇴역한 Mapper/DeviceStatus field 검사를 제거하고 현재 EdgeX Metadata/Core Data Device·KPI 계약으로 교체했다. non-healthy node는 physical `operator_focus_count`에 합치지 않으며, live 12개 Device와 virtual resource API가 strict 검사에서 PASS했다.

## [2026-07-23] ingest | 등록 연결·실장비 증거와 프로토콜 패키지 상태 분리

- 근거: `edge-orch/state-aggregator/app/device_management.py`,
  `edge-orch/state-aggregator/app/service.py`,
  `edge-orch/state-aggregator/app/static/device-management.js`
- 갱신: `docs/문서-안내.md`, `docs/프로젝트-범위.md`, `docs/저장소-구조.md`,
  `docs/대시보드-판단-정책.md`, `docs/대시보드-정보-구조.md`,
  `docs/대시보드-화면-설계.md`,
  `docs/ops/어댑터-런타임-디바이스-연결-관리.md`,
  `docs/wiki/대시보드와-kpi.md`, `docs/wiki/운영-진입점.md`
- 메모: Git binding 등록, EdgeX 장비 관측, Device Service 통신과 Core Data 최신성을
  분리했다. 신규 Device에 `hardwareBindingId`를 보존하고 protocol package를 재사용 가능,
  설치 가능, 연결 등록 필요, 검증 필요로 표시한다. 범용 scanner나 미검증 package 설치
  경로는 추가하지 않았다.

## [2026-07-28] verify | Sense HAT I2C 능동 발견·승인 E2E

- 근거: `edgex/device-discovery-agent/app/i2c_plugin.py`,
  `edgex/adapter-controller/config/discovery_plans.json`,
  `edgex/adapter-controller/config/device_bindings.json`, Argo CD revision
  `46703f7dc2dc35192d6bf7cf344682add6337c19`과 live candidate/EdgeX readback
- 갱신: 디바이스 발견·등록 최신 기준/운영 문서와 생성 HTML
- 메모: dev0003 bus 1에서 Sense HAT v1의 네 read-only chip identity를 하나의
  fingerprint로 확인했다. 토큰 없는 운영자 승인 뒤 기존 Argo Runtime, Profile과 대표
  Device를 소유권 변경 없이 재사용하고 승인 이후 첫 Event로 `EVENT_CONFIRMED`를
  확인했다. 일반 Discovery는 계속 비권한이며 I2C 전용 Pod만 exact `/dev/i2c-1`에
  제한된 privileged device-cgroup 예외를 사용한다.

## [2026-07-28] fix | decommission 장비 stable ID 자동 재탐색

- 근거: `edgex/adapter-controller/app/discovery.py`, commit `8e54e8b`,
  Adapter Controller 124개 테스트와 dev0003 Sense HAT live decommission/readback
- 갱신: 등록 연결 해제 뒤에도 물리 장비가 계속 관측되면 같은 candidate ID를
  `PENDING_APPROVAL`로 복구하고 운영자 승인과 첫 Event gate를 다시 요구
- 메모: 후보 `candidate-f0829ddd...`가 다음 30초 report에서 같은 ID로 복귀했고 재승인
  뒤 `EVENT_CONFIRMED`가 됐다. 전후 EdgeX Device 13개와 Sense HAT Device 6개는
  동일해 `reuse-existing` 리소스가 삭제되거나 중복 생성되지 않았다.

## [2026-07-28] verify | Raspberry Pi USB Serial passive discovery 활성화

- 근거: commit `dbde4e9`, Argo CD revision
  `dbde4e9ebe7b09b5ce57589c151e570131e00ed9`, dev0003 Agent log와
  `/management/discovery` live readback
- 갱신: 세 edge node에 일반 Serial Discovery를 배치하고 report에
  `scannedProtocols` 범위를 추가했다.
- 메모: dev0003에서 CH340 `1a86:7523`,
  `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0` 후보를 반복 관측했다. 검증
  binding/Profile이 없어 `BLOCKED`, `binding-required`로 유지했으며 기존 Sense HAT
  후보는 `EVENT_CONFIRMED`를 유지했다. 장비 발견을 자동 신뢰·등록으로 승격하지 않았다.

## [2026-07-28] ingest | 대시보드 Device Profile 편집·검증 추가

- 근거: `edge-orch/state-aggregator/app/device_management*.py`,
  `edge-orch/state-aggregator/app/static/device-management.*`, Core Metadata readback
  계약과 state-aggregator Python/JavaScript 테스트
- 갱신: Device Profile 목록·검증·idempotent 생성 BFF와 한국어 Profile 편집
  대화상자를 추가했다. 읽기 전용 resource를 최대 64개 입력하고 동일 이름의 다른 계약은
  차단하며 동일 계약은 재사용한다.
- 메모: 후보의 model/capability는 type을 추측하지 않는 초안으로만 사용한다. Profile
  생성은 parser, Device Service image, raw device path, Catalog binding, Device,
  command 또는 후보 승인을 자동 생성하지 않는다.

## [2026-07-28] fix | 장비 관리 진입점과 Profile 선택 단순화

- 근거: `edge-orch/state-aggregator/app/static/device-management.*`,
  `edge-orch/state-aggregator/app/static/index.html`, dashboard JavaScript 테스트
- 갱신: 등록 장비를 첫 영역으로 유지하고 새 연결 주 동작을 `장비 연결` 하나로 통합했다.
  웹 생성 Profile은 등록 현황에 즉시 표시하며 전체 Profile은 연결 3단계의 선택 목록으로
  제공한다. 수동 후보 입력은 자동 탐색 보조 영역의 `직접 후보 등록`으로 접었다.
- 메모: Profile 생성은 parser, Device Service image, hardware binding 또는 장비 등록을
  자동 생성하지 않는 기존 검증 경계를 유지한다.

## [2026-07-30] verify | MQTT 웹 연결 전체 경로 실클러스터 검증

- 근거: commits `bbcdc0f`, `95b3842`, `c753edf`,
  Runtime `adapter-mqtt-978887c832`, EdgeX Profile/Device/Core Data readback과
  connection operation `ACTIVE`
- 갱신: 검증된 protocol package의 Runtime 설정을 웹에서 입력하고
  `검증하고 연결` 한 번으로 Device Service 배포, Profile/Device 등록,
  first Event와 `operatingState=UP` readback까지 수행하도록 연결 Saga를 보강했다.
- 메모: Jetson의 선택적 Mosquitto fixture에서
  `mqtt-temperature-sim-001/temperature` Float64 Event를 확인했다. 이 결과는
  anonymous 개발 broker 검증이며 운영 TLS/auth broker나 MQTT retained discovery
  subscriber 지원을 뜻하지 않는다.

## [2026-08-03] docs | 2차년도 옥동 PoC 보고 본문 간소화

- 근거: 2026년 7월 29일 옥동 현장회의 결과와 사용자 확정 추진계획
- 갱신: `docs/단계별-추진계획.md`, `docs/프로젝트-배경.md`,
  `docs/프로젝트-범위.md`, `docs/시스템-구축-목표.md`, `docs/저장소-구조.md`,
  `docs/문서-안내.md`, `docs/wiki/2차년도-설계-트랙.md`, `docs/wiki/지식-지도.md`
- 메모: 보고 본문에는 생산품질 판별과 유압펌프·모터 이상감지 서비스 2종, 월별 일정,
  기관별 역할과 산출물만 남겼다. 데이터 항목, 장애 시나리오, 화면 목록과 구현 증거는
  기존 별첨 기술·운영 문서로 연결했으며 계획 목표와 현재 완료 상태를 분리했다.

## [2026-08-03] ingest | 옥동 펌프·모터 결과 영속화 수직 경로

- 근거: commit `849f10f`, Actions run `30796771024`, Jetson
  `sensor-anomaly-demo@sha256:9f0736a1c6b6eda612a3dd4257ff526d483c2f45cb5d94c52a66f61214232796`
- 갱신: versioned 옥동 데이터 계약, JSONL simulator/replay, model adapter,
  application-owned SQLite 결과·알림 저장, 결과 BFF와 dashboard 알림 요약, 1Gi RWO PVC
- 메모: 재시작 전 결과 172건과 특정 origin을 기록하고 새 Pod에서 동일 origin 재조회와
  217건 증가를 확인했다. 원시 telemetry 권위는 계속 EdgeX이며 중앙 HA 결과 저장과 실제
  장치 anomaly는 완료 범위가 아니다.
