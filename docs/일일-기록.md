# 일일 기록

## 목적

이 문서는 KubeEdge 기반 혼합 디바이스 edge AI 운영 가시화 PoC의 일일 작업 기록을 남기기 위한 문서다.

일지는 매일의 작업 사실, 확인한 상태, 남은 이슈, 다음 작업을 짧게 기록하는 용도다.
현재 PoC는 서비스 데모 우선, 디바이스-서비스 연결 구조, 통합 운영 가시화, 현장 적용성, 생산성 향상 효과를 중심으로 설명한다.

## 작성 원칙

- 실제로 확인한 내용과 계획을 구분한다.
- dashboard에서 관찰한 상태는 telemetry freshness, DeviceStatus snapshot, mapper 상태, node 상태를 나눠 적는다.
- `status.state=online`만으로 healthy라고 판단하지 않는다.
- raw telemetry 값을 DeviceStatus에 올린 것처럼 쓰지 않는다.
- workflow/offloading/agent-assisted planning은 현재 구현 완료 기능처럼 쓰지 않는다.
- 이슈는 원인 확정 여부를 함께 적는다.

## 일일 기록 템플릿

```markdown
## YYYY-MM-DD

### 오늘 목표

- 작성 예정

### 진행 내용

- 작성 예정

### 확인한 상태

| 항목 | 상태 | 근거 |
|---|---|---|
| 서비스 데모 |  |  |
| 디바이스 등록/관리 |  |  |
| 디바이스-서비스 연결 구조 |  |  |
| telemetry freshness |  |  |
| DeviceStatus snapshot |  |  |
| mapper / node 상태 |  |  |
| dashboard 표시 |  |  |

### 이슈 / 리스크

- 작성 예정

### 결정 사항

- 작성 예정

### 다음 작업

- 작성 예정
```

## 2026-06-17

### 오늘 목표

- 일일 작업 기록을 남길 수 있는 문서 구조를 만든다.

### 진행 내용

- `docs/일일-기록.md`를 추가해 일일 기록 템플릿을 정리했다.

### 확인한 상태

| 항목 | 상태 | 근거 |
|---|---|---|
| 서비스 데모 | 미기록 | 오늘 일지에서는 데모 실행 상태를 아직 기록하지 않았다. |
| 디바이스 등록/관리 | 미기록 | 오늘 일지에서는 device 상태를 아직 기록하지 않았다. |
| 디바이스-서비스 연결 구조 | 미기록 | 오늘 일지에서는 binding 상태를 아직 기록하지 않았다. |
| telemetry freshness | 미기록 | 오늘 일지에서는 InfluxDB latest telemetry를 아직 확인하지 않았다. |
| DeviceStatus snapshot | 미기록 | 오늘 일지에서는 DeviceStatus snapshot을 아직 확인하지 않았다. |
| mapper / node 상태 | 미기록 | 오늘 일지에서는 mapper와 node 상태를 아직 확인하지 않았다. |
| dashboard 표시 | 미기록 | 오늘 일지에서는 dashboard 표시 상태를 아직 확인하지 않았다. |

### 이슈 / 리스크

- 없음.

### 결정 사항

- 일지는 작업 사실과 관찰 근거를 분리해서 기록한다.

### 다음 작업

- 실제 데모 점검일에는 위 템플릿을 복사해 실행 결과와 관찰 근거를 채운다.

## 2026-07-21

### 오늘 목표

- Jetson Serial Device Service에 로컬 최근 데이터 cache/API를 추가하고 6개 가상 Device의 중앙·로컬 경로를 같은 revision에서 검증한다.

### 진행 내용

- Device/resource별 최근 10분·최대 10,000 sample의 동시성 안전 메모리 cache를 구현했다.
- latest/recent Local Data API와 Core Command 최신값 조회를 같은 cache로 통합했다.
- ARM64 image `sha256:4c933c3d8827dec325055634ed196380690c4748362fecb3f9676b412e724b71`을 배포했다.
- Argo CD `edgex-telemetry` revision `a5dc329b442d54c82ee222127783566f34b6e7d3`에서 live readback을 수행했다.

### 확인한 상태

| 항목 | 상태 | 근거 |
|---|---|---|
| Device Service | PASS | `device-serial-jetson`이 `etri-dev0001-jetorn`에서 Ready, restart 0, 기대 digest 실행 |
| 디바이스 등록/관리 | PASS | 6개 Device가 각 1-resource Profile과 binding되고 모두 `UNLOCKED`, `UP` |
| 중앙 telemetry | PASS | 6개 Core Data latest Event가 각 `Int32` Reading 하나와 고유 resource를 반환 |
| read-only command | PASS | 6개 Core Command GET이 cache 최신값과 origin 반환 |
| Local Data API | PASS | 6개 source 각각 recent 20개, origin 오름차순, latest 일치; empty 200, unknown 404, invalid limit 400 |
| dashboard | PASS | 동시 4회 조회에서 6개 모두 `connected`, `available`, `fresh` |
| legacy 부재 | PASS | aggregate `arduino-001` Event 0건, Agent/InfluxDB/edge-local broker/PVC 없음 |
| EdgeMesh | 무변경 | generation/observed 14, 5/5 Ready, image `kubeedge/edgemesh-agent:v1.17.0` |

### 이슈 / 리스크

- 현재 Flannel CNI는 Kubernetes NetworkPolicy를 집행하지 않는다. label 없는 기존 Pod도 unauthenticated read-only Local Data API에 접근할 수 있음을 실측했다.
- 실제 접근 제어가 필요하면 NetworkPolicy 지원 CNI 또는 mTLS/애플리케이션 인증이 필요하다.
- 실제 Serial 케이블 분리·복구와 중앙 단절 중 데이터 보존은 검증 범위가 아니다.

### 결정 사항

- Workflow client label은 현재 배치·정책 의도 표식으로만 사용하며 보안 경계로 설명하지 않는다.
- cache는 로컬 저지연 조회, PostgreSQL은 영구 이력, 미구현 outbox는 장애 재전송 책임으로 계속 분리한다.

### 다음 작업

- Raspberry Pi Sense HAT I2C Device Service를 독립 수직 슬라이스로 구현·검증한다.
- 정책 집행 또는 API 인증을 별도 보안 강화 작업으로 다룬다.

### Sense HAT I2C 추가 진행

- 공식 EdgeX Device SDK 기반 `device-sensehat-raspi`를 추가하고 `etri-dev0003-raspi5`의 exact `/dev/i2c-1` CharDevice를 직접 연결했다.
- RTIMULib reader를 Device Service 내부 protocol module로 두고 physical `sensehat-001`을 environment/IMU 가상 Device 6개, 총 11개 `Float64` resource로 fan-out했다.
- Device/resource별 최근 10분·최대 10,000 sample 메모리 cache, read-only Core Command와 Local Data API 계약을 Serial service와 같은 경계로 구현했다.
- ARM64 image `sha256:13c7b672110362d4907b797ccae50aad67d968da2296783295509f99c82b836c`와 구현 revision `365af7f21b92edf4b2fe2433096236d2892da65b`을 배포했다.

| 항목 | 상태 | 근거 |
|---|---|---|
| Sense HAT Device Service | PASS | Raspberry Pi에서 Ready, restart 0, 기대 immutable ARM64 digest 실행 |
| 실제 I2C 입력 | PASS | temperature 2개, humidity, pressure, compass, orientation 3축, gyroscope 3축이 모두 유한한 실제 값 반환 |
| Metadata/Core Data | PASS | I2C Device 6개가 `UNLOCKED`, `UP`; 6개 Event의 총 11개 `Float64` resource readback |
| Command/Local Data API | PASS | command 6개와 11개 local source의 최신값, 10분·10,000 sample retention 확인 |
| dashboard | PASS | Serial 6개 + I2C 6개, 총 12개가 `connected`, `available`, `fresh` |
| Service discovery | PASS | Raspberry Pi 임시 client에서 `.svc.cluster.local` FQDN으로 조회; 고정 ClusterIP/PodIP/node IP 미사용 |
| EdgeMesh | 무변경 | generation/observed 14, 5/5 Ready, image `kubeedge/edgemesh-agent:v1.17.0` 유지 |

### 추가 이슈 / 다음 검증

- Sense HAT 보드 분리·복구 현장 시연은 아직 남아 있다.
- Modbus와 OPC-UA는 실제 endpoint가 없으므로 구현·실장 검증 완료로 표시하지 않는다.
- EdgeX SDK의 중앙 internal MessageBus 연결은 유지하지만 물리 I2C 입력에 local MQTT broker나 Agent를 사용하지 않는다.

## 2026-07-22

### 오늘 목표

- 실제 Jetson Arduino 가속도를 사용하는 첫 서비스 데모를 구현하고 디바이스-서비스-AI 연결을 대시보드에 표시한다.

### 진행 내용

- `sensor-anomaly-demo`를 추가해 `virtual-acceleration-{x,y,z}-001`의 Local Data API를 500ms polling하고 같은 nanosecond `origin`을 한 frame으로 결합했다.
- 30 sample warm-up, threshold 4.0, anomaly 2연속, clear 3연속인 `online-gaussian-baseline-v1` 변화 탐지기를 구현했다.
- 최근 결과 최대 1,000건, pending frame 10초·1,000건, source별 cursor와 중복 제거를 bounded memory로 구현했다.
- `/healthz`, `/readyz`, `/api/v1/status`, `/api/v1/results` read-only API와 Jetson Deployment/Service/NetworkPolicy/Argo CD Application을 추가했다.
- `state-aggregator`에 failure-isolated `/state/service-demo`를 추가하고 dashboard에 physical source → Device Service → Device 3개 → AI consumer binding, raw X/Y/Z, magnitude, score, origin, 최근 input age와 상태를 표시했다.
- 실제 센서 입력이 없던 `edge-orch-factory-vision-inspection-ai` manifest와 live Application, exact orphan workload를 제거했다.

### 확인한 상태

| 항목 | 상태 | 근거 |
|---|---|---|
| origin frame join | PASS | 같은 origin의 X/Y/Z만 처리, 중복·불완전 frame TTL/capacity 테스트 통과 |
| detector | PASS | warm-up, anomaly 2연속, clear 3연속, anomaly baseline 제외와 정상 EWMA 테스트 통과 |
| Local Data client | PASS | FQDN 기본값, source identity, Int32/schema/retention/count/order와 오류 계약 테스트 통과 |
| service API/runtime | PASS | stale/error/degraded/recovery, bounded results와 worker lifecycle 테스트 통과 |
| state-aggregator | PASS | 정상 normalize와 backend/schema 장애 격리 테스트 포함 전체 72개 테스트 통과 |
| dashboard panel | PASS | 서비스 데모 6개와 기존 source-isolation 2개를 합친 dashboard JS 테스트 8개 통과 |
| ARM64 image | BUILD PASS | `sensor-anomaly-demo@sha256:1b020fe8acd3562ba5fe2d5f59efa52159aea593a60dcefd8d20da159a762a43` |
| live rollout | PASS | `sensor-anomaly-demo` Jetson Ready/restart 0, local origin `1784686831141419624` → `1784686833177002883`, frame 2535 → 2537 |
| central readback | PASS | `/state/service-demo`가 `mode=live`, `normal`, `fresh`, model ready, 세 Device binding과 `observation_error=null` 반환 |
| state-aggregator image | PASS | AMD64 tag `20260722-service-demo-v3`, registry·manifest·Pod imageID가 모두 `sha256:4069e3a15a8acc2dda9701497089af8d788816acbc4e9a5043093ab20e8cfe04`, Ready/restart 0 |
| Argo CD | PASS | 두 서비스 Application의 기능 rollout commit `547722d0831f5e203740717f3d686200c30ac578`에서 `Synced/Healthy`; 후속 검증 기록 commit에는 workload diff 없음 |
| browser | PASS | Firefox 실제 렌더에서 NORMAL/fresh, 12/12 available/fresh, input age 포함 5개 지표의 desktop·390px 무겹침과 console error/warning 0 확인 |
| legacy cleanup | PASS | 가짜 비전 Application과 orphan Deployment/Service/ServiceAccount/Pod 제거 확인 |
| EdgeMesh | 무변경 | generation/observed 14, 5/5 Ready, `kubeedge/edgemesh-agent:v1.17.0` 유지 |

### 이슈 / 리스크

- 500ms REST polling은 push stream이 아니므로 고주기 진동·카메라 입력 경로로 일반화하지 않는다.
- detector는 학습된 고장 분류기가 아니라 online 통계 baseline의 magnitude 변화 탐지다.
- 실제 장치를 움직여 anomaly를 발생시키는 현장 시연 전에는 실제 고장 검출 실증으로 표현하지 않는다.
- 현재 Flannel CNI에서는 NetworkPolicy label이 접근 제어 경계가 아니다.

### 결정 사항

- EdgeX Core Metadata/Core Data/PostgreSQL은 raw inventory·telemetry 권위를 유지하고 AI result는 별도 derived service output으로 둔다.
- 첫 데모는 동적 Workflow Builder가 아니라 명시적으로 배포하는 고정 Jetson workload로 제한한다.
- 고정 ClusterIP/PodIP/node IP를 설정에 넣지 않고 Service FQDN만 사용한다.

### 다음 작업

- 실제 장치를 움직여 anomaly 상태를 발생시키고 정상 복귀까지 현장 화면으로 시연한다.
- Serial 케이블과 Sense HAT 보드 분리·복구를 별도 현장 gate로 검증한다.
- 500ms polling보다 높은 주기가 필요한 입력은 push/stream 경로를 별도 설계한다.

### Device 저장 이력 dashboard 추가 진행

- Assets의 Device 선택 화면을 중앙 Core Data/PostgreSQL-backed 저장 이력과 연결하고 기본 30분, 5분·1시간·24시간, 새로고침, resource별 series, 반환 Reading 수와 실제 첫/마지막 timestamp를 추가했다.
- EdgeX device Event endpoint가 지원하지 않는 `start`/`end` query를 무시하는 동작을 실데이터에서 확인했다. `state-aggregator`가 최신 1,000 Event를 먼저 읽고 Event `origin`으로 요청 시간창을 적용하도록 수정했다.
- AMD64 image `state-aggregator@sha256:c8b7f192ec88d43510baed079310e4ef62ada44affbd785c149eb29f0ddeb648`을 배포했고, 기능 rollout revision `c28ce3f7306b9f633a6d7f60f7a6cc8fe92edbf8`에서 Argo CD `Synced/Healthy/Succeeded`와 Pod Ready/restart 0을 확인했다.

| 항목 | 상태 | 근거 |
|---|---|---|
| 12개 5분 이력 | PASS | Serial 6개는 각각 294~295 Event/Reading, Sense HAT 6개는 각각 300 Event를 반환했고 모든 Reading `origin`이 요청 시작 이후였다. |
| 다중 resource | PASS | Sense HAT temperature 2개 resource는 600 Reading, orientation/gyroscope 각 3개 resource는 900 Reading으로 독립 series를 반환했다. |
| 긴 범위 상한 | PASS | Serial temperature의 30분·1시간·24시간은 최신 1,000 Event와 약 1,016초 실제 구간, orientation은 3,000 Reading과 약 999초 실제 구간을 표시했다. |
| browser | PASS | Firefox에서 Serial 5분/24시간과 Sense HAT orientation 3-series를 확인했고, 1280px·390px 가로 넘침 없음, console error/warning 0이었다. |
| 조회 권위 | PASS | dashboard → `state-aggregator` → EdgeX Core Data API → PostgreSQL 경로만 사용하고 dashboard/aggregator의 PostgreSQL 직접 연결은 없다. |
| EdgeX/EdgeMesh | 무변경 | `edgex-telemetry` revision `668e2fb57df613313b0550771c556f0386449595` 유지, EdgeMesh generation/observed 14와 5/5 Ready 유지. |

### 저장 이력 이슈 / 결정

- 24시간 버튼은 24시간 전체 보존 결과를 보장하지 않는다. 최신 1,000 Event 상한과 화면의 실제 구간을 함께 해석한다.
- 로컬 최근 cache는 엣지 Workflow용이고 dashboard 저장 이력은 중앙 Core Data용이라는 책임 분리를 유지한다.
- 오류·빈 결과는 latest Reading으로 대체하지 않으며, 늦게 끝난 이전 Device/범위 요청이 현재 선택을 덮어쓰지 않는다.

### Local Data Cache v2 구현 진행

- Serial과 Sense HAT의 기존 `map[(device,resource)][]sample` slice cache를 `edgex/local-data-cache` 공통 제네릭 ring 구현으로 교체했다.
- 최근 10분·series별 최대 10,000 sample 계약을 유지하고 Device Service별 sample slot 할당 상한 67,108,864 bytes를 추가했다.
- `/api/v3/localdata/stats`와 `LocalDataCacheSamples`, `LocalDataCacheSeries`, `LocalDataCacheAllocatedBytes`, `LocalDataCacheEvictions`를 두 서비스에 동일하게 추가했다.
- cache API/race/vet, Kustomize manifest 11개 테스트와 클러스터 server-side dry-run을 통과했다.
- 64MiB는 ring sample slot만 계산하며 Go runtime, map과 HTTP 응답 메모리는 포함하지 않는다. cache는 Pod 재시작 시 초기화되고 SQLite outbox/offline replay는 여전히 없다.
- ARM64 image는 Serial `sha256:ddece2451dd0079483c3d7441f4ce6f393b57dea6ce338601adb407c9113db3b`, Sense HAT `sha256:53df28e46ae20272092bcf14cbc831b9934bbcc643c2dbf84338f76ba96cf216`으로 빌드·배포했다. 두 registry config는 모두 `linux/arm64`였다.
- 기능 rollout revision `0a8a761bf05051178074c5d18accdaa6a1d4bc0d`에서 Argo CD `edgex-telemetry`가 `Synced/Healthy/Succeeded`이고 두 Pod가 지정 node에서 Ready, restart 0, 기대 imageID를 실행했다.

| 항목 | 상태 | 근거 |
|---|---|---|
| Serial stats | PASS | `slotBytes=32`, `series=6`, `samples=234`, `allocatedBytes=12288/67108864`, eviction 0 |
| Sense HAT stats | PASS | `slotBytes=32`, `series=11`, `samples=748`, `allocatedBytes=45056/67108864`, eviction 0 |
| Local Data 증가 | PASS | Serial temperature origin `1784699203443249292` → `1784699205478384325`, Sense HAT humidity `1784699231333119027` → `1784699236333489623` |
| Sense HAT 전체 source | PASS | 11개 source가 모두 유한한 `Float64`와 원본 nanosecond origin 반환 |
| 중앙 저장/dashboard | PASS | Serial/I2C Core Data latest Event가 새 image 이후 계속 증가하고 temperature 5분 이력 API가 20개 typed Reading 반환 |
| AI 서비스 회귀 | PASS | `mode=live`, `inputState=fresh`, origin `1784699321490010870` → `1784699324541469507`, frame 14807 → 14810 |
| 임시 검증 리소스 | 정리 완료 | Raspberry Pi same-node `cache-v2-sensehat-check` Pod 삭제 확인 |
| EdgeMesh | 무변경 | generation/observed 14, 5/5 Ready, `kubeedge/edgemesh-agent:v1.17.0` 유지 |

### Device Service 장애·복구 실측

- Jetson Arduino의 정확한 USB identity를 VID/PID `2341:0043`, serial `75035303230351E0D171`, `cdc_acm` interface `1-2.1:1.0`으로 확인하고 해당 interface만 detach/rebind했다.
- Raspberry Pi Sense HAT Device Service의 Python I2C reader 자식 프로세스만 종료해 source restart를 주입했다.
- Argo CD self-heal을 원복 가능한 범위에서 잠시 비활성화하고 중앙 MessageBus가 실제 0 Pod인 구간을 만든 뒤 두 local ring, AI consumer와 중앙 Core Data를 분리 관찰했다.

| 항목 | 상태 | 근거 |
|---|---|---|
| Serial source state fan-out | PASS | USB detach 후 6개 Device가 `DOWN`; rebind 약 31초 뒤 6개 `UP`, 새 local origin `1784706241287759855`와 Core Data readback 확인 |
| Sense HAT reader restart | PASS | reader PID 15 종료 후 Metadata가 `UP → DOWN → UP`; 약 1.3초 뒤 새 PID 37과 11개 resource/중앙 Event 확인 |
| Device Service Pod recovery | PASS | Serial ring `3540 → 78`, Sense HAT ring `6600 → 154`로 초기화된 뒤 다시 증가; 두 Pod restart 0 |
| 중앙 장애 중 local continuity | PASS | MessageBus 0 Pod 동안 Serial ring `1878 → 2094`, Sense HAT ring `3234 → 3641`, AI service `live/fresh` 유지 |
| 중앙 장애 중 persistence | 유실 확인 | Serial count `78741`, I2C count `73870` 고정; 복구 후 각각 1건부터 재개, 최대 origin gap 약 128.213초/128.010초 |
| outbox/offline replay | 없음 확인 | outage 중 ring sample은 중앙 미전송 queue가 아니며 복구 뒤 과거 구간이 replay되지 않음 |
| Argo CD | 원복 완료 | `selfHeal=true`, MessageBus 1/1, `edgex-telemetry` `Synced/Healthy/Succeeded` |
| 임시 검증 Pod | 정리 완료 | `recovery-check-jetson`, `recovery-check-sensehat`, `serial-host-recovery-check` 삭제 |
| EdgeMesh | 무변경 | generation/observed 14, 5/5 Ready, image `kubeedge/edgemesh-agent:v1.17.0` |

### 장애 검증 결정

- Serial/I2C의 정상 수집과 driver/reader 수준 복구는 현재 PASS로 기록한다.
- 실제 케이블·Sense HAT 보드 손분리와 실제 장치 움직임 anomaly는 별도 현장 gate로 유지한다.
- 중앙 단절 중 미전송 Event 보존이 요구되면 ring cache를 재사용하지 않고 persisted ACK 계약을 가진 SQLite outbox를 별도 설계한다.
- 현행 read-only 종합 점검 진입점 `tools/verify_e2e.sh`를 두 Device Service, 12개 Device, Local Data stats와 중앙 Core Data 경계에 맞게 갱신한다.

## 2026-07-23 Dashboard Device Management 온보딩 MVP

### 구현 범위

- `state-aggregator`에 Adapter Catalog, validation, guarded Profile/Device create, allowlist
  PATCH와 operation 조회 API를 추가했다.
- Adapter Catalog는 Serial Jetson과 Sense HAT I2C만 현재 `installed` 후보로 두고 Core
  Metadata의 실제 Device Service 상태로 `installed`/`unavailable`을 판정한다. Modbus,
  OPC-UA, MQTT와 RTSP는 검증 전 `unsupported`로 고정했다.
- create는 Profile → Device → Metadata readback → first Core Data Event 확인 순서의 제한된
  saga로 처리하고, Device 생성 전 실패 시 이번 요청이 만든 미사용 Profile만 보상 삭제한다.
- HMAC 기반 request ID, payload hash와 예약 Device tag로 동일 요청 재시도와 Metadata 적용
  이후 operation 복구를 지원한다. raw idempotency key와 payload hash는 API에 노출하지 않는다.
- Dashboard에 별도 `Device Management` 화면을 추가해 adapter 상태, onboarding dry-run,
  기존 Device 선택, allowlist PATCH와 `metadata_applied`/`waiting_for_event`/`verified`/`failed`
  상태를 표시한다.

### 안전 경계

- 초기 온보딩 MVP 운영 manifest는 `DEVICE_MANAGEMENT_ENABLED=false`를 명시했다. adapter
  조회와 dry-run은 가능하지만 create/PATCH는 `404`이고 화면 control도 비활성화했다.
- mutation 활성화 시 관리자 Bearer token과 요청별 `Idempotency-Key`, 별도 HMAC key가 모두
  필요하다. token은 browser JavaScript 메모리에만 있고 Git, URL, localStorage와
  sessionStorage에 저장하지 않는다.
- Device/Profile 삭제, actuator command, adapter 자동 설치, Kubernetes apply/restart,
  KubeEdge Device CRD와 EdgeMesh 변경은 없다.
- 자동 테스트는 fake EdgeX client로 create/readback/compensation을 검증하며 live Core Metadata
  mutation이나 canary 정리는 수행하지 않는다.
- 현재 Serial/Sense HAT의 exact endpoint/resource route 중복, Device Service LOCKED/미등록,
  Profile-selector 불일치와 system tag 변조를 dry-run/PATCH에서 차단한다. Core Data 확인 장애는
  Metadata 성공을 취소하지 않고 `waiting_for_event`로 복구 가능하게 유지한다.
- CI가 Deployment image를 직접 바꾼 뒤 Argo CD `selfHeal`에 의해 이전 digest로 복구되는
  경합을 제거했다. 빌드 digest와 Git SHA를 `edge-orch-state-aggregator` Application에 함께
  반영하고 목표 digest 확인 후 rollout을 판정한다.
- 워크플로 파일 변경으로 legacy `mqttvirtual` 단계가 평가되더라도 실제 DaemonSet이 없으면
  rollout만 건너뛴다. MQTT workload나 KubeEdge mapper를 새로 생성하지 않는다.

### 검증 상태

| 항목 | 상태 | 근거 |
|---|---|---|
| state-aggregator Python | PASS | API/domain/EdgeX client와 기존 상태 API를 포함한 138개 테스트 통과 |
| dashboard JavaScript | PASS | Device Management, history, source isolation과 service demo 28개 테스트 통과 |
| 저장소 root Python | PASS | 문서·dashboard·배포 계약을 포함한 88개 테스트 통과 |
| EdgeX manifest 회귀 | PASS | `edgex/k8s/tests` 111개 통과 |
| state-aggregator render | PASS | server-side dry-run에서 Deployment, Service, ServiceAccount, RBAC와 ServiceMonitor 검증 |
| live read-only browser | PASS | 실제 EdgeX 12개 Device와 Serial/I2C adapter 상태, endpoint/resource 중복 차단, 390px 반응형과 console error 0건 확인 |
| 네트워크 경계 | PASS | 고정 `clusterIP`, PodIP, EdgeMesh와 KubeEdge Device/DeviceModel 리소스를 추가하지 않음 |
| 문서 생성 | PASS | 95개 source Markdown을 `docs/html`과 검색 색인으로 재생성 |
| live Metadata mutation | 미실행 | 자동 테스트는 fake client만 사용하고 기존 12개 Device/Profile을 변경하지 않음 |

## 2026-07-23 Dashboard Adapter Runtime 관리 확장

초기 온보딩 MVP 이후 운영자가 dashboard에서 Adapter Runtime 선택부터 EdgeX 연결까지
관리하도록 경계를 확장했다.

- `AdapterRuntime` CRD와 별도 `edgex-adapter-controller`를 root `edgex/k8s` 배포에
  추가했다.
- `state-aggregator`는 Kubernetes 쓰기 권한 없이 내부 HMAC으로 Controller를 호출하고,
  Runtime 준비 → Profile/Device 적용 → Metadata readback → first Event를 하나의 connection
  operation으로 추적한다.
- 운영 manifest는 management와 runtime mutation flag를 활성화한다. 관리자 token과 두 HMAC
  key는 namespace별 Secret에만 두며 누락 시 Pod가 시작하지 않는다.
- 기존 Serial/I2C Argo runtime은 조회·재사용 전용이다. Controller 소유 runtime만
  재시작·consumer-safe 퇴역한다.
- raw manifest, 임의 image/hostPath/hostNetwork, 고정 ClusterIP/PodIP, EdgeMesh와 KubeEdge
  Device CRD 변경 경로는 제공하지 않는다.
- Modbus/OPC-UA/MQTT/RTSP는 승인 image, profile과 실장비 Event 증거 전까지
  `unverified`/`BLOCKED`다.

### Adapter Runtime 관리 라이브 검증

- 기능 revision `fdb5b39cdfdda7a315ab66561a808eadb6c1e115`의 GitHub Actions run
  `29985731891`이 성공했고 `edgex-telemetry`, `edge-orch-state-aggregator`, `docs-html`
  Application이 모두 `Synced/Healthy/Succeeded`가 됐다.
- 검증 다이제스트를 소스 fallback으로 고정한 revision
  `14a9eecb0b4b7c227f4858c4d30cd90da3a22c08`의 GitHub Actions run
  `29987266411`도 성공했고 세 Application이 해당 revision에서
  `Synced/Healthy/Succeeded`가 됐다.
- 소스 fallback으로 고정한 1차 검증 image는 Controller
  `sha256:540be3b209cbf91c4a378dffbb759f0dc3a2017ce147e4d574d329a32de07bcc`,
  Serial `sha256:215dc73e86c7e9e69938b4e0b1f991947705083f61ca851758c1fb259c883eda`,
  Sense HAT `sha256:dd7ea84f91f1bc351ff47b1ffc81696f2f53ffdc3857b194acba3c2b0fafae38`,
  state-aggregator
  `sha256:aeb7109bf097c4dcd87fcad803b5a622b541f7c9b9a26f6988a9951582363a3a`다.
- 최종 Argo image override와 실행 Pod imageID는 Controller
  `sha256:eb42cb1f4afbd67204e90acda391d1c35d49c0e64a7ca847d11e238890d733c3`,
  Serial `sha256:c237c32a2eba9bf65d3d8c4bbb4f456c1e007de4b0cf9345606ce83ca32882d6`,
  Sense HAT `sha256:f772cfc279d3b09a28fd13eefd23f9eadaa7989c07ea4d35871127700ec11c22`,
  state-aggregator
  `sha256:82b6499c828238ad30dba91ce0110c4f03a04bd6480ee3541182aa20244754a2`로
  일치했다.

| 항목 | 상태 | 근거 |
|---|---|---|
| Runtime inventory | PASS | Serial과 Sense HAT을 `SERVICE_READY`, `external`, `argocd`, `hardware-verified`, consumer 6개씩으로 발견 |
| Runtime mutation 경계 | PASS | 두 external runtime의 `mutable=false`; Restart/Retire 버튼 비활성화 |
| Runtime plan | PASS | Serial·Sense HAT `auto`는 `REUSE`; Modbus·OPC-UA·MQTT·RTSP `deploy`는 `template_unverified`로 `BLOCKED` |
| Connection validation | PASS | 기존 Serial temperature route 재사용 시 `protocol_binding_exists`; `Connect Device` 비활성화, mutation 없음 |
| EdgeX 회귀 | PASS | 12개 Device가 `UNLOCKED`, `UP`, `connected`, `available`, latest Core Data Event `fresh` |
| 서비스 데모 회귀 | PASS | `sensor-anomaly-demo`가 `mode=live`, input `fresh`, model `ready`, observation error 없음 |
| 최소 RBAC | PASS | state-aggregator의 `edgex-edge` Deployment/AdapterRuntime create는 `no`; Controller는 해당 namespace만 `yes` |
| 실제 workload | PASS | Controller, Serial, Sense HAT, state-aggregator 모두 Ready, restart 0, 기대 imageID 실행 |
| 브라우저 | PASS | Firefox desktop·390px에서 관리 화면, validation 결과와 12개 Device 확인; console error/warning 0, 390px 가로 overflow 0 |
| 관리 CR canary | 미실행 | 현재 승인 hardware binding은 기존 external runtime이 사용 중이며 새 격리 장비가 없어 `AdapterRuntime` 생성 없이 read-only/validation canary만 수행 |
| EdgeMesh | 무변경 | generation/observed 14, 5/5 Ready, 모든 agent restart 0, `kubeedge/edgemesh-agent:v1.17.0` 유지 |

### 현재 복구 경계

- EdgeX Device tag를 사용한 Metadata 적용 이후 operation 복구와 동일 payload 재요청의
  중복 방지는 구현돼 있다.
- 신규 Controller runtime이 아직 준비 중일 때의 connection operation 저장소는
  state-aggregator 메모리에 있다. 현재 승인 template은 모두 `deploymentEnabled=false`라 이
  경로를 운영에서 실행하지 않지만, 향후 managed template을 승격하기 전에는 CR에 connection
  payload reference를 남기거나 별도 영속 operation store를 추가해 GET-only 재시작 복구를
  검증해야 한다.

## 2026-07-23 노드별 한국어 디바이스 관리

### 구현 범위

- 공통 dashboard 제목과 메뉴, 디바이스 관리 화면, 동적 상태·오류·필드 문구를 한국어로
  정리했다.
- `GET /state/nodes` 관측 결과와 Adapter Runtime, EdgeX Device, 승인 hardware binding을
  결합해 노드별 관리 범위를 만든다.
- 관리 카드에는 `node_type=edge_*`인 노드와 Runtime·Device·승인 binding이 실제로 참조하는
  노드만 표시한다. 관계없는 중앙 `cloud_server` 노드는 제외한다.
- 노드를 선택하면 Runtime inventory, 승인 Adapter, hardware binding, EdgeX Device 목록을
  함께 필터링하고 연결 마법사의 target node를 선택 노드로 고정한다.
- 기존 Device 수정은 선택 노드 안의 Device를 명시적으로 선택한 뒤에만 활성화한다.
- 노드 필터는 workload placement와 운영 탐색 범위이며 물리 Device의 inventory, state,
  telemetry, command 권위는 계속 EdgeX다.

### 자동 검증

| 항목 | 상태 | 근거 |
|---|---|---|
| state-aggregator Python | PASS | 상태·관리 API를 포함한 170개 테스트 통과 |
| dashboard JavaScript | PASS | 노드 범위, cloud server 제외, Device 선택 전 PATCH 차단을 포함한 39개 테스트 통과 |
| 저장소 root Python | PASS | dashboard·문서·배포 계약 89개 테스트 통과 |
| 문서 검색 | PASS | 생성 HTML과 검색 색인 7개 테스트 통과 |
| 문서 일관성 | PASS | 정책 규칙 7개 PASS, WARN 0, FAIL 0 |

### 라이브 배포 검증

- 기능 revision `527a59079028b74bd2159161b216c7d618a907b9`에 노드 범위와 수정 안전성
  보강을 반영했고, 운영 문서 revision `5075f8792556bdeab4ecdd549bed6d163f6a5274`까지
  `main`에 반영했다.
- 정상 push GitHub Actions run `29989511969`이 성공했다. 변경 감지는 state-aggregator와
  docs만 선택했고 EdgeX Controller, Serial, Sense HAT와 legacy mapper 단계는 건너뛰었다.
- `edge-orch-state-aggregator` Application은 revision `5075f8792556bdeab4ecdd549bed6d163f6a5274`,
  `Synced/Healthy/Succeeded`이며 실행 image와 imageID는
  `sha256:878071d728b7790fc0bc20e43c87cb73722fc1a0571acbec191376e42a0ceee7`로
  일치했다. Pod는 Ready, restart 0이다.
- `docs-html`은 `main`에서 `Synced/Healthy/Succeeded`다. `edgex-telemetry`는 기존 revision
  `14a9eecb0b4b7c227f4858c4d30cd90da3a22c08`을 유지했다.
- 변경 범위를 전부 선택하는 수동 `workflow_dispatch` run `29989262966`은 EdgeX 배포 단계
  전에 취소했다. 실행 중 생성된 `latest` image는 운영의 digest-pinned Deployment에
  적용하지 않았고, 이후 정상 push 실행에서 EdgeX 단계가 skip된 것을 확인했다.
- 실제 Firefox 화면에는 Jetson, AI HAT 후보 Raspberry Pi, Sense HAT Raspberry Pi의 엣지
  노드 카드 3개만 표시됐다. 중앙 `cloud_server` 노드는 표시되지 않았다.
- Jetson 선택 시 Serial Runtime 1개·Device 6개·승인 Adapter 1개, Sense HAT 노드 선택 시
  I2C Runtime 1개·Device 6개·승인 Adapter 1개만 각각 표시됐다. AI HAT 후보 노드는
  Runtime·Device·승인 Adapter가 0개이고 연결 controls가 비활성화됐다.
- 기존 Device 수정 버튼은 Device 선택 전 비활성, 선택 후 활성 상태를 확인했다. 390px
  viewport의 `innerWidth`와 `scrollWidth`는 모두 390이고 console error/warning은 0건이다.
- EdgeMesh는 generation/observed 14, 5/5 Ready,
  `kubeedge/edgemesh-agent:v1.17.0`으로 변하지 않았다.
