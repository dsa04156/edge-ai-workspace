# Raspberry Pi Sense HAT I2C Device Service 구현 계획

> **실행 방식:** 테스트를 먼저 실패시키고 최소 구현으로 통과시킨 뒤, ARM64 image와
> live EdgeX readback까지 단계별로 검증한다.

**Goal:** `etri-dev0003-raspi5`의 Sense HAT v1을 MQTT 없이 직접 읽는 EdgeX Device
Service를 배포하고 6개 가상 Device를 중앙/로컬 API와 dashboard에서 확인한다.

**Architecture:** image 내부 RTIMULib Python reader가 `/dev/i2c-1`을 한 번 읽어 JSON
sample을 만들고 Go EdgeX Device SDK driver가 6개 Device Event, Command와 bounded recent
cache로 fan-out한다. 중앙 EdgeX와 EdgeMesh는 유지한다.

**Tech Stack:** Go 1.25, EdgeX Device SDK Go v4.0.2, Python 3/RTIMULib 7.2.1,
Kustomize, Argo CD, Docker buildx, ko, pytest/unittest.

---

## Task 1: RTIMULib protocol reader

**Files:**

- Create: `edgex/device-sensehat/reader/read_sensehat.py`
- Create: `edgex/device-sensehat/reader/test_read_sensehat.py`
- Create: `edgex/device-sensehat/reader/RTIMULib.ini`

1. degree normalization, validity flag, finite-value와 11-field sample 계약 테스트를 먼저
   작성하고 RED를 확인한다.
2. fake RTIMU object로 pressure/humidity/IMU 성공과 invalid read 오류를 테스트한다.
3. shell/network/MQTT 없이 JSON line 하나를 생성하는 reader를 구현한다.
4. retry는 Go layer가 소유하므로 Python fatal read는 non-zero exit로 단순화한다.

## Task 2: Go parser와 managed process source

**Files:**

- Create: `edgex/device-sensehat/go.mod`
- Create: `edgex/device-sensehat/internal/driver/sample.go`
- Create: `edgex/device-sensehat/internal/driver/sample_test.go`
- Create: `edgex/device-sensehat/internal/driver/source.go`
- Create: `edgex/device-sensehat/internal/driver/source_test.go`

1. strict JSON, device ID, required finite Float64 field 테스트를 RED로 만든다.
2. command factory를 주입할 수 있는 source test double로 process exit, state UP/DOWN,
   backoff와 context stop을 검증한다.
3. scanner limit, stderr drain, shell 미사용과 child cleanup을 구현한다.

## Task 3: I2C binding, recent cache와 Local Data API

**Files:**

- Create: `edgex/device-sensehat/internal/driver/config.go`
- Create: `edgex/device-sensehat/internal/driver/config_test.go`
- Create: `edgex/device-sensehat/internal/driver/recent_cache.go`
- Create: `edgex/device-sensehat/internal/driver/recent_cache_test.go`
- Create: `edgex/device-sensehat/internal/driver/local_data_api.go`
- Create: `edgex/device-sensehat/internal/driver/local_data_api_test.go`

1. `/dev/i2c-1`, `sensehat-001`과 6개 ResourceGroup만 허용하는 binding 테스트를 작성한다.
2. Float64 cache의 10분·10,000개 eviction, device/resource 격리, newest limit, 동시 접근을
   테스트한다.
3. Jetson과 같은 latest/recent HTTP status/query/정렬 계약을 테스트한다.

## Task 4: EdgeX Device SDK driver

**Files:**

- Create: `edgex/device-sensehat/cmd/device-sensehat/main.go`
- Create: `edgex/device-sensehat/internal/driver/driver.go`
- Create: `edgex/device-sensehat/internal/driver/driver_test.go`

1. 한 sample의 6개 Device fan-out과 group별 resource 수/단위/type/origin을 테스트한다.
2. multi-resource Core Command GET, write 거부, LOCKED/remove/cache cleanup, 동일 update no-op,
   shared source 한 개와 state fan-out을 테스트한다.
3. Device SDK custom route와 async channel 통합을 구현한다.

## Task 5: Profile/Device/config resources

**Files:**

- Create: `edgex/device-sensehat/res/configuration.yaml`
- Create: `edgex/device-sensehat/res/profiles/etri-sensehat-*.yaml` (6개)
- Create: `edgex/device-sensehat/res/devices/sensehat-virtual-devices.yaml`
- Create: `edgex/device-sensehat/internal/driver/resources_test.go`

1. 6개 Device/Profile 이름, Float64/resource/units/command와 공통 I2C binding 테스트를 RED로
   작성한다.
2. canonical resources를 구현하고 aggregate `sensehat-001` Device를 만들지 않는다.

## Task 6: ARM64 runtime와 Kubernetes manifest

**Files:**

- Create: `edgex/device-sensehat/Dockerfile.base`
- Create: `edgex/device-sensehat/.ko.yaml`
- Create: `edgex/k8s/base/device-sensehat-raspi/**`
- Modify: `edgex/k8s/kustomization.yaml`
- Create: `edgex/k8s/tests/test_device_sensehat_manifest.py`
- Modify: `edgex/k8s/tests/test_device_serial_manifest.py`

1. root render에 Serial/I2C 두 edge workload만 있고 MQTT Agent/InfluxDB/PVC가 없다는
   manifest 테스트를 먼저 실패시킨다.
2. exact Raspberry Pi placement, `/dev/i2c-1` CharDevice, Recreate, FQDN, port 59911,
   read-only root와 `/tmp`, 정책 의도 label을 테스트한다.
3. canonical resource와 ConfigMap byte equality를 테스트한다.
4. RTIMULib ARM64 base image를 build/push하고 digest로 `.ko.yaml`을 고정한다.
5. Go service image를 build/push하고 workload image를 digest로 고정한다.

## Task 7: Argo live cutover와 회귀

1. 구현 commit을 원격 branch에 push하고 `edgex-telemetry` targetRevision을 해당 SHA로
   동기화한다.
2. `device-sensehat-raspi`가 `etri-dev0003-raspi5`에서 기대 digest, Ready, restart 0인지
   확인한다.
3. Metadata 6개, Core Data latest 6개, Core Command 6개, Local Data API와 dashboard 12개
   inventory/freshness를 readback한다.
4. Jetson Serial 6개가 계속 fresh이고 local cache/command가 회귀하지 않았는지 확인한다.
5. legacy MQTT publisher의 최근 publish가 없고 새 Pod가 local MQTT port를 사용하지 않음을
   확인한다.
6. EdgeMesh generation 14, image v1.17.0, 5/5 Ready가 유지되는지 확인한다.

## Task 8: protocol matrix와 문서/최종 검증

**Files:**

- Update: `docs/문서-안내.md`, `docs/프로젝트-범위.md`, `docs/프로젝트-배경.md`
- Update: `docs/원시-텔레메트리-데이터-플레인.md`, `docs/디바이스-서비스-연결.md`
- Update: `docs/현재-데모-경로.md`, `docs/ops/현재-데모-운영-절차.md`
- Update: `docs/wiki/*.md`, `docs/일일-기록.md`, `docs/wiki/변경-기록.md`
- Regenerate: `docs/html/**`, `docs/generated/consistency-report.html`

1. Serial/I2C는 live PASS, MQTT physical input은 미사용, Modbus/OPC-UA는 endpoint 부재로
   미검증이라고 명시한다.
2. 기존 Sense HAT MQTT/KubeEdge fixture를 현재 경로로 설명하지 않는다.
3. Python/Go race/vet, EdgeX K8s, state-aggregator, docs test와 root render를 모두 실행한다.
4. live resource, 임시 Pod 부재, fixed IP/PVC/Agent/InfluxDB 부재와 EdgeMesh 무변경을 다시
   확인한다.
5. `git diff --check`, clean worktree와 remote HEAD 일치를 확인한 뒤 최종 push한다.
