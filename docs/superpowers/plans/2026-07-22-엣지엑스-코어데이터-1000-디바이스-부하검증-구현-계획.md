# EdgeX Core Data 1,000 디바이스 부하검증 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 현재 EdgeX Core Data의 Event 단위 PostgreSQL 저장 경로를 1,000개 합성 device와 지정 입력률로 재현하고 자동 판정하는 일회성 부하검증 도구를 만든다.

**Architecture:** Go 표준 라이브러리 CLI가 rate-limited Core Data REST Event를 보내고 HTTP commit 응답, latency, device별 readback과 cleanup을 측정한다. shell runner가 amd64 정적 바이너리를 빌드해 server2 고정 일회성 Kubernetes Pod에서 실행하며 운영 Kustomization과 EdgeMesh는 변경하지 않는다.

**Tech Stack:** Go 1.22 standard library, EdgeX Core Data v3 REST API, Bash, Kubernetes Pod, pytest runner contract tests

**Implementation Status (2026-07-22):** 완료. 최종 운영 기준과 실측 PASS/FAIL은 `docs/ops/엣지엑스-코어데이터-1000-디바이스-부하검증.md`를 따른다. 초기 계획의 1Hz 기본값은 live OOM 결과에 따라 1/60Hz 안전 기본값과 별도 1Hz 스트레스 profile로 변경했다.

## Global Constraints

- 물리 inventory/state/telemetry/command의 권위는 EdgeX다.
- 합성 Device를 Core Metadata에 등록하지 않는다.
- 고정 ClusterIP, PodIP 또는 node IP를 사용하지 않고 Service FQDN을 사용한다.
- EdgeMesh와 Argo CD Application을 변경하지 않는다.
- root `edgex/k8s/kustomization.yaml`에 부하 Pod를 추가하지 않는다.
- 합성 Event cleanup은 실행별 정확한 device 이름에만 수행한다.
- 기본 EdgeX Core Data 저장 구현은 변경하지 않는다.

---

### Task 1: 부하검증 도메인과 판정

**Files:**
- Create: `tools/edgex-core-data-loadtest/go.mod`
- Create: `tools/edgex-core-data-loadtest/loadtest.go`
- Create: `tools/edgex-core-data-loadtest/loadtest_test.go`

**Interfaces:**
- Produces: `Config.Validate() error`, `PlannedEvents(Config) int`, `Percentile([]time.Duration, float64) time.Duration`, `Evaluate(Config, Report) []string`

- [ ] **Step 1: Write failing tests for config, event count, percentile and threshold evaluation**

```go
func TestPlannedEvents(t *testing.T) {
    cfg := Config{Devices: 1000, PerDeviceHz: 1, Duration: time.Minute}
    if got := PlannedEvents(cfg); got != 60000 { t.Fatalf("got %d", got) }
}
```

- [ ] **Step 2: Run the focused test and confirm it fails because the symbols do not exist**

Run: `cd tools/edgex-core-data-loadtest && go test ./...`

Expected: FAIL with undefined `Config` or `PlannedEvents`.

- [ ] **Step 3: Implement the minimal domain types and deterministic calculations**

Implement validated positive device/rate/duration/concurrency/timeouts, nearest-rank percentiles, error rate, target-rate 95% floor, p95 and verification mismatch reasons.

- [ ] **Step 4: Run focused tests**

Run: `cd tools/edgex-core-data-loadtest && go test ./...`

Expected: PASS.

### Task 2: EdgeX HTTP Event, readback and cleanup client

**Files:**
- Modify: `tools/edgex-core-data-loadtest/loadtest.go`
- Modify: `tools/edgex-core-data-loadtest/loadtest_test.go`

**Interfaces:**
- Produces: `NewHTTPClient(Config) *HTTPClient`, `HTTPClient.Store(context.Context, Sample) Result`, `HTTPClient.Count(context.Context, string) (int64, error)`, `HTTPClient.Delete(context.Context, string) error`

- [ ] **Step 1: Write failing `httptest.Server` tests**

Verify the POST path, EdgeX v3 AddEventRequest JSON, UUID fields, Float64 Reading, `201` response ID reconciliation, device count decoding and exact device DELETE path.

- [ ] **Step 2: Run tests and confirm expected missing-method failures**

Run: `cd tools/edgex-core-data-loadtest && go test ./... -run 'TestHTTPClient|TestBuildEvent'`

Expected: FAIL because the client does not exist.

- [ ] **Step 3: Implement the standard-library HTTP client**

Use a pooled `http.Transport`, per-request context timeout, `url.PathEscape`, JSON content type and bounded response-body reads. Treat only `201` as Store success, use the dedicated count endpoint's `200`/`count` response, accept DELETE `202`, wait for `count=0`, and require the returned Event ID to match.

- [ ] **Step 4: Run the client tests**

Run: `cd tools/edgex-core-data-loadtest && go test ./...`

Expected: PASS.

### Task 3: Rate-limited runner and JSON report CLI

**Files:**
- Modify: `tools/edgex-core-data-loadtest/loadtest.go`
- Modify: `tools/edgex-core-data-loadtest/loadtest_test.go`
- Create: `tools/edgex-core-data-loadtest/main.go`

**Interfaces:**
- Produces: `Run(context.Context, Config, Client) Report` and a CLI whose exit code is zero only for a PASS report

- [ ] **Step 1: Write failing runner tests with an in-memory client**

Test exact attempt count, round-robin device distribution, bounded concurrency, failed Store aggregation, readback mismatch, cleanup invocation and cancellation.

- [ ] **Step 2: Run tests and verify expected runner failures**

Run: `cd tools/edgex-core-data-loadtest && go test ./... -run 'TestRun'`

Expected: FAIL because `Run` is missing.

- [ ] **Step 3: Implement scheduler, worker pool, metrics and CLI flags**

The scheduler computes each target send time from the run start instead of accumulating ticker drift. The report includes run ID, timestamps, target/achieved rate, attempted/succeeded/failed, error groups, p50/p95/p99/max, readback, cleanup and PASS reasons.

- [ ] **Step 4: Run tests and build**

Run: `cd tools/edgex-core-data-loadtest && go test ./... && go build -o /tmp/edgex-core-data-loadtest .`

Expected: PASS and exit 0.

### Task 4: 일회성 Kubernetes 실행 경로

**Files:**
- Create: `tools/run_edgex_core_data_loadtest.sh`
- Create: `tests/test_edgex_core_data_loadtest_runner.py`

**Interfaces:**
- Consumes: Go files from `tools/edgex-core-data-loadtest`
- Produces: execution-specific Pod and `artifacts/edgex-loadtest/<generated-run-id>.json`

- [ ] **Step 1: Write failing runner contract tests**

Assert the safe default 1,000 devices × 1/60Hz, Service FQDN, server2 node selector, automount disabled, digest-pinned BusyBox, no fixed IP, no EdgeMesh mutation and cleanup trap.

- [ ] **Step 2: Run the contract test and confirm failure because the script is absent**

Run: `python3 -m pytest tests/test_edgex_core_data_loadtest_runner.py -q`

Expected: FAIL with missing script.

- [ ] **Step 3: Implement the runner**

Build a static linux/amd64 binary in a temporary directory, apply a run-scoped unprivileged Pod with an `emptyDir`, copy the binary into that volume, execute it, collect the JSON report, preserve failure diagnostics and delete only the run-scoped Pod unless `KEEP_RESOURCES=1`.

- [ ] **Step 4: Run contract and shell syntax tests**

Run: `python3 -m pytest tests/test_edgex_core_data_loadtest_runner.py -q && bash -n tools/run_edgex_core_data_loadtest.sh`

Expected: PASS.

### Task 5: 운영 문서와 live 검증

**Files:**
- Create: `docs/ops/엣지엑스-코어데이터-1000-디바이스-부하검증.md`
- Modify: `docs/현재-데모-경로.md`
- Modify: `docs/단계별-추진계획.md`

**Interfaces:**
- Consumes: runner JSON report and Kubernetes status
- Produces: repeatable runbook, measured baseline and explicit scope boundary

- [ ] **Step 1: Document the isolated REST storage baseline and commands**

Include low-rate and 1Hz profiles, PASS thresholds, report location, cleanup behavior and the fact that MessageBus/Device Service throughput is not measured.

- [ ] **Step 2: Run repository and module verification**

Run: `cd tools/edgex-core-data-loadtest && go test -race ./... && go vet ./... && go build .`

Run: `python3 -m pytest tests/test_edgex_core_data_loadtest_runner.py tests/test_docs_consistency.py -q`

Expected: all PASS.

- [ ] **Step 3: Execute live low-rate and 1Hz profiles**

Run: `DEVICES=1000 PER_DEVICE_HZ=0.0166666667 DURATION=60s tools/run_edgex_core_data_loadtest.sh`

Run: `DEVICES=1000 PER_DEVICE_HZ=1 DURATION=30s tools/run_edgex_core_data_loadtest.sh`

Expected: report is collected for both runs; low-rate PASS and 1Hz OOM FAIL are recorded from measured output rather than assumed.

- [ ] **Step 4: Verify live workload recovery and synthetic-data cleanup**

Run: `kubectl -n edgex-system get pod -l 'app.kubernetes.io/name in (edgex-core-data,edgex-postgres)' -o wide`

Run: `RUN_ID=$(python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))["runId"])' artifacts/edgex-loadtest/latest.json); kubectl get --raw "/api/v1/namespaces/edgex-system/services/http:edgex-core-data:59880/proxy/api/v3/event/device/name/loadtest-${RUN_ID}-0000?limit=1"`

Expected: Core Data/PostgreSQL Ready; cleaned test device returns zero events or not found while physical Device Events continue increasing.

- [ ] **Step 5: Commit and push main only after fresh verification**

Run: `git status --short && git diff --check && git add <scoped-files> && git commit -m "test: add EdgeX Core Data scale baseline" && git push origin main`

Expected: commit succeeds and `origin/main` equals local `HEAD`.
