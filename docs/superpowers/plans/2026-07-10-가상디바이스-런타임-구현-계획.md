# 가상디바이스 런타임 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an executable second-year prototype that reads Serial JSON through a profile-driven adapter, emits normalized virtual-device telemetry to a local MQTT broker, and publishes lifecycle/heartbeat status without implementing AI, KubeEdge, EdgeX, registry, dashboard, or orchestration features.

**Architecture:** A validated YAML `DeviceProfile` configures one adapter, one normalizer, and one MQTT output. `VirtualDeviceRuntime` owns lifecycle, duplicate/stale suppression, status transitions, heartbeat, bounded reconnect backoff, and graceful shutdown; adapter and publisher dependencies are injectable so tests need neither serial hardware nor a broker.

**Tech Stack:** Python 3.11+, dataclasses, PyYAML, pyserial, paho-mqtt, pytest, Docker.

## Global Constraints

- Keep `sensor-collector/serial_mqtt_collector.py` and `tests/test_serial_mqtt_collector.py` unchanged and passing.
- Publish only fields declared under `mapping.properties`; never copy the complete raw payload.
- Keep `sourceTimestamp` separate from `collectedAt` and suppress exact duplicates and older comparable timestamps.
- Runtime support is limited to Serial JSON plus a test-only fake adapter and local MQTT output.
- Preserve all pre-existing dirty-worktree changes and do not commit unrelated files.

---

### Task 1: Scope boundary, profile validation, and normalized models

**Files:**
- Modify: `docs/프로젝트-범위.md`
- Modify: `docs/저장소-구조.md`
- Create: `virtual-device/pyproject.toml`
- Create: `virtual-device/profiles/serial-vibration-example.yaml`
- Create: `virtual-device/virtual_device/__init__.py`
- Create: `virtual-device/virtual_device/config.py`
- Create: `virtual-device/virtual_device/models.py`
- Create: `virtual-device/virtual_device/normalizer.py`
- Create: `virtual-device/tests/test_config.py`
- Create: `virtual-device/tests/test_normalizer.py`
- Create: `virtual-device/tests/test_duplicate_suppression.py`

**Interfaces:**
- Produces: `load_profile(path, env=None, overrides=None) -> DeviceProfile`
- Produces: `Normalizer(profile).normalize(raw, collected_at) -> NormalizedSample`
- Produces: `SampleGuard.check(source_timestamp, data) -> SampleDecision`
- Produces: `TelemetryEnvelope.from_sample(profile, sample, sequence).to_dict()`

- [x] Write tests for valid/invalid YAML, CLI/environment precedence, mapping-only output, type conversion, timestamp preservation, missing required fields, exact duplicate suppression, same-value/new-timestamp acceptance, and older-timestamp rejection.
- [x] Run `cd virtual-device && ../.venv-publisher/bin/python -m pytest tests/test_config.py tests/test_normalizer.py tests/test_duplicate_suppression.py -q`; verify collection/import failures because modules do not exist.
- [x] Implement the dataclasses, validation, environment overrides, normalization, quality errors, and sample guard with only the behavior required by the failing tests.
- [x] Re-run the same command and require all Task 1 tests to pass.

### Task 2: Adapter and publisher boundaries

**Files:**
- Create: `virtual-device/virtual_device/adapters/__init__.py`
- Create: `virtual-device/virtual_device/adapters/base.py`
- Create: `virtual-device/virtual_device/adapters/serial_json.py`
- Create: `virtual-device/virtual_device/adapters/fake.py`
- Create: `virtual-device/virtual_device/publisher.py`
- Create: `virtual-device/tests/test_serial_adapter.py`

**Interfaces:**
- Produces: `Adapter.start()`, `Adapter.read()`, `Adapter.health()`, `Adapter.stop()`
- Produces: `SerialJsonAdapter(connection, serial_factory=None)` and `FakeAdapter(samples)`
- Produces: `MqttPublisher(config, client_factory=None)` and `InMemoryPublisher`
- Produces: `InvalidSampleError` for malformed JSON and `AdapterConnectionError` for transport failures.

- [x] Write tests using a fake serial object for decode, malformed JSON recovery, health state, and close behavior; test fake adapter sequencing and in-memory publication.
- [x] Run `cd virtual-device && ../.venv-publisher/bin/python -m pytest tests/test_serial_adapter.py -q`; verify expected missing-interface failures.
- [x] Implement minimal adapter and publisher classes with lazy imports for pyserial and paho-mqtt.
- [x] Re-run the Task 2 tests and require them to pass without serial hardware or a broker.

### Task 3: Runtime lifecycle, status, heartbeat, and reconnect

**Files:**
- Create: `virtual-device/virtual_device/status.py`
- Create: `virtual-device/virtual_device/runtime.py`
- Create: `virtual-device/tests/test_runtime.py`

**Interfaces:**
- Produces: `RuntimeStatusTracker` with starting/running/degraded/stopped/failed phases.
- Produces: `VirtualDeviceRuntime(profile, adapter, publisher, clock, sleeper)` with `run(max_iterations=None)`, `request_stop()`, and graceful cleanup.

- [x] Write runtime tests for fake-adapter end-to-end output, invalid JSON continuation, invalid mapping quality, disconnected/degraded status, heartbeat, bounded reconnect backoff, and stopped status.
- [x] Run `cd virtual-device && ../.venv-publisher/bin/python -m pytest tests/test_runtime.py -q`; verify expected missing-runtime failures.
- [x] Implement lifecycle and status transitions, publish telemetry only after `SampleGuard` accepts it, increment sequence only for published telemetry, retry failed adapter starts/reads with capped backoff, and always close adapter/publisher.
- [x] Re-run Task 3 plus Tasks 1-2 and require all tests to pass.

### Task 4: CLI, container image, and operator documentation

**Files:**
- Create: `virtual-device/virtual_device/main.py`
- Create: `virtual-device/Dockerfile`
- Create: `virtual-device/README.md`

**Interfaces:**
- Produces: `python -m virtual_device.main --profile profiles/serial-vibration-example.yaml`
- CLI overrides: `--mqtt-host`, `--mqtt-port`, `--serial-port`, `--serial-baud-rate`, and `--serial-timeout-seconds`.
- Environment overrides: `VD_MQTT_HOST`, `VD_MQTT_PORT`, `VD_SERIAL_PORT`, `VD_SERIAL_BAUD_RATE`, and `VD_SERIAL_TIMEOUT_SECONDS`, with documented legacy aliases.

- [x] Add a CLI configuration test to `test_config.py` if parsing/override behavior is not already covered.
- [x] Verify the test fails before `main.py` exists.
- [x] Implement adapter/publisher factories, signal handlers for SIGINT/SIGTERM, clear configuration errors, and the container entrypoint.
- [x] Document definition, AI-service distinction, flow, layout, profile schema, precedence, local and Docker commands, topics/payloads, supported scope, exclusions, and concrete adapter extension points.
- [x] Run `cd virtual-device && ../.venv-publisher/bin/python -m virtual_device.main --help` and require exit code 0.

### Task 5: Full verification and review

**Files:**
- Verify only; modify a production file only after first adding a reproducing failing test.

- [x] Run `cd virtual-device && ../.venv-publisher/bin/python -m pytest -q`.
- [x] Run `../.venv-publisher/bin/python -m pytest tests/test_serial_mqtt_collector.py -q` from the repository root.
- [x] Run `cd virtual-device && ../.venv-publisher/bin/python -m compileall -q virtual_device`.
- [x] Inspect `git diff --check` and the scoped diff for raw-payload leakage, timestamp conflation, unbounded busy loops, and accidental legacy changes.
- [x] Report created/modified files, design decisions, exact test evidence, hardware command, exclusions, and compatibility status.
