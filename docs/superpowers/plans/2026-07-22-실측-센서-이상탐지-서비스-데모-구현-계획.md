# 실측 센서 이상 탐지 서비스 데모 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Jetson의 실제 Arduino 가속도 X/Y/Z Local Data를 준실시간으로 결합·분석하고, 온라인 이상 탐지 상태와 Device→Device Service→AI consumer 관계를 중앙 dashboard에 표시한다.

**Architecture:** 새 `sensor-anomaly-demo` FastAPI 서비스가 `device-serial-jetson` Local Data API를 origin cursor로 읽고 세 축 frame을 결합한다. Jetson에서 온라인 Gaussian baseline을 실행하며, `state-aggregator`는 read-only Service FQDN으로 결과를 정규화해 dashboard에 표시한다. 원시 telemetry/history 권위는 중앙 EdgeX Core Data/PostgreSQL에 유지한다.

**Tech Stack:** Python 3.11, FastAPI 0.116.1, httpx 0.28.1, Pydantic 2.11.3, pytest 8.4.2, vanilla JavaScript, Kubernetes/Kustomize, Argo CD, Docker buildx

## Global Constraints

- 모든 shell 명령은 저장소 규칙에 따라 `rtk`로 시작한다.
- 구현은 테스트를 먼저 실패시킨 뒤 최소 구현으로 통과시키는 TDD 순서를 따른다.
- runtime 데이터 경로는 Service FQDN만 사용하며 fixed ClusterIP, PodIP 또는 node IP를 사용하지 않는다.
- `sensor-anomaly-demo`는 `etri-dev0001-jetorn`에 배치하고 `edge-ai.io/local-data-client=true` label을 가진다.
- Local Data polling 기본 간격은 0.5초, input stale 기준은 10초다.
- 세 축 `Int32` sample은 같은 nanosecond `origin`일 때만 하나의 frame으로 처리한다.
- warm-up 30 frame, threshold 4.0, 2회 연속 초과 시 anomaly, 3회 연속 정상 시 clear, EWMA alpha 0.05를 사용한다.
- API는 read-only이며 합성 anomaly 주입, actuator command, EdgeX metadata/state mutation을 추가하지 않는다.
- local MQTT broker, InfluxDB, Agent, SQLite/PVC, `hostNetwork`를 추가하지 않는다.
- EdgeMesh manifest, image, ConfigMap과 DaemonSet을 수정하지 않는다.
- 기존 `vision_stage_runner`의 가짜 결과는 운영 데모 근거로 사용하지 않고 해당 Argo Application을 퇴역한다.
- 현재 Flannel이 NetworkPolicy를 집행하지 않는다는 한계를 문서와 운영 결과에 유지한다.
- 설계 기준은 `docs/superpowers/specs/2026-07-22-실측-센서-이상탐지-서비스-데모-설계.md`다.

## 파일 구조

| 경로 | 책임 |
|---|---|
| `edge-orch/sensor-anomaly-demo/app/models.py` | axis sample, joined frame, inference result와 API schema |
| `edge-orch/sensor-anomaly-demo/app/joiner.py` | origin 기반 세 축 결합, 중복·pending eviction |
| `edge-orch/sensor-anomaly-demo/app/detector.py` | warm-up, baseline, score와 hysteresis |
| `edge-orch/sensor-anomaly-demo/app/config.py` | 환경변수 설정과 source identity |
| `edge-orch/sensor-anomaly-demo/app/local_data.py` | Local Data v3 HTTP client와 응답 검증 |
| `edge-orch/sensor-anomaly-demo/app/runtime.py` | polling lifecycle, counters, recent result ring과 status snapshot |
| `edge-orch/sensor-anomaly-demo/app/main.py` | FastAPI app factory, probe/status/results routes |
| `edge-orch/sensor-anomaly-demo/tests/` | domain/client/runtime/API/manifest 계약 테스트 |
| `edge-orch/sensor-anomaly-demo/k8s/` | Jetson Deployment, Service, NetworkPolicy와 Kustomize entrypoint |
| `edge-orch/state-aggregator/app/service_demo_models.py` | 중앙 dashboard용 strict response schema |
| `edge-orch/state-aggregator/app/service_demo.py` | AI 서비스 client와 degraded failure isolation |
| `edge-orch/state-aggregator/app/static/service-demo.js` | service demo fetch/normalize/render |
| `edge-orch/state-aggregator/app/static/service-demo.css` | overview demo panel layout/state styling |
| `edge-orch/state-aggregator/tests/test_service_demo.py` | 중앙 client/API failure isolation 테스트 |
| `edge-orch/state-aggregator/tests/test_service_demo_dashboard.js` | dashboard pure renderer 계약 테스트 |
| `edge-orch-argocd/argocd-apps.yaml` | fake vision Application 선언 제거 |
| `edge-orch-argocd/sensor-anomaly-demo-app.yaml` | 새 서비스 전용 Argo Application 선언 |

---

### Task 1: 세 축 frame 모델과 origin joiner

**Files:**
- Create: `edge-orch/sensor-anomaly-demo/app/__init__.py`
- Create: `edge-orch/sensor-anomaly-demo/app/models.py`
- Create: `edge-orch/sensor-anomaly-demo/app/joiner.py`
- Create: `edge-orch/sensor-anomaly-demo/tests/test_joiner.py`

**Interfaces:**
- Consumes: axis 이름 `Literal["x", "y", "z"]`, Local Data sample의 `origin`, `valueType`, `value`
- Produces: `AxisSample`, `AccelerationFrame`, `FrameJoiner.ingest(axis, samples, now_ns) -> list[AccelerationFrame]`, `FrameJoiner.counters`

- [ ] **Step 1: 같은 origin만 결합하고 중복·불완전 frame을 처리하는 실패 테스트 작성**

```python
from app.joiner import FrameJoiner
from app.models import AxisSample


def sample(origin: int, value: int) -> AxisSample:
    return AxisSample(origin=origin, value_type="Int32", value=value)


def test_joiner_emits_only_complete_same_origin_frames_once() -> None:
    joiner = FrameJoiner(pending_ttl_ns=10_000, max_pending=3)
    assert joiner.ingest("x", [sample(100, 1)], now_ns=100) == []
    assert joiner.ingest("y", [sample(100, 2)], now_ns=100) == []
    assert joiner.ingest("z", [sample(100, 3)], now_ns=100) == [
        AccelerationFrame(origin=100, x=1, y=2, z=3)
    ]
    assert joiner.ingest("z", [sample(100, 3)], now_ns=101) == []
    assert joiner.counters.duplicates_ignored == 1


def test_joiner_evicts_expired_and_capacity_bounded_pending_frames() -> None:
    joiner = FrameJoiner(pending_ttl_ns=10, max_pending=2)
    joiner.ingest("x", [sample(1, 1), sample(2, 2), sample(3, 3)], now_ns=3)
    assert joiner.pending_origins == [2, 3]
    joiner.ingest("y", [], now_ns=20)
    assert joiner.pending_origins == []
    assert joiner.counters.incomplete_frames_dropped == 3
```

- [ ] **Step 2: joiner 테스트가 모듈 부재로 실패하는지 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_joiner.py`

Expected: FAIL with `ModuleNotFoundError: No module named 'app.joiner'`.

- [ ] **Step 3: immutable domain model과 bounded joiner 최소 구현**

```python
# app/models.py
from dataclasses import dataclass
from typing import Literal

AxisName = Literal["x", "y", "z"]

@dataclass(frozen=True)
class AxisSample:
    origin: int
    value_type: str
    value: int

@dataclass(frozen=True)
class AccelerationFrame:
    origin: int
    x: int
    y: int
    z: int

@dataclass
class JoinCounters:
    duplicates_ignored: int = 0
    incomplete_frames_dropped: int = 0
```

```python
# app/joiner.py 핵심 계약
class FrameJoiner:
    def __init__(self, pending_ttl_ns: int, max_pending: int) -> None:
        if pending_ttl_ns <= 0 or max_pending <= 0:
            raise ValueError("pending limits must be positive")
        self.pending_ttl_ns = pending_ttl_ns
        self.max_pending = max_pending
        self.counters = JoinCounters()
        self._pending: dict[int, dict[AxisName, int]] = {}
        self._pending_seen_at: dict[int, int] = {}
        self._processed_seen_at: dict[int, int] = {}

    def ingest(
        self,
        axis: AxisName,
        samples: list[AxisSample],
        now_ns: int,
    ) -> list[AccelerationFrame]:
        self._evict(now_ns)
        frames: list[AccelerationFrame] = []
        for sample in samples:
            if sample.origin in self._processed_seen_at:
                self.counters.duplicates_ignored += 1
                continue
            values = self._pending.setdefault(sample.origin, {})
            self._pending_seen_at.setdefault(sample.origin, now_ns)
            if axis in values:
                self.counters.duplicates_ignored += 1
            values[axis] = sample.value
            if set(values) == {"x", "y", "z"}:
                frames.append(AccelerationFrame(
                    origin=sample.origin,
                    x=values["x"], y=values["y"], z=values["z"],
                ))
                self._pending.pop(sample.origin)
                self._pending_seen_at.pop(sample.origin)
                self._processed_seen_at[sample.origin] = now_ns
        self._evict(now_ns)
        return sorted(frames, key=lambda item: item.origin)

    @property
    def pending_origins(self) -> list[int]:
        return sorted(self._pending)

    def _evict(self, now_ns: int) -> None:
        cutoff = now_ns - self.pending_ttl_ns
        expired = [origin for origin, seen in self._pending_seen_at.items() if seen < cutoff]
        capacity = max(0, len(self._pending) - len(expired) - self.max_pending)
        survivors = [origin for origin in sorted(self._pending) if origin not in set(expired)]
        for origin in expired + survivors[:capacity]:
            self._pending.pop(origin, None)
            self._pending_seen_at.pop(origin, None)
            self.counters.incomplete_frames_dropped += 1
        for origin, seen in list(self._processed_seen_at.items()):
            if seen < cutoff:
                self._processed_seen_at.pop(origin)
```

구현은 `dict[int, dict[AxisName, int]]` pending map과 processed-origin set을 사용한다. 완전한
frame은 origin 오름차순으로 반환하고 processed origin은 다시 방출하지 않는다. TTL과
capacity eviction 모두 `incomplete_frames_dropped`를 정확히 한 번 올린다.

- [ ] **Step 4: joiner 전체 테스트 통과 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_joiner.py`

Expected: PASS, `2 passed` 이상.

- [ ] **Step 5: domain/joiner 독립 커밋**

```bash
rtk git add edge-orch/sensor-anomaly-demo/app edge-orch/sensor-anomaly-demo/tests/test_joiner.py
rtk git commit -m "feat: join live acceleration frames by origin"
```

---

### Task 2: 온라인 baseline 이상 탐지기

**Files:**
- Modify: `edge-orch/sensor-anomaly-demo/app/models.py`
- Create: `edge-orch/sensor-anomaly-demo/app/detector.py`
- Create: `edge-orch/sensor-anomaly-demo/tests/test_detector.py`

**Interfaces:**
- Consumes: `AccelerationFrame`
- Produces: `InferenceResult`, `OnlineGaussianDetector.process(frame) -> InferenceResult`, `OnlineGaussianDetector.snapshot() -> ModelSnapshot`

- [ ] **Step 1: warm-up, anomaly 진입/복귀와 baseline 오염 방지 실패 테스트 작성**

```python
from app.detector import DetectorConfig, OnlineGaussianDetector
from app.models import AccelerationFrame


def frame(origin: int, x: int) -> AccelerationFrame:
    return AccelerationFrame(origin=origin, x=x, y=0, z=0)


def test_detector_warms_up_then_requires_two_anomalies_and_three_normals() -> None:
    detector = OnlineGaussianDetector(DetectorConfig(
        warmup_samples=3, threshold=4.0, stddev_floor=1.0,
        anomaly_streak=2, clear_streak=3, ewma_alpha=0.05,
    ))
    assert [detector.process(frame(i, 10)).status for i in range(1, 4)] == [
        "warming_up", "warming_up", "normal"
    ]
    baseline_before = detector.snapshot().baseline_mean
    assert detector.process(frame(4, 30)).status == "normal"
    assert detector.process(frame(5, 30)).status == "anomaly"
    assert detector.snapshot().baseline_mean == baseline_before
    assert [detector.process(frame(i, 10)).status for i in range(6, 9)] == [
        "anomaly", "anomaly", "normal"
    ]


def test_detector_uses_vector_magnitude_and_variance_floor() -> None:
    detector = OnlineGaussianDetector(DetectorConfig(warmup_samples=2, stddev_floor=2.0))
    detector.process(AccelerationFrame(origin=1, x=3, y=4, z=0))
    ready = detector.process(AccelerationFrame(origin=2, x=3, y=4, z=0))
    assert ready.magnitude == 5.0
    assert ready.score == 0.0
```

- [ ] **Step 2: detector 테스트 실패 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_detector.py`

Expected: FAIL because `app.detector` does not exist.

- [ ] **Step 3: Welford warm-up과 normal-only EWMA detector 구현**

```python
@dataclass(frozen=True)
class DetectorConfig:
    warmup_samples: int = 30
    threshold: float = 4.0
    stddev_floor: float = 1.0
    anomaly_streak: int = 2
    clear_streak: int = 3
    ewma_alpha: float = 0.05

class OnlineGaussianDetector:
    def process(self, frame: AccelerationFrame) -> InferenceResult:
        magnitude = math.sqrt(frame.x**2 + frame.y**2 + frame.z**2)
        if self._count < self.config.warmup_samples:
            self._count += 1
            delta = magnitude - self._mean
            self._mean += delta / self._count
            self._m2 += delta * (magnitude - self._mean)
            if self._count == self.config.warmup_samples:
                self._variance = self._m2 / max(1, self._count - 1)
                status = "normal"
            else:
                status = "warming_up"
            return self._result(frame, magnitude, 0.0, status)

        stddev = max(math.sqrt(max(self._variance, 0.0)), self.config.stddev_floor)
        score = abs(magnitude - self._mean) / stddev
        exceeds = score >= self.config.threshold
        if exceeds:
            self._above += 1
            self._below = 0
            if self._above >= self.config.anomaly_streak:
                self._anomaly = True
        else:
            self._below += 1
            self._above = 0
            if self._anomaly and self._below >= self.config.clear_streak:
                self._anomaly = False
            alpha = self.config.ewma_alpha
            delta = magnitude - self._mean
            self._mean += alpha * delta
            self._variance = (1 - alpha) * (self._variance + alpha * delta * delta)
            self._count += 1
        status = "anomaly" if self._anomaly else "normal"
        return self._result(frame, magnitude, score, status)
```

`InferenceResult`는 `origin`, X/Y/Z, magnitude, score, anomaly, status를 갖고, status는
`warming_up | normal | anomaly`로 제한한다. `ModelSnapshot`은 algorithm,
sample_count, warmup_samples, threshold, baseline_mean/stddev를 반환한다. float API 값은
소수점 6자리에서 반올림해 동일 입력에 안정적인 JSON을 만든다.

- [ ] **Step 4: detector 테스트 통과 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_detector.py`

Expected: PASS.

- [ ] **Step 5: detector 회귀 커밋**

```bash
rtk git add edge-orch/sensor-anomaly-demo/app/models.py edge-orch/sensor-anomaly-demo/app/detector.py edge-orch/sensor-anomaly-demo/tests/test_detector.py
rtk git commit -m "feat: detect acceleration changes with online baseline"
```

---

### Task 3: strict Local Data v3 client

**Files:**
- Create: `edge-orch/sensor-anomaly-demo/app/config.py`
- Create: `edge-orch/sensor-anomaly-demo/app/local_data.py`
- Create: `edge-orch/sensor-anomaly-demo/tests/test_local_data.py`
- Create: `edge-orch/sensor-anomaly-demo/requirements.txt`

**Interfaces:**
- Consumes: Local Data v3 response, `AxisSource(axis, device_name, resource_name)`
- Produces: `Settings`, `LocalDataClient.fetch(source, from_origin, to_origin) -> list[AxisSample]`, `LocalDataError`

- [ ] **Step 1: source identity, cursor query와 strict validation 실패 테스트 작성**

```python
import asyncio
import httpx
import pytest
from app.local_data import AxisSource, LocalDataClient, LocalDataResponseError


def test_client_uses_encoded_identity_and_origin_cursor() -> None:
    async def handler(request: httpx.Request) -> httpx.Response:
        assert request.url.params["from"] == "101"
        assert request.url.params["to"] == "200"
        assert request.url.params["limit"] == "1000"
        return httpx.Response(200, json={
            "apiVersion": "v3", "statusCode": 200,
            "deviceName": "virtual-acceleration-x-001",
            "resourceName": "acceleration_x_raw", "count": 1,
            "retention": {"maxAge": "10m0s", "maxSamples": 10000},
            "samples": [{"origin": 150, "valueType": "Int32", "value": 7}],
        })
    client = LocalDataClient("http://device.test:59910", 2.0,
        transport=httpx.MockTransport(handler))
    rows = asyncio.run(client.fetch(AxisSource("x", "virtual-acceleration-x-001",
        "acceleration_x_raw"), 101, 200))
    assert rows[0].origin == 150 and rows[0].value == 7


@pytest.mark.parametrize("payload", [
    {"apiVersion": "v2", "statusCode": 200, "samples": []},
    {"apiVersion": "v3", "statusCode": 200, "deviceName": "wrong", "samples": []},
    {"apiVersion": "v3", "statusCode": 200, "samples": [
        {"origin": 1, "valueType": "Float64", "value": 1.0}
    ]},
])
def test_client_rejects_non_contract_payload(payload) -> None:
    source = AxisSource("x", "virtual-acceleration-x-001", "acceleration_x_raw")
    complete = {
        "apiVersion": "v3", "statusCode": 200,
        "deviceName": source.device_name, "resourceName": source.resource_name,
        "count": len(payload.get("samples", [])), "samples": payload.get("samples", []),
    }
    complete.update(payload)
    transport = httpx.MockTransport(lambda request: httpx.Response(200, json=complete))
    client = LocalDataClient("http://device.test:59910", 2.0, transport=transport)
    with pytest.raises(LocalDataResponseError):
        asyncio.run(client.fetch(source, None, 200))
```

- [ ] **Step 2: client 테스트가 실패하는지 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_local_data.py`

Expected: FAIL because client/config modules do not exist.

- [ ] **Step 3: 환경변수 설정과 HTTP client 구현**

```python
class Settings(BaseModel):
    local_data_base_url: str = "http://device-serial-jetson.edgex-edge.svc.cluster.local:59910"
    poll_interval_seconds: float = 0.5
    input_stale_seconds: float = 10.0
    http_timeout_seconds: float = 2.0
    warmup_samples: int = 30
    anomaly_threshold: float = 4.0
    stddev_floor: float = 1.0
    ewma_alpha: float = 0.05
    recent_result_limit: int = 1000
```

```python
@dataclass(frozen=True)
class AxisSource:
    axis: AxisName
    device_name: str
    resource_name: str

class LocalDataClient:
    async def fetch(
        self, source: AxisSource, from_origin: int | None, to_origin: int
    ) -> list[AxisSample]:
        device = quote(source.device_name, safe="")
        resource = quote(source.resource_name, safe="")
        path = f"/api/v3/localdata/device/name/{device}/resource/name/{resource}"
        params = {"to": to_origin, "limit": 1000}
        if from_origin is not None:
            params["from"] = from_origin
        try:
            async with httpx.AsyncClient(
                timeout=self.timeout_seconds, transport=self.transport
            ) as client:
                response = await client.get(f"{self.base_url}{path}", params=params)
                response.raise_for_status()
        except (httpx.RequestError, httpx.HTTPStatusError) as exc:
            raise LocalDataUnavailable(str(exc)) from exc
        payload = self._validated_payload(response, source)
        return [self._axis_sample(item) for item in payload["samples"]]
```

URL path segment는 `urllib.parse.quote(segment, safe="")`로 encode한다. HTTP status,
`apiVersion=v3`, envelope status, source identity, count/list 일치, `Int32`, int-not-bool value,
positive origin을 모두 검증한다. Request/HTTP failure는 `LocalDataUnavailable`, schema 실패는
`LocalDataResponseError`로 구분한다.

`requirements.txt`에는 다음 exact version을 넣는다.

```text
fastapi==0.116.1
httpx==0.28.1
pydantic==2.11.3
uvicorn==0.35.0
pytest==8.4.2
```

- [ ] **Step 4: client 전체 테스트 통과 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_local_data.py`

Expected: PASS for success, timeout/non-2xx, invalid JSON/envelope/source/type/count cases.

- [ ] **Step 5: client 계약 커밋**

```bash
rtk git add edge-orch/sensor-anomaly-demo/app/config.py edge-orch/sensor-anomaly-demo/app/local_data.py edge-orch/sensor-anomaly-demo/requirements.txt edge-orch/sensor-anomaly-demo/tests/test_local_data.py
rtk git commit -m "feat: read strict EdgeX local acceleration data"
```

---

### Task 4: polling runtime와 read-only API

**Files:**
- Create: `edge-orch/sensor-anomaly-demo/app/runtime.py`
- Create: `edge-orch/sensor-anomaly-demo/app/main.py`
- Create: `edge-orch/sensor-anomaly-demo/tests/test_runtime.py`
- Create: `edge-orch/sensor-anomaly-demo/tests/test_api.py`
- Create: `edge-orch/sensor-anomaly-demo/Dockerfile`
- Create: `edge-orch/sensor-anomaly-demo/.dockerignore`

**Interfaces:**
- Consumes: `Settings`, `LocalDataClient`, `FrameJoiner`, `OnlineGaussianDetector`
- Produces: `AnomalyRuntime.start/stop/poll_once/status/results`, FastAPI `create_app(runtime=None, settings=None)` and port 8080 routes

- [ ] **Step 1: 한 cycle에서 세 축을 조회하고 cursor/result/counter를 갱신하는 실패 테스트 작성**

```python
def test_runtime_processes_joined_frames_and_does_not_repeat_origins() -> None:
    fake = FakeLocalDataClient({
        "x": [AxisSample(100, "Int32", 3)],
        "y": [AxisSample(100, "Int32", 4)],
        "z": [AxisSample(100, "Int32", 0)],
    })
    runtime = build_runtime(fake, warmup_samples=1)
    asyncio.run(runtime.poll_once(now_ns=200))
    first = runtime.status(now_ns=200)
    assert first.mode == "live"
    assert first.latest.origin == 100
    assert first.latest.magnitude == 5.0
    assert first.counters.frames_processed == 1
    asyncio.run(runtime.poll_once(now_ns=300))
    assert runtime.status(now_ns=300).counters.frames_processed == 1


def test_runtime_marks_input_degraded_without_discarding_last_result() -> None:
    runtime = warmed_runtime(last_origin=100, input_stale_ns=10)
    state = runtime.status(now_ns=111)
    assert state.status == "degraded"
    assert state.input_state == "stale"
    assert state.latest.origin == 100
```

- [ ] **Step 2: runtime 테스트 실패 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_runtime.py`

Expected: FAIL because `app.runtime` does not exist.

- [ ] **Step 3: cancellable polling runtime와 bounded result ring 구현**

```python
class AnomalyRuntime:
    async def start(self) -> None:
        if self._task is None:
            self._stop.clear()
            self._task = asyncio.create_task(self._run())

    async def stop(self) -> None:
        self._stop.set()
        if self._task is not None:
            await self._task
            self._task = None

    async def poll_once(self, now_ns: int | None = None) -> None:
        current = time.time_ns() if now_ns is None else now_ns
        responses = await asyncio.gather(
            *(self._fetch_source(source, current) for source in self.sources),
            return_exceptions=True,
        )
        for source, response in zip(self.sources, responses, strict=True):
            if isinstance(response, Exception):
                self._record_input_error(source, response)
                continue
            frames = self.joiner.ingest(source.axis, response, current)
            for frame in frames:
                result = self.detector.process(frame)
                self._results.append(result)
                self._last_origin = result.origin
                self._frames_processed += 1

    def status(self, now_ns: int | None = None) -> ServiceStatus:
        current = time.time_ns() if now_ns is None else now_ns
        return self._build_status(current)

    def results(self, limit: int) -> list[InferenceResult]:
        if limit < 1 or limit > self._results.maxlen:
            raise ValueError("limit outside result retention")
        return list(self._results)[-limit:]
```

세 fetch는 `asyncio.gather(*requests, return_exceptions=True)`와 같은 실패 격리 방식으로 실행한다. source별 cursor는 성공
응답의 최대 origin으로만 전진한다. 일부 source 오류여도 성공 sample은 pending joiner에
전달하지만 완전 frame이 아니면 inference하지 않는다. worker loop는 0.5초 주기를
`Event.wait()` timeout으로 구현해 shutdown이 즉시 끝나게 한다. 최근 결과는 `deque(maxlen=1000)`다.

- [ ] **Step 4: runtime 상태·복구 테스트 통과 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_runtime.py`

Expected: PASS for warm-up/normal/anomaly/degraded, cursor, partial failure, recovery and bounded history.

- [ ] **Step 5: API contract 실패 테스트 작성**

```python
from fastapi.testclient import TestClient
from app.main import create_app


def test_status_results_and_probes_are_read_only() -> None:
    runtime = FakeRuntime(status_payload=normal_status(), results_payload=[result(100)])
    with TestClient(create_app(runtime=runtime)) as client:
        assert client.get("/healthz").status_code == 200
        assert client.get("/readyz").status_code == 200
        status = client.get("/api/v1/status")
        assert status.status_code == 200
        assert status.json()["mode"] == "live"
        assert client.get("/api/v1/results?limit=1").json()["count"] == 1
        assert client.get("/api/v1/results?limit=0").status_code == 422
        assert client.post("/api/v1/status").status_code == 405
```

- [ ] **Step 6: app factory, lifespan과 Docker image 정의 구현**

```python
def create_app(
    runtime: AnomalyRuntime | None = None,
    settings: Settings | None = None,
) -> FastAPI:
    selected_settings = settings or Settings.from_env()
    selected_runtime = runtime or build_runtime(selected_settings)

    @asynccontextmanager
    async def lifespan(_: FastAPI):
        await selected_runtime.start()
        yield
        await selected_runtime.stop()

    app = FastAPI(title="sensor-anomaly-demo", version="1.0.0", lifespan=lifespan)

    @app.get("/healthz")
    async def healthz() -> dict[str, str]:
        return {"status": "ok"}

    @app.get("/readyz")
    async def readyz() -> dict[str, str]:
        return {"status": "ready"}

    @app.get("/api/v1/status", response_model=ServiceStatus)
    async def status() -> ServiceStatus:
        return selected_runtime.status()

    @app.get("/api/v1/results", response_model=ResultEnvelope)
    async def results(limit: int = Query(default=100, ge=1, le=1000)) -> ResultEnvelope:
        rows = selected_runtime.results(limit)
        return ResultEnvelope(count=len(rows), results=rows)

    return app
```

Dockerfile은 `python:3.11-slim`, non-root UID/GID 65532, read-only compatible `/tmp`, port
8080과 다음 command를 사용한다.

```dockerfile
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
```

- [ ] **Step 7: 전체 새 서비스 테스트와 import smoke 통과 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests`

Expected: PASS.

Run: `rtk python -m compileall -q edge-orch/sensor-anomaly-demo/app`

Expected: exit 0.

- [ ] **Step 8: runtime/API/container 커밋**

```bash
rtk git add edge-orch/sensor-anomaly-demo
rtk git commit -m "feat: serve live Jetson anomaly demo status"
```

---

### Task 5: Jetson workload, NetworkPolicy와 Argo application

**Files:**
- Create: `edge-orch/sensor-anomaly-demo/k8s/kustomization.yaml`
- Create: `edge-orch/sensor-anomaly-demo/k8s/workload.yaml`
- Create: `edge-orch/sensor-anomaly-demo/k8s/networkpolicy.yaml`
- Create: `edge-orch/sensor-anomaly-demo/tests/test_manifest.py`
- Modify: `edge-orch-argocd/argocd-apps.yaml`
- Create: `edge-orch-argocd/sensor-anomaly-demo-app.yaml`

**Interfaces:**
- Consumes: image repository `192.168.0.56:5000/sensor-anomaly-demo`, Device Service FQDN/port 59910
- Produces: `Service/sensor-anomaly-demo:8080`, `Deployment/sensor-anomaly-demo` on Jetson, Argo Application `edge-orch-sensor-anomaly-demo`

- [ ] **Step 1: manifest 불변조건 실패 테스트 작성**

```python
def test_demo_workload_is_edge_local_read_only_and_bounded() -> None:
    resources = render_kustomize(ROOT / "edge-orch/sensor-anomaly-demo/k8s")
    deployment = one(resources, "Deployment", "sensor-anomaly-demo")
    pod = deployment["spec"]["template"]
    assert pod["spec"]["nodeSelector"]["kubernetes.io/hostname"] == "etri-dev0001-jetorn"
    assert pod["metadata"]["labels"]["edge-ai.io/local-data-client"] == "true"
    container = pod["spec"]["containers"][0]
    assert "@sha256:" in container["image"]
    env = {item["name"]: item["value"] for item in container["env"]}
    assert env["LOCAL_DATA_BASE_URL"] == (
        "http://device-serial-jetson.edgex-edge.svc.cluster.local:59910"
    )
    assert pod["spec"].get("hostNetwork") is not True
    assert "resources" in container and container["resources"]["limits"]
```

테스트는 render text 전체에서 `clusterIP:`의 숫자값, `hostNetwork: true`, `mqtt`,
`influx`, PVC를 금지하고, ingress/egress policy의 label/port와 old vision Application
부재도 확인한다.

- [ ] **Step 2: manifest 테스트 실패 확인**

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_manifest.py`

Expected: FAIL because k8s files/Application do not exist.

- [ ] **Step 3: ARM64 image를 먼저 build/push하고 immutable digest 확보**

```bash
rtk docker buildx build --platform linux/arm64 -t 192.168.0.56:5000/sensor-anomaly-demo:20260722-live-v1 --push edge-orch/sensor-anomaly-demo
rtk docker buildx imagetools inspect 192.168.0.56:5000/sensor-anomaly-demo:20260722-live-v1
```

Expected: registry manifest digest가 `sha256:`와 64자리 hexadecimal로 출력된다. 다음
Step의 image 필드에는 이 명령에서 방금 출력된 exact digest만 사용한다.

- [ ] **Step 4: Service/Deployment/NetworkPolicy/Kustomization 작성**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensor-anomaly-demo
  namespace: edgex-edge
spec:
  replicas: 1
  strategy: {type: Recreate}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sensor-anomaly-demo
        edge-ai.io/local-data-client: "true"
    spec:
      nodeSelector:
        kubernetes.io/hostname: etri-dev0001-jetorn
      automountServiceAccountToken: false
      securityContext:
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: sensor-anomaly-demo
          env:
            - name: LOCAL_DATA_BASE_URL
              value: http://device-serial-jetson.edgex-edge.svc.cluster.local:59910
            - {name: POLL_INTERVAL_SECONDS, value: "0.5"}
            - {name: INPUT_STALE_SECONDS, value: "10"}
          ports: [{name: http, containerPort: 8080}]
```

위 snippet의 container 첫 field로 `image`를 추가하되 값은 Step 3 buildx가 출력한
repository digest를 복사한 exact `repository@digest` 한 줄만 사용한다. 임의 digest나
mutable tag를 쓰지 않는다. startup/readiness/liveness probe,
25m/64Mi request, 250m/128Mi limit, non-root 65532, read-only root filesystem와 `/tmp`
`emptyDir`를 포함한다.

NetworkPolicy는 DNS UDP/TCP 53, 같은 namespace의
`app.kubernetes.io/name=device-serial-jetson` TCP 59910 egress와
`default` namespace의 `app=state-aggregator` TCP 8080 ingress 의도만 선언한다. Flannel
미집행은 policy annotation이 아니라 운영 문서에 기록한다.

- [ ] **Step 5: Argo Application을 fake vision에서 실제 sensor demo로 교체**

`argocd-apps.yaml`에서 `edge-orch-factory-vision-inspection-ai` 전체 YAML document를
제거하고, 다음 document를 별도 `sensor-anomaly-demo-app.yaml`로 추가한다. 별도 파일은
live apply 시 다른 Application의 현재 exact revision pin을 덮어쓰지 않게 한다.

```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: edge-orch-sensor-anomaly-demo
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dsa04156/edge-ai-workspace.git
    path: edge-orch/sensor-anomaly-demo/k8s
    targetRevision: main
    kustomize: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: edgex-edge
  syncPolicy:
    automated: {prune: true, selfHeal: true}
```

- [ ] **Step 6: render와 manifest 테스트 실행**

Run: `rtk kubectl kustomize edge-orch/sensor-anomaly-demo/k8s`

Expected: Service, Deployment and NetworkPolicy documents render without errors.

Run: `rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_manifest.py`

Expected: all assertions PASS and image field uses the exact Step 3 digest.

- [ ] **Step 7: manifest 구조 커밋**

```bash
rtk git add edge-orch/sensor-anomaly-demo/k8s edge-orch/sensor-anomaly-demo/tests/test_manifest.py edge-orch-argocd/argocd-apps.yaml edge-orch-argocd/sensor-anomaly-demo-app.yaml
rtk git commit -m "deploy: define Jetson sensor anomaly service"
```

---

### Task 6: state-aggregator client와 failure-isolated API

**Files:**
- Create: `edge-orch/state-aggregator/app/service_demo_models.py`
- Create: `edge-orch/state-aggregator/app/service_demo.py`
- Create: `edge-orch/state-aggregator/tests/test_service_demo.py`
- Modify: `edge-orch/state-aggregator/app/config.py`
- Modify: `edge-orch/state-aggregator/app/main.py`
- Modify: `edge-orch/state-aggregator/k8s/deployment.yaml`

**Interfaces:**
- Consumes: `GET {service_demo_url}/api/v1/status`
- Produces: `ServiceDemoClient.get_state() -> ServiceDemoState`, `GET /state/service-demo`

- [ ] **Step 1: 정상 schema와 upstream 장애 격리 실패 테스트 작성**

```python
def test_service_demo_endpoint_normalizes_live_upstream(monkeypatch) -> None:
    async def healthy():
        return live_service_demo_state(status="normal", origin=100)
    monkeypatch.setattr(service_demo_client, "get_state", healthy)
    with TestClient(app) as client:
        payload = client.get("/state/service-demo").json()
    assert payload["status"] == "normal"
    assert payload["mode"] == "live"
    assert payload["binding"]["consumer"] == "sensor-anomaly-demo"
    assert payload["binding"]["node"] == "etri-dev0001-jetorn"


def test_service_demo_endpoint_returns_degraded_snapshot_on_timeout(monkeypatch) -> None:
    async def timeout():
        raise httpx.ConnectTimeout("unreachable")
    monkeypatch.setattr(service_demo_client, "get_state", timeout)
    with TestClient(app) as client:
        response = client.get("/state/service-demo")
        devices = client.get("/state/devices")
    assert response.status_code == 200
    assert response.json()["status"] == "degraded"
    assert response.json()["observation_error"] == "sensor anomaly demo unavailable: ConnectTimeout"
    assert devices.status_code == 200
```

- [ ] **Step 2: 중앙 service demo 테스트 실패 확인**

Run: `rtk pytest -q edge-orch/state-aggregator/tests/test_service_demo.py`

Expected: FAIL because models/client/route are absent.

- [ ] **Step 3: strict Pydantic schema와 read-only client 구현**

```python
ServiceDemoStatus = Literal["starting", "warming_up", "normal", "anomaly", "degraded"]

class ServiceDemoBinding(BaseModel):
    physical_source: str = "arduino-001"
    device_service: str = "device-serial-jetson"
    devices: list[str]
    consumer: str = "sensor-anomaly-demo"
    node: str = "etri-dev0001-jetorn"

class ServiceDemoState(BaseModel):
    generated_at: datetime
    mode: Literal["live", "unavailable"]
    status: ServiceDemoStatus
    input_state: str
    model_state: str
    binding: ServiceDemoBinding
    latest: ServiceDemoLatest | None = None
    model: ServiceDemoModel | None = None
    counters: ServiceDemoCounters
    observation_error: str | None = None
```

`ServiceDemoClient`는 `httpx.AsyncClient(timeout=2.0)`로 upstream을 조회하고, non-2xx,
invalid JSON/schema를 각각 명시적 `ServiceDemoBackendError`/
`ServiceDemoResponseError`로 변환한다. `main.py` route는 이 오류와 `httpx.HTTPError`를
잡아 `degraded_service_demo_state()`를 반환한다.

Settings 기본값:

```python
service_demo_url = "http://sensor-anomaly-demo.edgex-edge.svc.cluster.local:8080"
service_demo_timeout_seconds = 2.0
```

- [ ] **Step 4: state-aggregator deployment에 FQDN env 추가**

```yaml
- name: SENSOR_ANOMALY_DEMO_URL
  value: http://sensor-anomaly-demo.edgex-edge.svc.cluster.local:8080
- name: SENSOR_ANOMALY_DEMO_TIMEOUT_SECONDS
  value: "2"
```

- [ ] **Step 5: 중앙 API와 기존 dashboard 회귀 테스트 실행**

Run: `rtk pytest -q edge-orch/state-aggregator/tests/test_service_demo.py edge-orch/state-aggregator/tests/test_dashboard_resilience.py edge-orch/state-aggregator/tests/test_api.py`

Expected: PASS.

- [ ] **Step 6: 중앙 API 커밋**

```bash
rtk git add edge-orch/state-aggregator/app edge-orch/state-aggregator/tests/test_service_demo.py edge-orch/state-aggregator/k8s/deployment.yaml
rtk git commit -m "feat: expose live sensor demo state"
```

---

### Task 7: 서비스 데모 dashboard panel

**Files:**
- Create: `edge-orch/state-aggregator/app/static/service-demo.js`
- Create: `edge-orch/state-aggregator/app/static/service-demo.css`
- Create: `edge-orch/state-aggregator/tests/test_service_demo_dashboard.js`
- Modify: `edge-orch/state-aggregator/app/static/index.html`

**Interfaces:**
- Consumes: browser `GET /state/service-demo`
- Produces: Overview panel DOM IDs `serviceDemoState`, `serviceDemoFlow`, `serviceDemoValues`, `serviceDemoModel`, `serviceDemoError`

- [ ] **Step 1: pure view-model과 safe rendering 실패 테스트 작성**

```javascript
const test = require("node:test");
const assert = require("node:assert/strict");
const { buildServiceDemoView } = require("../app/static/service-demo.js");

test("builds an honest live device-to-consumer view", () => {
  const view = buildServiceDemoView({
    mode: "live", status: "anomaly", input_state: "fresh",
    binding: {
      physical_source: "arduino-001",
      device_service: "device-serial-jetson",
      devices: ["virtual-acceleration-x-001", "virtual-acceleration-y-001", "virtual-acceleration-z-001"],
      consumer: "sensor-anomaly-demo", node: "etri-dev0001-jetorn",
    },
    latest: {origin: 100, values: {x: 1, y: 2, z: 3}, magnitude: 3.742, score: 5.1, anomaly: true},
    model: {algorithm: "online-gaussian-baseline-v1", sample_count: 40, threshold: 4.0},
    observation_error: null,
  });
  assert.equal(view.badge, "ANOMALY");
  assert.match(view.flow, /arduino-001.*device-serial-jetson.*sensor-anomaly-demo/);
  assert.match(view.score, /5.10 \/ 4.00/);
  assert.equal(view.copy, "실측 raw 변화 이상 탐지 · Jetson local inference");
});

test("shows unavailable observation without inventing zero values", () => {
  const view = buildServiceDemoView({mode: "unavailable", status: "degraded", latest: null,
    observation_error: "sensor anomaly demo unavailable: ConnectTimeout", binding: {devices: []}});
  assert.equal(view.score, "관측 불가");
  assert.match(view.error, /ConnectTimeout/);
});
```

- [ ] **Step 2: JS 테스트 실패 확인**

Run: `rtk node --test edge-orch/state-aggregator/tests/test_service_demo_dashboard.js`

Expected: FAIL because `service-demo.js` is absent.

- [ ] **Step 3: pure formatter, fetch loop와 DOM renderer 구현**

```javascript
function buildServiceDemoView(data) {
  const latest = data.latest || null;
  const model = data.model || null;
  const binding = data.binding || {};
  const score = latest && model
    ? `${Number(latest.score).toFixed(2)} / ${Number(model.threshold).toFixed(2)}`
    : "관측 불가";
  return {
    badge: String(data.status || "degraded").toUpperCase(),
    flow: [binding.physical_source, binding.device_service, binding.consumer]
      .filter(Boolean).join(" → ") || "source binding 관측 불가",
    score,
    values: latest
      ? `X ${latest.values.x} · Y ${latest.values.y} · Z ${latest.values.z} · |a| ${latest.magnitude}`
      : "관측 불가",
    model: model
      ? `${model.algorithm} · ${model.sample_count} samples`
      : "model 관측 불가",
    copy: "실측 raw 변화 이상 탐지 · Jetson local inference",
    error: data.observation_error || "",
  };
}

function renderServiceDemo(data, documentRef = document) {
  const view = buildServiceDemoView(data);
  documentRef.getElementById("serviceDemoState").textContent = view.badge;
  documentRef.getElementById("serviceDemoFlow").textContent = view.flow;
  documentRef.getElementById("serviceDemoValues").textContent = `${view.values} · score ${view.score}`;
  documentRef.getElementById("serviceDemoModel").textContent = `${view.model} · ${view.copy}`;
  const error = documentRef.getElementById("serviceDemoError");
  error.textContent = view.error;
  error.hidden = !view.error;
}

async function refreshServiceDemo(fetchFn = fetch) {
  try {
    const response = await fetchFn("/state/service-demo", {cache: "no-store"});
    if (!response.ok) throw new Error(`HTTP ${response.status}`);
    renderServiceDemo(await response.json());
  } catch (error) {
    renderServiceDemo({
      mode: "unavailable", status: "degraded", latest: null,
      binding: {devices: []}, observation_error: `dashboard fetch failed: ${error.name}`,
    });
  }
}

if (typeof module !== "undefined") module.exports = { buildServiceDemoView };
if (typeof window !== "undefined") {
  window.addEventListener("DOMContentLoaded", () => {
    refreshServiceDemo();
    window.setInterval(refreshServiceDemo, 5000);
  });
}
```

모든 사용자/서버 문자열은 `textContent`로 렌더링하고 `innerHTML`에 삽입하지 않는다.
origin은 nanosecond integer를 millisecond `Date`로 변환하되 정밀도 손실을 피하기 위해
문자열→`BigInt`→나눗셈을 사용한다. latest가 없으면 `0` 대신 `관측 불가`를 표시한다.

- [ ] **Step 4: overview HTML panel과 scoped CSS 추가**

```html
<section class="panel service-demo-panel dashboard-page active" data-page="overview" aria-labelledby="serviceDemoTitle">
  <div class="panel-head">
    <div><p class="section-kicker">Live Service Demo</p><h2 id="serviceDemoTitle">Jetson Sensor Anomaly Detection</h2></div>
    <strong id="serviceDemoState" class="service-demo-badge">STARTING</strong>
  </div>
  <p id="serviceDemoFlow" class="service-demo-flow">source binding pending</p>
  <div id="serviceDemoValues" class="service-demo-values"></div>
  <div id="serviceDemoModel" class="service-demo-model"></div>
  <p id="serviceDemoError" class="service-demo-error" hidden></p>
</section>
```

head에 `/static/service-demo.css?v=live-sensor-20260722`, body 끝에
`/static/service-demo.js?v=live-sensor-20260722`를 추가한다. 상태 색상은 normal/anomaly/
degraded에만 사용하고 색상 외 badge text도 항상 표시한다.

- [ ] **Step 5: JS와 기존 dashboard 회귀 테스트 실행**

Run: `rtk node --test edge-orch/state-aggregator/tests/test_service_demo_dashboard.js edge-orch/state-aggregator/tests/test_dashboard_source_isolation.js`

Expected: PASS.

Run: `rtk pytest -q edge-orch/state-aggregator/tests`

Expected: PASS.

- [ ] **Step 6: dashboard panel 커밋**

```bash
rtk git add edge-orch/state-aggregator/app/static edge-orch/state-aggregator/tests/test_service_demo_dashboard.js
rtk git commit -m "feat: show live Jetson anomaly demo on dashboard"
```

---

### Task 8: 활성 문서와 운영 검증 계약 최신화

**Files:**
- Modify: `docs/프로젝트-범위.md`
- Modify: `docs/서비스-데모-시나리오.md`
- Modify: `docs/디바이스-서비스-연결.md`
- Modify: `docs/현재-데모-경로.md`
- Modify: `docs/단계별-추진계획.md`
- Modify: `docs/저장소-구조.md`
- Modify: `docs/ops/현재-데모-운영-절차.md`
- Modify: `docs/일일-기록.md`
- Modify: `docs/wiki/운영-모델.md`
- Modify: `docs/wiki/운영-진입점.md`
- Modify: `docs/문서-안내.md`
- Regenerate: `docs/html/**`
- Test: `tests/test_docs_consistency.py`
- Test: `tests/test_docs_html_search.py`
- Test: `tests/test_docs_editor_server.py`

**Interfaces:**
- Consumes: 구현된 API, manifests, live 검증 명령/결과
- Produces: 서비스 데모의 현재 범위, 실행 절차, PASS/미완료 경계와 생성 HTML

- [ ] **Step 1: `docs-refresh-ko` skill을 읽고 현재 문서 검증 명령을 확인**

Run: `rtk cat /home/jinuk/codex-work/jinuk/.agents/skills/docs-refresh-ko/SKILL.md`

Expected: 전체 skill 내용을 읽고 요구하는 inventory/build/link 검증 절차를 이 Task에 적용한다.

- [ ] **Step 2: 코드 기준 문서 불일치 검색**

Run: `rtk rg -n "vision_stage_runner|factory-vision-inspection-ai|가짜|서비스 데모|Local Data API|동적 workflow|MQTT|InfluxDB" docs --glob '!docs/archive/**' --glob '!docs/generated/**'`

Expected: 현재 서비스 데모로 잘못 설명하는 문장과 갱신 대상 목록을 확보한다.

- [ ] **Step 3: 활성 문서에 정확한 현재 경로와 한계 반영**

모든 활성 문서는 다음 동일한 흐름을 사용한다.

```text
Arduino Serial
  -> device-serial-jetson
  -> acceleration virtual Device 3개 / Local Data API
  -> sensor-anomaly-demo @ Jetson
  -> online-gaussian-baseline-v1
  -> /state/service-demo
  -> dashboard

raw history -> EdgeX Core Data/PostgreSQL
```

다음 문구를 PASS 경계에 포함한다.

```text
실제 증가하는 raw origin과 Jetson local inference 경로는 검증한다. detector의 anomaly
상태 전이는 결정론적 테스트로 검증하며, 사람이 실제 장치를 움직이는 현장 시연 전에는
실제 고장 검출 실증으로 표현하지 않는다. polling은 push stream이 아니다.
```

동적 Workflow Builder는 계속 browser-local dry-run이며 이번 고정 서비스 Deployment를
동적 오케스트레이션 완성으로 설명하지 않는다.

- [ ] **Step 4: 운영 절차에 exact readback 명령 추가**

```bash
rtk kubectl -n edgex-edge get deployment,pod,service -l app.kubernetes.io/name=sensor-anomaly-demo -o wide
rtk kubectl -n edgex-edge exec deploy/sensor-anomaly-demo -- python -c 'import json,urllib.request; print(json.load(urllib.request.urlopen("http://127.0.0.1:8080/api/v1/status")))'
rtk kubectl get --raw '/api/v1/namespaces/default/services/http:state-aggregator:8000/proxy/state/service-demo'
```

Expected fields: `mode=live`, 세 device identity, positive/increasing `latest.origin`,
`model.algorithm=online-gaussian-baseline-v1`, `observation_error=null`.

- [ ] **Step 5: HTML 재생성과 문서 테스트**

실제 skill이 지정한 build command를 실행한다. 저장소의 현재 기본 명령이 유지되면:

Run: `rtk python scripts/build-docs-html.py`

Expected: generated HTML이 새 문서 navigation/content를 포함한다.

Run: `rtk pytest -q tests/test_docs_consistency.py tests/test_docs_html_search.py tests/test_docs_editor_server.py`

Expected: PASS.

- [ ] **Step 6: 문서 커밋**

```bash
rtk git add docs
rtk git commit -m "docs: document live sensor anomaly service demo"
```

---

### Task 9: 전체 검증, ARM64/AMD64 image, live 배포와 push

**Files:**
- Modify: `edge-orch/state-aggregator/k8s/deployment.yaml` with actual AMD64 image digest
- Modify: `docs/일일-기록.md` with exact live evidence if build/deploy values changed after Task 8

**Interfaces:**
- Consumes: Tasks 1–8 artifacts and local registry `192.168.0.56:5000`
- Produces: immutable deployed revisions, Argo Healthy/Synced, live API/dashboard evidence, remote branch

- [ ] **Step 1: verification-before-completion skill을 읽고 completion evidence 목록 확정**

Run: `rtk cat /home/jinuk/.codex/plugins/cache/openai-curated-remote/superpowers/6.1.1/skills/verification-before-completion/SKILL.md`

Expected: 전체 skill 내용을 읽고 아래 fresh commands의 출력만 완료 근거로 사용한다.

- [ ] **Step 2: 모든 unit/integration/static/manifest/docs 테스트 fresh 실행**

```bash
rtk pytest -q edge-orch/sensor-anomaly-demo/tests
rtk pytest -q edge-orch/state-aggregator/tests
rtk node --test edge-orch/state-aggregator/tests/test_service_demo_dashboard.js edge-orch/state-aggregator/tests/test_dashboard_source_isolation.js
rtk pytest -q edgex/k8s/tests
rtk pytest -q tests/test_docs_consistency.py tests/test_docs_html_search.py tests/test_docs_editor_server.py
rtk git diff --check
```

Expected: all PASS, diff check empty. 실제 docs test path는 Task 8에서 확인한 target을 사용한다.

- [ ] **Step 3: Kustomize render/server validation과 금지 경로 검색**

```bash
rtk kubectl kustomize edge-orch/sensor-anomaly-demo/k8s
rtk kubectl apply --dry-run=server -k edge-orch/sensor-anomaly-demo/k8s
rtk kubectl apply --dry-run=server -k edge-orch/state-aggregator/k8s
rtk rg -n "hostNetwork: true|clusterIP: [0-9]|mqtt|influx|edge-agent" edge-orch/sensor-anomaly-demo edge-orch/state-aggregator/k8s
```

Expected: both dry-runs succeed; prohibited runtime routes/components have no match in new demo manifests.

- [ ] **Step 4: ARM64 demo image digest와 manifest 일치 재검증**

```bash
rtk docker buildx imagetools inspect 192.168.0.56:5000/sensor-anomaly-demo:20260722-live-v1
rtk rg -n "image: 192.168.0.56:5000/sensor-anomaly-demo@sha256:" edge-orch/sensor-anomaly-demo/k8s/workload.yaml
```

Expected: registry가 반환한 digest와 workload image의 digest가 정확히 같다. 다르면 code
변경 후 image를 재build한 뒤 실제 새 digest를 `apply_patch`로 반영하고 Task 5 manifest
test를 다시 실행한다.

- [ ] **Step 5: AMD64 state-aggregator image build/push와 immutable digest 확보**

```bash
rtk docker buildx build --platform linux/amd64 -t 192.168.0.56:5000/state-aggregator:20260722-service-demo-v1 --push edge-orch/state-aggregator
rtk docker buildx imagetools inspect 192.168.0.56:5000/state-aggregator:20260722-service-demo-v1
```

Expected: registry manifest digest를 deployment의
repository 문자열 `192.168.0.56:5000/state-aggregator`, literal `@sha256:`와 inspect가
출력한 정확한 64자리 hexadecimal digest를 연결해 deployment에 반영한다.

- [ ] **Step 6: digest 테스트, render와 state-aggregator image commit**

```bash
rtk pytest -q edge-orch/sensor-anomaly-demo/tests/test_manifest.py
rtk kubectl kustomize edge-orch/sensor-anomaly-demo/k8s
rtk git diff --check
rtk git add edge-orch/state-aggregator/k8s/deployment.yaml
rtk git commit -m "deploy: pin live sensor demo images"
```

Expected: image assertions PASS, mutable/build-required tag 없음.

- [ ] **Step 7: branch push 후 Argo Applications 동기화**

Run: `rtk git push origin agent/retire-edge-agents`

Expected: remote branch advances to local HEAD.

현재 live Argo Applications는 서로 다른 exact commit SHA로 pin돼 있으므로 remote branch에
push한 뒤 `rtk git rev-parse HEAD`가 출력한 exact SHA를 다음 patch에 사용한다. `main`으로
강제 push하거나 다른 Application의 revision을 일괄 변경하지 않는다.

- [ ] **Step 8: exact old Application 확인 후 퇴역 및 new Application 확인**

```bash
rtk kubectl -n argocd get application edge-orch-factory-vision-inspection-ai -o jsonpath='{.metadata.name}{" target="}{.spec.source.targetRevision}{" resources="}{len(.status.resources)}{"\n"}'
rtk kubectl -n argocd apply -f edge-orch-argocd/sensor-anomaly-demo-app.yaml
```

old Application이 실제 존재하는 것을 확인한 뒤 exact target만 삭제한다. 새 Application과
state-aggregator에는 Step 7의 exact pushed SHA를 각각 merge patch하고 hard refresh한다.

```bash
rtk kubectl -n argocd delete application edge-orch-factory-vision-inspection-ai
rtk kubectl -n argocd annotate application edge-orch-sensor-anomaly-demo edge-orch-state-aggregator argocd.argoproj.io/refresh=hard --overwrite
```

delete와 annotate 사이에 Step 7의 `rtk git rev-parse HEAD`가 출력한 40자리 SHA를 두 개의
merge patch JSON `spec.source.targetRevision`에 그대로 넣어 각각 `rtk kubectl -n argocd
patch application edge-orch-sensor-anomaly-demo --type merge -p JSON`과
`edge-orch-state-aggregator` 명령을 실행한다. Expected: old fake demo Application과 관리
workload가 제거되고 new Application이 생성된다.
삭제 대상과 복구 방법(동일 Git revision의 Application 재적용)을 최종 보고에 남긴다.

- [ ] **Step 9: Argo sync/health와 node placement 확인**

```bash
rtk kubectl -n argocd get application edge-orch-sensor-anomaly-demo edge-orch-state-aggregator edgex-telemetry -o custom-columns=NAME:.metadata.name,SYNC:.status.sync.status,HEALTH:.status.health.status,REV:.status.sync.revision
rtk kubectl -n edgex-edge rollout status deployment/sensor-anomaly-demo --timeout=180s
rtk kubectl -n default rollout status deployment/state-aggregator --timeout=180s
rtk kubectl -n edgex-edge get pod -l app.kubernetes.io/name=sensor-anomaly-demo -o custom-columns=NAME:.metadata.name,NODE:.spec.nodeName,READY:.status.containerStatuses[0].ready,RESTARTS:.status.containerStatuses[0].restartCount,IMAGE:.status.containerStatuses[0].imageID
```

Expected: all Applications Synced/Healthy, demo Pod on `etri-dev0001-jetorn`, Ready true,
imageID matches pinned digest.

- [ ] **Step 10: live sensor, model과 dashboard end-to-end readback**

```bash
rtk kubectl -n edgex-edge exec deploy/sensor-anomaly-demo -- python -c 'import json,urllib.request,time; u="http://127.0.0.1:8080/api/v1/status"; a=json.load(urllib.request.urlopen(u)); time.sleep(2); b=json.load(urllib.request.urlopen(u)); print(json.dumps({"first":a,"second":b},sort_keys=True))'
rtk kubectl get --raw '/api/v1/namespaces/default/services/http:state-aggregator:8000/proxy/state/service-demo'
rtk kubectl get --raw '/api/v1/namespaces/default/services/http:state-aggregator:8000/proxy/state/devices'
```

Expected:

- both status snapshots have `mode=live`, correct three devices and null observation error;
- second `latest.origin` is greater than first and framesProcessed increases;
- model reaches `ready` after warm-up and score/threshold are finite;
- central endpoint matches local latest origin/status;
- the three source Devices remain available/fresh in `/state/devices`.

- [ ] **Step 11: dashboard asset and fake demo absence 확인**

```bash
rtk kubectl get --raw '/api/v1/namespaces/default/services/http:state-aggregator:8000/proxy/dashboard'
rtk kubectl get --raw '/api/v1/namespaces/default/services/http:state-aggregator:8000/proxy/static/service-demo.js?v=live-sensor-20260722'
rtk kubectl get deployment,pod -A -l app=factory-vision-inspection-ai
```

Expected: dashboard HTML references new CSS/JS, JS contains `/state/service-demo`, old label returns no resources.

- [ ] **Step 12: EdgeMesh 불변성과 금지 컴포넌트 부재 확인**

```bash
rtk kubectl -n kubeedge get daemonset edgemesh-agent -o jsonpath='{.metadata.generation}{" "}{.spec.template.spec.containers[0].image}{"\n"}'
rtk kubectl get deployment,statefulset -A -o name
```

Expected: EdgeMesh generation/image equals pre-change recorded value; no demo-scoped MQTT broker,
InfluxDB or Agent workload exists. 기존 중앙 `edgex-messagebus`와 PostgreSQL은 유지된다.

- [ ] **Step 13: live evidence 문서 반영, 최종 docs build/test와 push**

정확한 revision, image digest, Argo 상태, origin 증가와 한계를 `docs/일일-기록.md`에
`apply_patch`로 기록한다. 생성 HTML을 다시 빌드하고 문서 검증을 실행한다.

```bash
rtk python scripts/build-docs-html.py
rtk pytest -q tests/test_docs_consistency.py tests/test_docs_html_search.py tests/test_docs_editor_server.py
rtk git diff --check
rtk git add docs
rtk git commit -m "docs: record live sensor demo verification"
rtk git push origin agent/retire-edge-agents
```

Expected: remote branch equals local HEAD and worktree is clean.

- [ ] **Step 14: requirement-by-requirement completion audit**

다음 증거를 각각 PASS/FAIL로 표로 작성하고 하나라도 missing이면 goal을 complete로 표시하지
않는다.

```text
실제 센서 origin 증가
같은-origin X/Y/Z 결합
online model warm-up/score/status
Jetson placement와 immutable ARM64 image
central /state/service-demo
dashboard panel/assets
EdgeX source Device 3개 회귀
old fake Application/workload 부재
no fixed runtime IP/MQTT/Influx/Agent
EdgeMesh unchanged
tests/render/Argo/docs/remote revision
```
