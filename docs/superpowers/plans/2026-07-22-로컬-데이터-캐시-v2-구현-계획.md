# 로컬 데이터 캐시 v2 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Serial과 Sense HAT Device Service의 최근 데이터 저장을 공통 bounded ring cache로 교체하고 64MiB 슬롯 예산과 운영 관측값을 제공한다.

**Architecture:** EdgeX SDK와 분리된 `edgex/local-data-cache` 제네릭 Go module이 series별 링버퍼, 시간·개수·전체 byte 제한과 stats를 소유한다. 각 Device Service는 얇은 타입 adapter, Driver 설정, EdgeX custom metrics와 `/api/v3/localdata/stats` route만 소유하며 중앙 AsyncValues 흐름은 유지한다.

**Tech Stack:** Go 1.25 generics, EdgeX Device SDK v4 MetricsManager, Echo custom routes, Kustomize, ko ARM64 image build, pytest/Go race tests

## Global Constraints

- 운영 배포 진입점은 `edgex/k8s`와 Argo CD Application `edgex-telemetry`다.
- 고정 ClusterIP, PodIP와 node IP를 설정하거나 우회 경로로 사용하지 않는다.
- 캐시 기본값은 `10m`, series별 `10000`, Device Service별 sample slot `67108864` bytes다.
- 기존 latest/recent HTTP status, 정렬, limit과 JSON field는 깨지지 않는다.
- 물리 디바이스와 Event/Reading 권위는 EdgeX이며 KubeEdge DeviceStatus를 병행 plane으로 추가하지 않는다.
- SQLite Outbox, offline replay, Redis와 카메라 frame cache는 이번 구현 범위가 아니다.
- 구현은 `main` worktree에서 직접 커밋하되 원래 루트 worktree의 사용자 변경은 건드리지 않는다.

---

### Task 1: 공통 제네릭 링 캐시

**Files:**
- Create: `edgex/local-data-cache/go.mod`
- Create: `edgex/local-data-cache/cache.go`
- Create: `edgex/local-data-cache/ring.go`
- Create: `edgex/local-data-cache/cache_test.go`

**Interfaces:**
- Produces: `localcache.New[T](Options) (*Cache[T], error)`
- Produces: `Append(device, resource string, Sample[T]) error`, `Latest`, `Query`, `DeleteDevice`, `Stats`
- Produces: `Options{MaxAge, MaxSamplesPerSeries, MaxBytes, Observer}` and cumulative `Stats`

- [ ] **Step 1: RED — 링 overwrite, age, 전체 byte quota와 out-of-order 계약 테스트 작성**

```go
func TestCacheRebalancesSeriesWithinByteBudget(t *testing.T) {
    slot := int64(unsafe.Sizeof(Sample[int32]{}))
    cache, err := New[int32](Options{
        MaxAge: time.Hour, MaxSamplesPerSeries: 4, MaxBytes: slot * 4,
    })
    require.NoError(t, err)
    for _, resource := range []string{"a", "b"} {
        for origin := int64(1); origin <= 4; origin++ {
            require.NoError(t, cache.Append("device", resource, Sample[int32]{Origin: origin, ValueType: "Int32", Value: int32(origin)}))
        }
    }
    assert.Equal(t, []int64{3, 4}, origins(cache.Query("device", "a", 0, 10, 10, 10)))
    assert.LessOrEqual(t, cache.Stats().AllocatedBytes, slot*4)
}
```

- [ ] **Step 2: RED 확인**

Run: `cd edgex/local-data-cache && go test ./...`

Expected: `New`, `Options`, `Cache`가 없어 compile FAIL.

- [ ] **Step 3: GREEN — 동적 capacity 링과 cache coordinator 최소 구현**

`ring.go`는 정상 append O(1), quota shrink, ordered copy와 out-of-order stable sort slow path를 구현한다. `cache.go`는 series map, RWMutex, slot budget, 공정 quota와 eviction 통계를 구현한다.

- [ ] **Step 4: GREEN 및 race 확인**

Run: `cd edgex/local-data-cache && go test -race ./...`

Expected: PASS, race 0.

- [ ] **Step 5: 공통 모듈 커밋**

```bash
git add edgex/local-data-cache
git commit -m "feat: add bounded local data ring cache"
```

### Task 2: Serial Device Service adapter와 API

**Files:**
- Modify: `edgex/device-serial/go.mod`
- Modify: `edgex/device-serial/internal/driver/recent_cache.go`
- Modify: `edgex/device-serial/internal/driver/recent_cache_test.go`
- Modify: `edgex/device-serial/internal/driver/local_data_api.go`
- Modify: `edgex/device-serial/internal/driver/local_data_api_test.go`
- Modify: `edgex/device-serial/internal/driver/driver.go`
- Modify: `edgex/device-serial/internal/driver/driver_test.go`

**Interfaces:**
- Consumes: `localcache.Cache[int32]`
- Produces: 기존 `cachedSample`, `newRecentCache`, latest/recent adapter 호환
- Produces: `GET /api/v3/localdata/stats`

- [ ] **Step 1: RED — config validation, 64MiB stats와 metric 등록 테스트 작성**

```go
func TestParseRecentCacheConfigRejectsInvalidBytes(t *testing.T) {
    _, err := parseRecentCacheConfig(map[string]string{
        "LocalDataCacheMaxBytes": "0",
    })
    assert.ErrorContains(t, err, "LocalDataCacheMaxBytes")
}
```

stats route 테스트는 `maxBytes=67108864`, 현재 sample/series/allocated bytes와 eviction breakdown을 검증한다.

- [ ] **Step 2: RED 확인**

Run: `cd edgex/device-serial && go test ./internal/driver`

Expected: config parser와 stats route 부재로 FAIL.

- [ ] **Step 3: GREEN — 공통 cache adapter, config와 metric observer 구현**

`recent_cache.go`는 공통 cache로 위임하고 기존 driver 호출 형태를 유지한다. Driver Initialize가 `DriverConfigs()`를 검증하고 `LocalDataCacheSamples`, `LocalDataCacheSeries`, `LocalDataCacheAllocatedBytes`, `LocalDataCacheEvictions`를 등록한다.

- [ ] **Step 4: GREEN 및 race 확인**

Run: `cd edgex/device-serial && go test -race ./...`

Expected: PASS, race 0.

- [ ] **Step 5: Serial 통합 커밋**

```bash
git add edgex/device-serial
git commit -m "feat: bound serial local data cache"
```

### Task 3: Sense HAT Device Service adapter와 API

**Files:**
- Modify: `edgex/device-sensehat/go.mod`
- Modify: `edgex/device-sensehat/internal/driver/recent_cache.go`
- Modify: `edgex/device-sensehat/internal/driver/recent_cache_test.go`
- Modify: `edgex/device-sensehat/internal/driver/local_data_api.go`
- Modify: `edgex/device-sensehat/internal/driver/local_data_api_test.go`
- Modify: `edgex/device-sensehat/internal/driver/driver.go`
- Modify: `edgex/device-sensehat/internal/driver/driver_test.go`

**Interfaces:**
- Consumes: `localcache.Cache[float64]`
- Produces: Serial과 동일한 설정·stats·metric 계약

- [ ] **Step 1: RED — float64 adapter와 동일 stats 계약 테스트 작성**

```go
func TestSenseHatCacheReportsBoundedStats(t *testing.T) {
    cache := newRecentCache(10*time.Minute, 10)
    cache.append("device", "humidity", floatSample(1, 35.2))
    stats := cache.stats()
    assert.Equal(t, 1, stats.Samples)
    assert.LessOrEqual(t, stats.AllocatedBytes, stats.MaxBytes)
}
```

- [ ] **Step 2: RED 확인**

Run: `cd edgex/device-sensehat && go test ./internal/driver`

Expected: stats adapter 부재로 FAIL.

- [ ] **Step 3: GREEN — float64 adapter, stats route와 metrics 구현**

Serial과 JSON·metric name·설정 key를 동일하게 유지하고 값 타입만 `float64`로 고정한다.

- [ ] **Step 4: GREEN 및 race 확인**

Run: `cd edgex/device-sensehat && go test -race ./...`

Expected: PASS, race 0.

- [ ] **Step 5: Sense HAT 통합 커밋**

```bash
git add edgex/device-sensehat
git commit -m "feat: bound sensehat local data cache"
```

### Task 4: 배포 설정과 manifest 계약

**Files:**
- Modify: `edgex/device-serial/res/configuration.yaml`
- Modify: `edgex/device-sensehat/res/configuration.yaml`
- Modify: `edgex/k8s/base/device-serial-jetson/config/configuration.yaml`
- Modify: `edgex/k8s/base/device-sensehat-raspi/config/configuration.yaml`
- Modify: `edgex/k8s/tests/test_device_serial_manifest.py`
- Modify: `edgex/k8s/tests/test_device_sensehat_manifest.py`

**Interfaces:**
- Produces: 두 canonical/runtime ConfigMap에 동일 cache Driver/Telemetry 설정

- [ ] **Step 1: RED — render 설정 exact equality 테스트 추가**

각 manifest 테스트에서 `Driver` 세 key와 `Writable.Telemetry.Metrics` 네 key가 정확히 존재하는지 검증한다.

- [ ] **Step 2: RED 확인**

Run: `/home/jinuk/codex-work/jinuk/.venv-publisher/bin/pytest -q edgex/k8s/tests/test_device_serial_manifest.py edgex/k8s/tests/test_device_sensehat_manifest.py`

Expected: 설정 부재로 FAIL.

- [ ] **Step 3: GREEN — canonical configuration 갱신**

두 source configuration과 Kustomize ConfigMap source에 동일 값을 추가한다.

- [ ] **Step 4: manifest GREEN과 root render 확인**

```bash
/home/jinuk/codex-work/jinuk/.venv-publisher/bin/pytest -q edgex/k8s/tests/test_device_serial_manifest.py edgex/k8s/tests/test_device_sensehat_manifest.py
kubectl kustomize edgex/k8s >/tmp/edgex-cache-v2-render.yaml
kubectl apply --dry-run=server -f /tmp/edgex-cache-v2-render.yaml
```

Expected: 11개 이상 PASS, server dry-run 성공.

- [ ] **Step 5: 배포 설정 커밋**

```bash
git add edgex/device-serial/res edgex/device-sensehat/res edgex/k8s/base edgex/k8s/tests
git commit -m "deploy: configure bounded device caches"
```

### Task 5: 현재 문서와 생성 HTML

**Files:**
- Modify: `docs/원시-텔레메트리-데이터-플레인.md`
- Modify: `docs/디바이스-서비스-연결.md`
- Modify: `docs/ops/현재-데모-운영-절차.md`
- Modify: `docs/문서-안내.md`
- Modify: `docs/wiki/상태와-텔레메트리.md`
- Modify: `docs/wiki/변경-기록.md`
- Modify: `docs/일일-기록.md`
- Regenerate: `docs/html/**`

**Interfaces:**
- Produces: 구현·manifest·live evidence와 일치하는 현재 문서

- [ ] **Step 1: cache v2 경계와 stats 검증 명령 반영**

문서에는 ring slot 64MiB budget, 기존 API 호환, stats endpoint, Pod restart 초기화와 SQLite Outbox 미구현을 함께 명시한다.

- [ ] **Step 2: HTML 재생성**

Run: `python3 scripts/build-docs-html.py`

Expected: source 문서와 검색 색인이 새 설계·운영 내용을 포함한다.

- [ ] **Step 3: 문서 검증**

```bash
python3 -m unittest tests.test_docs_html_search
git diff --check
```

Expected: PASS, whitespace error 0.

- [ ] **Step 4: 문서 커밋**

```bash
git add docs
git commit -m "docs: document bounded local data caches"
```

### Task 6: 전체 검증, ARM64 배포와 live 증거

**Files:**
- Modify: `edgex/k8s/base/device-serial-jetson/workload.yaml` with actual digest
- Modify: `edgex/k8s/base/device-sensehat-raspi/workload.yaml` with actual digest
- Modify: `docs/일일-기록.md` with exact revision/digests/live result

**Interfaces:**
- Produces: registry ARM64 images, immutable manifest pins, exact live evidence

- [ ] **Step 1: 전체 자동 검증**

```bash
cd edgex/local-data-cache && go test -race ./...
cd ../device-serial && go test -race ./...
cd ../device-sensehat && go test -race ./...
cd ../../ && /home/jinuk/codex-work/jinuk/.venv-publisher/bin/pytest -q edgex/k8s/tests tests
python3 -m unittest tests.test_docs_html_search
git diff --check
```

Expected: 모든 command exit 0.

- [ ] **Step 2: ko ARM64 image build/push**

```bash
cd edgex/device-serial
KO_DOCKER_REPO=192.168.0.56:5000/edgex-device-serial go run github.com/google/ko@v0.18.0 build --bare --platform=linux/arm64 --insecure-registry --sbom=none --tags=cache-v2-20260722 ./cmd/device-serial
cd ../device-sensehat
KO_DOCKER_REPO=192.168.0.56:5000/edgex-device-sensehat go run github.com/google/ko@v0.18.0 build --bare --platform=linux/arm64 --insecure-registry --sbom=none --tags=cache-v2-20260722 ./cmd/device-sensehat
```

Expected: 각각 immutable `repository@sha256:` 출력.

- [ ] **Step 3: registry platform 확인과 digest pin**

`crane manifest --insecure`로 config architecture가 `arm64`인지 확인한 뒤 workload image를 실제 digest로 교체한다. manifest tests와 root render를 다시 실행하고 커밋한다.

- [ ] **Step 4: main push와 Argo sync**

```bash
git push origin main
kubectl annotate application edgex-telemetry -n argocd argocd.argoproj.io/refresh=hard --overwrite
```

Argo `status.sync.revision`이 push SHA와 같고 `Synced/Healthy`가 될 때까지 60초 이하 polling으로 확인한다.

- [ ] **Step 5: 실기기 검증**

Serial과 Sense HAT Pod의 Ready/restart/imageID를 확인한다. same-node client에서 latest, recent와 `/api/v3/localdata/stats`를 조회하고 64MiB 이하 allocated bytes, Serial 6 series, Sense HAT 11 series와 증가 origin을 확인한다. `sensor-anomaly-demo /api/v1/status`를 두 번 읽어 `mode=live`, `inputState=fresh`, 증가 `framesProcessed`를 확인한다. Core Data latest Event와 dashboard history API 회귀도 확인한다.

- [ ] **Step 6: live 기록과 최종 검증 commit/push**

정확한 main SHA, image digest, Argo revision, stats와 실기기 결과를 `docs/일일-기록.md`에 기록하고 HTML을 재생성한다. 문서·manifest 테스트와 `git diff --check`를 다시 실행한 뒤 commit/push하고 원격 main과 Argo exact revision을 재확인한다.

## Plan Self-Review

- Spec coverage: 공통 cache, API 호환, 설정, metrics, manifest, docs, ARM64와 live gate가 Task 1~6에 연결된다.
- Placeholder scan: 구현 범위를 미루는 `TBD`/`TODO`가 없고 SQLite Outbox는 명시적으로 다음 별도 기능으로 제외했다.
- Type consistency: 두 adapter는 `Cache[int32]`/`Cache[float64]`, 동일 Options·Stats·metric name을 사용한다.
- Scope: 하나의 배포 가능한 cache v2 수직 변경이며 Outbox를 분리해 독립 검증 가능하다.
