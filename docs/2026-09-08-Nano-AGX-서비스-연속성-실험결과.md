# Nano–AGX 서비스 연속성 실험 결과

> 실험일: 2026-09-08 · 문서 작성: 2026-09-09 · 분류: 격리 자격시험 / 개발 참고
> 최종 판정: **실제 Nano → AGX Orin → Nano 왕복 10회와 Nano 단독 비교 10회 모두 합격**.

## 결과를 쉽게 설명하면

Nano에 일이 몰리면 AGX를 준비하고, 준비가 끝난 뒤 새 일을 AGX에 맡겼다.
준비하는 동안에도 Nano는 계속 일했다. 일이 줄면 Nano로 돌려주고 AGX의 모델 메모리를 비웠다.
이 과정을 실제 장비에서 10번 반복했고, 비교 시험까지 **17,520건 모두 결과를 받았다**.

이는 양쪽 장비가 켜져 있는 상태의 고정 Llama 데모 시험이다. 모든 요청에 지연이 없었다는
뜻은 아니다. AGX 준비와 겹친 요청의 첫 토큰 지연은 최대 2.407초였다.
외부 서비스용 공용 진입점이나 Nano 전원·네트워크 전체 장애 복구를 검증한 결과로 확대하지 않는다.

## 무엇을 검증했나

목적은 AI 모델 자체의 품질 평가가 아니라, 서비스를 계속 처리하면서 **부하 전환·복귀·자원 해제**를
수행할 수 있는지 확인하는 것이다. KubeEdge/Kubernetes는 노드와 workload를 관리하고,
시험 시작·정지·원장 회수는 별도 운영자가 소유한다.
격리된 시험 client가 고정 Kubernetes Service `llama-nano`, `llama-agx` 중 요청 대상을 선택한다.
Service selector나 EndpointSlice를 동적으로 바꾸지 않는다. EdgeX 물리 디바이스 경로와 기존
센서 서비스를 변경하지 않았다.

1. AGX는 모델 파일이 SD에 있는 `CACHED` 상태에서 시작한다. 모델을 GPU에 미리 유지하지 않는다.
2. Nano의 부하 조건을 만족하면 AGX를 활성화한다. 준비 중 요청은 Nano가 계속 처리한다.
3. AGX의 실제 추론 응답에서 요청 ID·모델·장비를 확인한 후 새 접수 요청을 AGX에 보낸다.
4. 전환 전에 Nano에 접수한 요청은 Nano가 마저 처리한다. 복귀 때도 기존 AGX 요청을 마저 처리한다.
5. 부하 감소 조건과 Nano의 실제 응답을 확인하면 새 요청을 Nano로 돌린다.
6. AGX의 처리 중·대기 요청이 없고 유휴 시간이 충족되면 모델 메모리를 해제한다.

모델 메모리 해제 이후에도 AGX 관리 Pod와 GPU 공유 슬롯은 유지한다. 실행 중 요청이나
LLM의 KV-cache를 다른 장비로 옮기는 방식은 아니다.

## 장비·입력·측정 조건

| 항목 | 고정 조건 |
| --- | --- |
| 원래 처리 장비 | 실제 Orin Nano, `etri-dev0001-jetorn` |
| 넘겨받는 장비 | 실제 AGX Orin, `etri-dev0005-jetagx`; RTX 대체 사용 없음 |
| 격리 namespace | `llama-continuity-test` |
| 모델·runtime | `llama3.2:1b`, Ollama 0.33.2, 실제 CUDA 추론 |
| 모델 digest | `baf6a787fdffd633537aa2eb51cfd54cb93ff08e28040095462bb63daf552878` |
| 입력 | 동일 prompt, 최대 출력 8토큰, temperature 0, seed 42, context 2048 |
| 처리 동시성 | worker별 1개; 전환 전 접수 요청은 기존 장비에서 완료 |
| AGX 모델 저장소 | ext4 SD `/srv/agx-model-cache/ollama`; 활성화 중 다운로드 0ms |
| 1회 부하 | 30초 × 2건/초 → 120초 × 4.8건/초 → 120초 × 2건/초 |
| 반복 | 단독/왕복 10쌍, 회차마다 선후 순서 교대; 회당 876건, 부하 시간 합계 약 90분 |
| 지연 측정 시작 | client의 **예정 도착 시각**; 대기·네트워크·처리 시간을 포함 |
| 첫 응답 지연 | 실제 스트리밍 첫 토큰 수신까지의 TTFT |
| 전체 응답시간 | 마지막 결과 수신까지; p95는 nearest-rank 방식 |

고정 prompt는 `In one short sentence, explain why edge computing reduces latency.`다.
더 긴 입력·출력의 [장비별 GPU 성능측정](Llama-장비별-GPU-성능측정.md)과는 작업 조건이
다르므로 처리율과 지연 수치를 섞어 사용하지 않는다.

## 먼저 고정한 기준

저부하 TTFT와 바쁜 상태의 직렬 처리시간을 각각 3회 측정했다. 부하 단계별 시험에서
Nano의 검증 처리율은 4.0건/초였고, AGX의 4.8건/초 수용은 별도 3회, 총 432건으로 확인했다.
SD에서 AGX를 준비하는 시간도 10회 측정한 후 baseline과 정책을 동결했다.

| 판단 | 동결한 기준 |
| --- | --- |
| 정상 지연 S | 양쪽 저부하 p95 TTFT 최댓값 × 2 = **548.41ms** |
| 전환 압력 | Nano 처리율의 85% 이상 또는 예상 대기 지연이 S를 넘는 상태가 5초 지속 |
| AGX 자격 | 해당 입력률 수용 가능, 준비 중 Nano 처리를 포함한 예상 이득 15% 이상 |
| Nano 복귀 | 전체 입력률이 Nano 처리율의 60% 이하로 30초 지속, 60초 cooldown과 실제 Nano 응답 확인 |
| AGX 모델 해제 | 처리 중·client 대기·worker 대기 요청 0, 30초 유휴 |
| 유한 대기열 | 최대 111건; 거절 발생 시 이번 왕복 시험 불합격 |
| 활성화 중 요청 지연 | 기준선 활성화 최대 22,298.12ms + S = **22,846.53ms** 이하 |
| 요청 검증 | 누락·중복·거절·오류 0, 단일 전송, 실제 node/model identity 확인 |

예측은 이후 입력률이 현재 수준으로 유지된다는 가정을 사용한다. 예측식의 **평균 비용 이득**과
아래의 **실측 p95 전체 응답시간 감소율**은 다른 지표다.

## 최종 결과

| 측정 항목 | 결과 |
| --- | --- |
| 전환·복귀·모델 해제 | **10/10회 합격** |
| 같은 부하의 Nano 단독 비교 | **10/10회 합격** |
| 성공 요청 | 왕복 8,760건 + 단독 8,760건 = **17,520/17,520건** |
| 누락·중복 전송·거절·오류 | **0건** |
| 정상 구간 p95 TTFT | 회차별 **103.03~379.47ms**, 모두 S 548.41ms 이하 |
| 활성화와 겹친 요청의 최대 TTFT | **2,407.42ms**, 예산 22,846.53ms 이하 |
| AGX 준비부터 실제 시험 응답 완료까지 | **22.132~22.547초**, 다운로드는 10회 모두 0ms |
| 단독 대비 p95 전체 응답시간 감소 | **62.11~88.11%**, 회차별 감소율 중앙값 **81.11%** |
| 왕복 응답 완료 사이 최대 간격 | **1,890.04ms**; 0ms 간격이나 무지연을 뜻하지 않음 |

정상 구간 지표에서는 활성화와 겹친 요청을 분리한다. 분리한 요청도 버리지 않고
별도 최대 TTFT 기준으로 판정한다. 아래 전체 응답시간 p95에는 그 요청들도 포함한다.

| 반복 | 왕복·단독 판정 | 정상 p95 TTFT (ms) | 왕복 p95 전체 응답 (ms) | 단독 p95 전체 응답 (ms) | 감소율 |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | 모두 합격 | 103.03 | 437.11 | 2096.40 | 79.15% |
| 2 | 모두 합격 | 244.65 | 699.75 | 5343.72 | 86.91% |
| 3 | 모두 합격 | 309.16 | 984.42 | 3131.86 | 68.57% |
| 4 | 모두 합격 | 201.37 | 806.22 | 5012.22 | 83.91% |
| 5 | 모두 합격 | 356.72 | 1068.91 | 3181.01 | 66.40% |
| 6 | 모두 합격 | 331.56 | 1327.09 | 3502.36 | 62.11% |
| 7 | 모두 합격 | 305.37 | 903.17 | 7595.10 | 88.11% |
| 8 | 모두 합격 | 236.96 | 916.41 | 5414.11 | 83.07% |
| 9 | 모두 합격 | 379.47 | 2109.30 | 6541.85 | 67.76% |
| 10 | 모두 합격 | 268.67 | 1234.55 | 9364.03 | 86.82% |

실제 활성화 최대 22.547초는 기준선 최대 22.298초보다 약 0.249초 길었다.
기준선 최대를 절대 상한으로 보장하지 않는다. 동결한 **요청 지연 예산**은 변경하지 않았고
모든 회차가 통과했다. 더 긴 작업과 SD·입력률 변동은 별도 검증이 필요하다.

## 첫 실패와 수정 근거

첫 `v2-sd-01` 왕복도 876건을 모두 처리하고 AGX 전환·Nano 복귀·모델 해제를 수행했다.
그러나 정상 구간 p95 TTFT가 **6,640.0ms**로 S를 넘어 실패했고, 후속 반복을 자동 중단했다.

원인은 요청이 늘수록 접수·전송·첫 토큰·완료마다 누적 JSON 전체를 다시 저장하는 기록 경로였다.
실제 실패 원본으로 수행한 로컬 진단에서 전체 저장 중앙값은 39.44ms, 변경 항목 저장은
4.11ms였다. 이는 로컬 원장 비용 비교이며 Jetson 추론 성능 측정값이 아니다.

요청·이벤트·표본별 변경분을 저장하도록 수정하면서 SQLite WAL과 `synchronous=FULL`을 유지했다.
전송 의도와 dispatch 이벤트는 네트워크 전송 전에 같은 트랜잭션으로 확정한다.
재시작 시 변경분을 합쳐 원장을 복구하며, 이미 전송해 결과가 불확실한 요청을 자동 재실행하지 않는다.

그 후 **같은 기준선·정책·부하**로 `v2-sd-02`에서 10쌍을 처음부터 다시 실행했다.
첫 실패 기록은 보존했으며 성공 부분만 골라 최종 10쌍에 섞지 않았다.

## 종료 상태와 검증 범위

2026-09-08 22:48 KST 종료 확인 기록에서 Nano는 추론 가능 상태였고, AGX는 `CACHED`,
모델 VRAM·active·queue 모두 0이었다. SD 여유는 약 122.76GB, AGX eMMC 여유는 약 14.86GB였으며
저장소 점검을 통과했다. 7개 노드는 Ready이고 Disk/Memory/PID pressure가 없었다.
기존 Llama·EdgeX Pod의 UID·컨테이너 ID·재시작 횟수·Ready 상태는 시험 시작 기록과 같았다.
이 수치는 **실험 종료 당시 관측**이며 현재 실시간 상태를 대신하지 않는다.

원격 전체 판정과 내보낸 원본 20회로 재계산한 로컬 판정이 일치했다.
원본·verdict 40개 파일의 SHA-256을 확인했고, 미완료 원장은 없었다.
93개 자동시험을 통과한 코드와 실제 controller 코드 해시가 일치했다.

외부 서비스용 공용 gateway, NEXUS 자동 제어, Nano 전체 전원·네트워크 장애, 긴 생성·다른 모델,
실제 SD 재부팅·실행 중 카드 분리는 이번 시험에 포함하지 않았다.
다음 단계는 이 접수·전송·복귀 계약을 외부 서비스의 공용 진입점에 연결하고 같은 기준으로 검증하는 것이다.

## 내려받을 근거와 재현 위치

- [전체 20회 판정과 대응 비교](assets/nano-agx-continuity-20260908/report.json)
- [최종 수치 요약](assets/nano-agx-continuity-20260908/summary.json)
- [동결 정책과 측정 계약](assets/nano-agx-continuity-20260908/policy.json)
- [종료 장비·기존 서비스·코드 검증](assets/nano-agx-continuity-20260908/final-verification.json)
- [내보낸 원본의 SHA-256](assets/nano-agx-continuity-20260908/export-sha256.json)

위 파일은 저장소 원본의 복사본이다. 요청별 원장·첫 실패·기준선은 저장소의
`qualification/llama-offloading/results/2026-09-08-nano-agx-v2/`에 보존한다.
실행 절차는 `qualification/llama-offloading/continuity-runbook.md`, 판정 코드는
`continuity_report.py`, 전환 정책은 `continuity_policy.py`, 실행·영속 기록은 `continuity_runner.py`다.

최종 정책 ID는 `c1368446293834f1e5bdfe1f7c0e085673e0004a2c27978be96e32be86c740b3`다.
