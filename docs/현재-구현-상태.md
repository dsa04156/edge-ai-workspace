# 프로토콜 독립형 디바이스 발견·등록 현재 구현 상태

> 기준일: 2026-07-28
> 판정 기준: 실제 코드, 테스트, `edgex/k8s` render
> “구현”과 “실장비 운영 검증”을 구분한다.

## 요약

Serial은 PTY 후보 발견부터 승인, 검증 Runtime 재사용, EdgeX Profile/Device idempotent 등록,
Core Data 첫 Event 확인까지 자동화된 코드와 fake backend 통합 테스트가 있다. 2026-07-24
실제 Jetson Arduino stable 후보를 운영자가 승인했고 기존 Device Service Runtime을 재사용해
aggregate Device `arduino-75035303230351e0d171-57fc03a907`의
`EVENT_CONFIRMED`와 Core Data Event까지 live 확인했다.

I2C는 allowlist bus/address와 여러 read-only identity register를 하나의 물리 보드
identity로 묶는 composite probe까지 구현했다. 2026-07-28
`etri-dev0003-raspi5`의 Sense HAT v1에서 `0x1c=0x3d`, `0x5c=0xbd`,
`0x5f=0xbc`, `0x6a=0x68`을 확인해 후보를 승인했고, 기존
`device-sensehat-raspi`, Profile `etri-sensehat-gyroscope`, Device
`imu-sensehat-gyroscope-01`을 재사용한 뒤 신규 Core Data Event로
`EVENT_CONFIRMED`까지 live 검증했다. 이어서 등록 연결을 decommission하고 다음 30초
탐색 주기에 같은 candidate ID가 `PENDING_APPROVAL`로 자동 복귀하는지 확인했으며,
재승인 뒤 다시 `EVENT_CONFIRMED`가 됐다. 기존 EdgeX Device 13개와 Sense HAT Device
6개는 삭제되거나 중복 생성되지 않았다.

같은 날 일반 Discovery DaemonSet의 Serial-only passive inventory를 dev0003까지
확장했다. Raspberry Pi에 연결된 CH340 계열 USB Serial 장비를
`/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`(VID/PID `1a86:7523`)에서
후보 `candidate-fb7190f...`로 반복 관측했다. 검증된 Raspberry Pi Serial hardware
binding과 Profile이 없으므로 후보는 의도대로 `BLOCKED`, `binding-required`,
`registrationReady=false`이며 EdgeX Device나 Runtime은 생성하지 않았다. Serial Agent와
I2C Agent가 같은 node를 보고해도 서로의 후보를 `STALE` 처리하지 않도록 report에
`scannedProtocols` scope를 추가했고, Sense HAT 후보는 계속 `EVENT_CONFIRMED`를 유지했다.
MQTT는 자기등록 schema parser까지만 구현했으며 subscriber가 없다.
Modbus는 네트워크 discovery 없이 운영자가 선언한 고정 개발 endpoint에 한해 공식 EdgeX
`device-modbus:4.0.2` Runtime, read-only Profile, Modbus TCP simulator와 승인 Saga를
연결했다. 2026-07-28 Unit 2 가상 온도 센서를 승인해 Controller-owned Runtime,
Core Metadata Device, 1초 AutoEvent, Core Data 저장과 대시보드 `available` 상태까지 live
확인했다. 이는 개발 simulator 수직 경로의 증거이며 실제 PLC 지원 완료가 아니다. OPC-UA와
ONVIF는 확장 contract와 명시적 `not implemented` 상태만 있다.

## 구현 완료

| 항목 | 근거 |
|---|---|
| stable candidate ID | `discovery.py:stable_candidate_identity`, SHA-256 단위 테스트 |
| candidate 중복 제거·재연결 | clean disconnect/reconnect와 동일 ID 테스트 |
| 명시적 상태 머신 | `discovery_state.py`, 허용/거부 전이 테스트 |
| 영속 candidate/Plan/Saga/audit/idempotency | `discovery_store.py`, SQLite 재시작 복구 테스트 |
| legacy ConfigMap 1회 import | 24자리 candidate ID 호환 migration 테스트 |
| 노드별 보수적 Discovery Plan | JSON seed, GET/PUT API, Agent signed 조회 |
| Serial stable inventory | `/dev/serial/by-id`, VID/PID/USB metadata |
| Serial WHOAMI/Manifest | JSON-line, timeout, invalid schema/frame 처리 |
| PTY Arduino simulator | valid/invalid/silent/unsupported, telemetry와 재연결 |
| protocol-scoped reconciliation | report의 `scannedProtocols` 범위 안에서만 missing candidate를 `STALE` 처리해 같은 node의 Serial/I2C Agent 병행 |
| Raspberry Pi passive Serial live discovery | dev0003 CH340 `1a86:7523` 후보 반복 report, exact binding 부재로 `BLOCKED` 유지 |
| Sense HAT I2C composite discovery | 허용 bus 1과 네 identity register를 한 후보로 식별, partial/mismatch 차단, dev0003 실장비 반복 report 검증 |
| Device/Profile Catalog | schema, exact/partial/ambiguous, allowlist와 invalid binding 격리 |
| 운영자 승인·거절·재시도 | API와 dashboard action, audit/history. 현재 PoC 관리 BFF는 browser 관리자 token을 사용하지 않음 |
| 인증 interface | `AllowAllMockProvider`, 외부 HTTPS provider, fail-closed 테스트 |
| Registration Saga | Runtime, digest readback, Metadata, first Event, retry |
| rollback 소유권 보호 | 생성 Device/Profile/Runtime만 정리하는 테스트 |
| EdgeX v3 실제 응답 계약 | HTTP 207 mutation array, requestId, readback 검증 테스트 |
| Arduino aggregate Device 지원 | `ResourceName: "*"`, 6 resource Profile |
| legacy 6 Device 전환 fan-out | aggregate와 단일 resource 공존, 중복 동일 route 차단 Go 테스트 |
| API 목록 | 요구된 `/api/v1` 경로와 OpenAPI |
| metric·health | `/metrics`, `/healthz`, `/readyz` |
| Kubernetes 배포 | Agent DaemonSet, Controller Deployment/Service/PVC/RBAC/NetworkPolicy, 개발 인증 및 Jetson/Raspberry Pi 예시 overlay |
| dashboard 표시 | 한글 state/auth/failure/model/Profile과 승인·거절·retry |
| 웹 Device Profile 편집 | Core Metadata Profile 목록, 읽기 전용 resource 반복 입력, mutation-free 검증, 이름 충돌 차단, 동일 계약 재사용, idempotent 생성과 readback. 웹 생성 Profile은 등록 현황에 즉시 표시되고 전체 목록은 장비 연결 단계에서 선택한다. 후보 capability는 type을 추측하지 않는 초안만 제공 |
| CI digest 정렬 | Device Service build 후 Catalog 해석, 실제 image readback gate |
| 공식 Modbus 개발 경로 | digest 고정 `device-modbus:4.0.2`, 수동 endpoint exact match, 동적 Runtime, Profile/Device/AutoEvent/first Event Saga 테스트 |
| Modbus TCP simulator | stdlib read-only FC03/FC04 서버, 잘못된 function/address/unit 처리와 개발 Kustomize overlay |
| Modbus 개발 fixture live 등록 | Unit 2 후보 `candidate-91e032158c...` 승인, Runtime `adapter-modbus-a22b09287e`, Device `virtual-temperature-54535762444ac76f7907e44c6ab33d18-91e032158c`, 첫 Event와 대시보드 `available` 확인 |
| Sense HAT 기존 등록 재사용 | 후보 `candidate-f0829ddd...` 승인, Runtime/Profile/Device 생성 없이 기존 6개 Device 유지, 대표 Device의 승인 이후 Event로 완료 확인 |
| decommission 뒤 자동 재탐색 | 등록 연결 해제 뒤 같은 stable candidate ID가 다음 node report에서 `PENDING_APPROVAL`로 복귀하고 재승인 후 `EVENT_CONFIRMED`; `reuse-existing` 리소스 수 불변 |
| 등록 완료 operating state | 첫 Event 뒤 Device를 `UP`으로 idempotent 갱신하고 Metadata readback이 성공해야 `EVENT_CONFIRMED` 처리 |

## 부분 구현

| 항목 | 현재 범위 | 남은 검증 |
|---|---|---|
| 다른 I2C 장비 discovery | 단일·복합 identity rule과 mock adapter | 장비별 안전한 identity register, Catalog/Profile, node별 장치 권한과 첫 Event |
| MQTT discovery | topic/payload/retained evidence parser | TLS broker subscriber, retained 수신, telemetry Device Service |
| 외부 인증 | HTTPS client와 timeout/denial 테스트 | 실제 운영 인증 endpoint, certificate/token Secret |
| Controller Runtime 배포 | 기존 `AdapterRuntime` renderer/reconciler 재사용 | `deploymentEnabled=true`인 신규 protocol 실장비 Runtime |
| Modbus TCP | 공식 Device Service와 개발 simulator Unit 1·2 allowlisted endpoint, Profile, AutoEvent, NetworkPolicy, approval-to-Event live 검증 | 실제 PLC register map·망 allowlist·분리·재연결·장애복구 |
| Serial active Manifest | PTY 검증 | 현재 Device Service와 port 경쟁 없는 유지보수 창의 Jetson 실장비 |
| Prometheus | text exposition endpoint | ServiceMonitor/NetworkPolicy 운영 연결 |
| decommission | ownership-aware 명시적 퇴역, 연결된 node-scan 후보의 자동 재탐색과 재승인 live 검증 | Controller가 실제 생성한 Runtime/Profile/Device의 실환경 퇴역·재생성 검증 |

## 미구현

| 항목 | 현재 동작 |
|---|---|
| Modbus RTU/TCP 자동 discovery | enabled Plan이면 `not implemented` scan error, 후보 생성 없음. 현재 개발 경로는 수동 endpoint 선언만 지원 |
| OPC-UA FindServers/GetEndpoints/Browse | 구현하지 않음 |
| ONVIF discovery/capability | 구현하지 않음 |
| MQTT broker subscriber | 구현하지 않음 |
| 임의 telemetry에서 schema/value type/units를 추론하는 자동 Device Profile 생성 | 구현하지 않음. 웹 편집기의 후보 초안은 model/capability 이름만 채우며 운영자 검토가 필요 |
| 운영자 승인 없는 자동 등록 | 의도적으로 구현하지 않음 |
| 네트워크 CIDR 자동 scan | 의도적으로 구현하지 않음 |
| RTSP frame의 EdgeX 저장 | 의도적으로 구현하지 않음 |
| KubeEdge Device CRD 동기화 | 현재 authority 정책상 구현하지 않음 |

## 실장비 검증 필요

- dev0003 CH340 USB Serial의 실제 분리·재연결과 같은 candidate ID 복구
- dev0003 CH340 장비의 Manifest/parser/Profile, exact device mount와 Device Service
  telemetry 검증
- Sense HAT 보드의 실제 분리·재연결과 같은 candidate ID 복구
- Sense HAT 외 I2C 장비의 identity rule과 실장비 Event
- 외부 인증 운영 endpoint
- 실제 PLC의 Modbus TCP/RTU register map과 연결·재연결
- Modbus 개발 fixture의 강제 장애와 ownership-aware rollback
- ONVIF/OPC-UA/MQTT 실장비는 구현 후 별도 검증

기존 `device-serial-jetson`과 센서별 6개 virtual Device의 live telemetry를 신규 aggregate
등록의 완료 증거로 대신 사용하지 않는다. 신규 candidate는 운영자 승인 전 Core Metadata에
추가되지 않는다.

## 보안·운영 주의

- Controller base는 `ADAPTER_AUTH_MODE=external`로 fail-closed한다. 현재 root testbed만
  `overlays/development/adapter-management`에서 `development-mock`을 적용하며 운영
  profile이 아니다.
- Catalog API는 read-only이며 임의 image 실행 API가 없다.
- 일반 `edge-device-discovery`는 dev0001/dev0002/dev0003에서 비권한 상태로 read-only
  `/dev`, `/sys` Serial inventory만 수행한다. dev0003의 `edge-device-discovery-i2c`는 container
  device cgroup 때문에 `privileged` 예외가 필요하지만 non-root UID/GID, exact
  `/dev/i2c-1` CharDevice 하나와 보조 group 988만 사용한다. 이 값은 현재 testbed
  host 권한에 종속되며 다른 노드에 그대로 복사하지 않는다.
- Controller SQLite는 1Gi RWO PVC를 사용해 단일 replica 기준이다. 다중 replica HA를
  활성화하지 않는다.
- SQLite PVC를 삭제하면 candidate, 전이 이력, 승인, Saga와 audit를 복구할 수 없다.
- Service는 ClusterIP 객체와 DNS를 사용하지만 고정 ClusterIP 우회는 없다.
- EdgeMesh manifest는 수정하지 않았다.
- 현재 PoC의 `/management/*` mutation은 browser 관리자 token이나 `Authorization`
  header를 요구하지 않는다. 기능 플래그, allowlist 검증, `Idempotency-Key`와 내부
  HMAC은 유지하지만 이는 운영자 인증이 아니다. 접근이 제한된 개발 테스트베드 경계이며
  운영 노출 전 별도 사용자 인증·인가가 필요하다.

## 현재 자동 테스트 결과

변경 경로는 2026-07-28에 다시 실행했다. 그 외 행은 2026-07-24 회귀 결과다.

| 영역 | 명령 | 결과 |
|---|---|---|
| Adapter Controller | `cd edgex/adapter-controller && PYTHONPATH=. pytest -q` | 128 passed |
| Discovery Agent | `cd edgex/device-discovery-agent && PYTHONPATH=. pytest -q` | 17 passed |
| Serial Device Service | `cd edgex/device-serial && go test ./...` | 2 packages passed |
| Sense HAT Device Service | `cd edgex/device-sensehat && go test ./...` | 2 packages passed |
| EdgeX Kustomize/manifests | `PYTHONPATH=. pytest -q edgex/k8s/tests` | 131 passed |
| state-aggregator Python | `cd edge-orch/state-aggregator && PYTHONPATH=. pytest -q` | 190 passed |
| dashboard JavaScript | `cd edge-orch/state-aggregator && node --test tests/*.js` | 75 passed |
| workspace 회귀 | `PYTHONPATH=. pytest -q tests` | 89 passed |
| 센서 이상탐지 데모 | `cd edge-orch/sensor-anomaly-demo && PYTHONPATH=. pytest -q` | 31 passed |
| Sense HAT reader | `cd edgex/device-sensehat/reader && PYTHONPATH=. pytest -q` | 5 passed |
| legacy telemetry-plane | `cd edgex/telemetry-plane && PYTHONPATH=. pytest -q` | 176 passed |
| 문서 일관성·HTML | `PYTHONPATH=. pytest -q tests/test_docs_consistency.py tests/test_docs_html_search.py tests/test_docs_editor_server.py` | 15 passed |

## 다음 우선순위

1. 운영 노출 전에 Ingress 앞의 사용자 인증·인가 방식을 별도 설계한다.
2. dev0003 CH340 USB Serial에 맞는 Manifest/parser/Profile, exact mount와 검증 Runtime
   binding을 Git Catalog에 추가한 뒤 승인부터 첫 Event까지 확인한다.
3. USB 케이블과 Sense HAT 보드 실제 분리·재연결로 같은 candidate ID와 Device Service
   복구를 확인한다.
4. 개발 Modbus Runtime을 강제 실패시켜 Controller ownership 기반 rollback과 retry를 live
   검증한다.
5. 실제 PLC endpoint와 register map을 별도 Catalog binding으로 추가한다. simulator 검증을
   실장비 검증으로 대체하지 않는다.
6. MQTT가 실제 요구될 때 TLS self-registration subscriber와 Device Service를 구현한다.
