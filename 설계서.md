# Edge AI Model Library·서비스 카탈로그 및 자원 프로파일 설계서

- 문서 버전: v0.5
- 기준일: 2026-08-28
- 문서 상태: Kubeflow Hub standalone 활용 + 이기종 자원 프로파일·배치 확장 구조 반영 초안
- 적용 범위: 옥동 스마트공장 PoC 및 엣지 AI 서버 기반 서비스 배치·오프로딩·자원 증강 실험

---

## 변경 이력

### v0.5 (2026-08-28)

- 1차 Model Registry 기반을 MLflow에서 **Kubeflow Hub standalone**으로 변경하고, Full Kubeflow는 범위에서 제외
- Kubeflow Hub를 orchestration 기능이 없는 passive metadata repository로 한정하고, 모델 artifact는 MinIO/S3에 저장
- Hub metadata DB와 대용량·반복 측정용 **Profile Store**를 분리하고, Hub에는 Edge 호환성 요약과 profile reference만 연결
- Hub API 변경에 대비해 `ModelRegistryPort`와 `KubeflowHubAdapter` 경계를 추가
- 모델 등록 확장 계약을 `EdgeModelDescriptor`, 실제 측정 결과를 `ModelExecutionProfile`로 분리
- 현재 Kubernetes `v1.31.14`와 KServe `v0.20`의 Kubernetes `1.32+` 요구사항 차이 때문에 KServe를 1차 범위에서 제외
- 1차 오프로딩은 pre-deployed endpoint routing으로 검증하고, KServe와 Stage-level 배치는 후속 qualification gate 통과 후 도입
- Kubeflow Hub standalone 도입 자격시험과 실패 시 Registry Adapter 교체 원칙 추가

### v0.4 (2026-08-28)

- 자체 Model Registry를 새로 구현하는 방향을 폐기하고 **MLflow Model Registry + MinIO/S3 기반**으로 변경
- Model Library를 `Model Registry + Artifact Store + Runtime Compatibility + Hardware Execution Profile`의 논리 계층으로 재정의
- Edge CPU는 ONNX Runtime, Jetson GPU는 TensorRT/ONNX Runtime CUDA, Edge AI Server GPU는 Triton을 우선 활용하는 구조로 정리
- 서버 GPU 프로파일링은 Triton Model Analyzer/DCGM 등 기존 기능을 최대한 활용하고, **Raspberry Pi·Jetson·Server를 같은 기준으로 비교하는 Edge Model Profiler**만 직접 개발하도록 개발 경계 명확화
- 과제 자체 개발 범위를 `Edge Model Profiler → Profile Aggregator → Placement Engine → Offloading Controller`로 집중
- Component Library(FFT, Windowing 등)는 서비스 분할을 위한 2차 확장 계층으로 유지하되, 1차 개발의 선행조건에서 제외
- 등록된 모델이 어떤 디바이스/런타임에서 실행 가능한지와 실제 p95·throughput·memory·power 값을 프로파일로 연결하는 구조 추가
- 서비스 Workflow는 Model Library에서 모델을 참조하고, Placement Engine이 모델/Stage의 실제 실행 위치를 결정하도록 수정

### v0.3 (2026-08-28)

- AI 서비스를 하나의 모델/Pod로 취급하지 않고 **재사용 가능한 실행 라이브러리(Stage)의 DAG/Workflow**로 정의
- 서비스 설계 단위를 `Service → Workflow → Library(Stage) → Model/Algorithm → Resource Profile`로 변경
- 라이브러리별 `CPU/GPU/NPU`, `arm64/amd64`, `stateful/stateless`, `offloadable`, 입·출력 크기, runtime, latency, memory 정보를 프로파일하도록 변경
- 오프로딩 단위를 서비스 전체가 아니라 **Stage 또는 연속된 Stage 그룹(cut)** 으로 정의
- S06·S11을 라이브러리 조합 기반 상세 예시로 재작성하고, S01~S11 전체에 기본 Workflow chain을 매핑
- 자원 프로파일을 서비스 단위뿐 아니라 **Library Profile + Workflow Profile + Inter-stage Transfer Profile**의 3계층으로 확장
- 실험 순서를 `라이브러리 기능 검증 → 장치별 라이브러리 프로파일링 → Workflow 조립 → 실제 동시 실행 → cut-point 오프로딩 → 동적 정책`으로 수정

### v0.2 (2026-08-28)

- 공개 모델 후보를 **현업 대표/운영 후보(Primary)** 와 **연구·비교 후보(Comparison)** 로 분리
- “유명한 최신 모델”보다 **현장 문제 적합성, 배포성, ARM64/CPU/GPU 실행성, 운영 설명 가능성**을 우선하는 선정 원칙 추가
- S06 펌프·모터 이상감지의 주 모델을 `FFT/RMS 특징 + LightGBM` 및 `1D-CNN/TCN` 계열로 조정하고, DCASE AE·TimesNet은 비교·검증 모델로 재분류
- S11 생산품질 판별은 LightGBM을 Primary, XGBoost를 비교 기준으로 명시
- Vision·OCR·3D·영상 이상검사 서비스에 대해 현업 적용 방법과 연구 비교 후보의 역할을 명시
- 실험 목적을 “모델 정확도 순위”가 아니라 **정확도·지연·자원·배포성의 트레이드오프를 프로파일링하여 배치/증강 정책에 연결**하는 것으로 명확화

---

## 0. 문서 요약

본 문서는 **모델 등록·버전관리·Serving 자체를 새로 개발하는 것이 아니라 기존 공개SW를 기반 계층으로 활용하고**, 과제의 핵심 개발을 이기종 Edge 환경의 **모델 실행 프로파일링·배치·오프로딩**에 집중하기 위한 설계 문서다. Model Registry는 Kubeflow Hub standalone을 우선 채택하되, 과제 모듈은 Hub API에 직접 결합하지 않고 교체 가능한 Adapter를 통해 연동한다.

핵심 구조는 다음과 같다.

> **Kubeflow Hub(기존) → Registry Adapter(개발) → Runtime(기존) → Hardware Execution Profile(개발) → Placement Engine(개발) → Offloading Controller(개발) → Service Workflow**

권장 기반 기술은 다음과 같다.

- **Model Registry/Catalog**: Kubeflow Hub standalone
- **Registry metadata 저장**: Hub backend RDBMS와 영속 볼륨
- **Artifact Store**: MinIO/S3
- **상세 실행 프로파일 저장**: 별도 Profile Store
- **Registry 연동 경계**: `ModelRegistryPort` + `KubeflowHubAdapter`
- **공통 교환 포맷**: ONNX 우선, 단 모델 특성에 따라 native format 허용
- **Raspberry Pi / 일반 ARM CPU**: ONNX Runtime CPU
- **Jetson**: ONNX Runtime CPU/CUDA 또는 TensorRT
- **Edge AI Server GPU**: Triton Inference Server + TensorRT/ONNX backend
- **모니터링**: Prometheus + GPU는 DCGM 계열
- **배포/장치 관리**: Kubernetes + KubeEdge
- **KServe**: 1차 범위 제외, Kubernetes 및 KubeEdge 호환성 자격시험 후 선택 도입

과제에서 직접 개발하는 핵심은 다음 계층이다.

1. **Model Registry Adapter**\
   Hub의 모델·버전·artifact metadata를 공통 `ModelRegistryPort`로 변환하여 나머지 모듈을 특정 Registry API에서 격리한다.

2. **Edge Model Profiler**\
   등록된 동일 모델·동일 입력을 Raspberry Pi, Jetson, Edge AI Server의 복수 runtime에서 자동 실행하고 성능·자원 값을 수집한다.

3. **Profile Aggregator**\
   `Model × Version × Device × Runtime × Input Profile` 기준으로 latency, throughput, CPU/GPU/NPU, memory, power, cold start 등 실행 프로파일을 통합한다.

4. **Placement Engine**\
   사전 프로파일 + 현재 장치 자원상태 + 네트워크 RTT/BW + 서비스 SLO를 함께 고려하여 Local/Jetson/Server CPU/GPU/NPU 후보를 결정한다.

5. **Offloading Controller**\
   Placement 결과를 KubeEdge/Kubernetes 배치 또는 요청 routing에 반영하고, 효과가 없거나 장애가 발생하면 rollback한다.

서비스 분할·Stage-level offloading은 이 모델 라이브러리 기반 위에 구성한다. 즉 **1차 구현은 Model Library/Profiler가 우선이고, FFT·Windowing 같은 Component Library는 이후 서비스 분할 단계에서 확장**한다.

---

# 1. 설계 배경과 원칙

## 1.1 과제 정합성

협약용 계획서의 2차년도 범위에는 다음 내용이 포함돼 있다.

- PoC 활용 시스템 및 시나리오 설계
- 동적 AI 서비스 배치 및 실시간 배치·이동·부하 경감 기능 구현
- AI inference 중 CPU/GPU 부하 시 디바이스 오프로딩 기술 개발
- AI 서비스별 CPU/GPU/메모리/네트워크 최소·최대 요구치를 정의하는 프로파일 명세
- 자원 사용량에 따른 장애 대응 및 프로파일 범위 내 자원 증강

따라서 실험의 목적은 단순히 “CPU가 몇 퍼센트일 때 서버로 보낼 것인가”가 아니다.

> **서비스 종류와 실행 환경에 따라 어떤 자원이 병목이 되는지 확인하고, 서비스 QoS를 유지하기 위해 로컬 유지·부분 오프로딩·전체 오프로딩·서버 자원 추가 할당 중 무엇이 적합한지 판단하는 것**이 목적이다.

## 1.2 현재 방식에서 수정할 점

다음 방식은 주 실험 시나리오로 사용하지 않는다.

- AI 서비스 목적과 무관하게 CPU 사용률부터 0·50·75·100%로 변경
- CPU 사용률 하나만으로 오프로딩 여부 결정
- 모델 정확도와 운영 성능을 구분하지 않음
- 서버 내부 추론 시간만 비교하고 네트워크·직렬화·대기시간을 제외
- 모든 서비스에 동일한 지연 기준 적용
- 실제 서비스가 아닌 계산 프록시 결과를 운영 임계값으로 일반화

부하 생성기는 실제 현상을 재현하기 위한 도구일 뿐, 시나리오 자체가 아니다.

- 올바른 표현: “추가 AI 서비스 동시 실행에 따른 CPU 자원 경합을 재현하기 위해 제어 가능한 경쟁 부하를 주입하였다.”
- 잘못된 표현: “CPU를 100%로 만든 뒤 오프로딩하였다.”

## 1.3 설계 원칙

### 원칙 1. 서비스 요구가 자원 판단보다 먼저다

`현장 목적 → 입력·출력 → SLO → 모델 → 실행 후보 → 자원 프로파일 → 증강 정책` 순으로 설계한다.

### 원칙 2. CPU·GPU·NPU를 억지로 동일하게 시험하지 않는다

각 서비스의 자연스러운 연산 특성을 기준으로 시험한다.

- 시계열·표형 데이터: CPU 중심 기준선, 필요 시 GPU 모델 비교
- 객체 검출·포즈·영상 이상검사: GPU/NPU 중심
- OCR: CPU·GPU·NPU 모두 후보
- 3D 정합: CPU·메모리 중심, 대규모 포인트클라우드는 GPU 후보

### 원칙 3. 자원 사용률과 서비스 저하를 분리한다

자원 사용률 상승은 조사 신호다. 실제 증강은 다음을 모두 확인한 후 결정한다.

1. 서비스별 병목 자원 확인
2. 서비스 SLO 저하 확인
3. 후보 실행 위치가 동일 모델·동일 입력에서 실제 E2E 개선을 제공하는지 확인
4. 오류·OOM·모델 버전 불일치·데이터 유실이 없는지 확인

### 원칙 4. 현업 대표 모델과 연구 비교 모델을 분리한다

공개 모델 후보를 한 목록에 섞지 않는다.

- **Primary / 운영 후보**: 제조 현장에서 설명·배포·유지보수하기 쉽고, ARM64/x86·CPU/GPU 등 실제 실행환경에서 재현 가능한 모델 또는 방법
- **Industry Reference / 현업 관행 참고**: 업계에서 널리 알려지거나 유사 문제에 흔히 사용되는 접근법. 라이선스·제품 의존성 때문에 기본 구현으로 채택하지 않을 수도 있음
- **Comparison / 연구 비교 후보**: 최신 연구성, 높은 계산량, 구조적 차이를 비교하기 위한 모델. 운영 기본값으로 자동 승격하지 않음

공개 형태도 함께 구분한다.

- **Pretrained**: 공개 가중치를 바로 추론에 사용할 수 있음
- **Baseline code**: 구현·평가 코드가 공개됐지만 옥동 데이터로 학습해야 함
- **Algorithm/library**: 학습 없이 적용하거나 자체 데이터로 구성해야 함

최종 채택은 “가장 최신 모델”이 아니라 **현장 정확도 + E2E 지연 + 자원 사용 + 배포성 + 운영성 + 라이선스**를 함께 비교해 결정한다.

### 원칙 5. 단순하고 설명 가능한 기준선을 먼저 둔다

딥러닝 모델이 항상 정답은 아니다.

- MES·센서 품질 판별: LightGBM/XGBoost 우선
- CAD-실물 형상 비교: Open3D 정합 우선
- 고정 위험구역 접근: 사람 검출 + PLC/ROI 규칙 우선
- 딥러닝 모델은 정확도 또는 일반화 이득이 확인될 때 승격


### 원칙 6. 1차는 모델 endpoint, 2차는 Stage를 오프로딩 단위로 삼는다

1차 구현은 동일 모델을 Local/Jetson/Server에 미리 배포하고 요청 endpoint를 전환하여 모델 단위 Placement를 검증한다. 서비스 전체 컨테이너의 실시간 이동은 1차 기본 방식으로 사용하지 않는다.

모델 단위 Placement가 검증된 뒤에는 서비스를 여러 Stage로 분해하고 각 Stage 또는 연속된 Stage 그룹이 어디서 실행되는 것이 좋은지 판단한다.

예를 들어 S06은 다음과 같이 분해할 수 있다.

`Sensor Reader → Time Align → Windowing → FFT/RMS 또는 TCN → Decision → Publisher`

이때 `Sensor Reader`와 `Decision`은 로컬에 유지하고, 계산량이 큰 `TCN` Stage만 Edge AI Server GPU로 오프로딩할 수 있다.

Stage 경계를 정할 때는 연산시간뿐 아니라 **경계에서 이동해야 하는 데이터 크기**를 함께 본다. 원시 영상·원시 센서 window보다 feature vector가 작다면, feature extraction 이후를 cut-point로 선택하는 것이 네트워크 비용 측면에서 더 유리할 수 있다.

### 원칙 7. Model Library·Component Library·Stage·Execution Unit을 구분한다

각 용어의 의미를 다음과 같이 구분한다.

- **Model Library**: Kubeflow Hub metadata, MinIO/S3 artifact, Edge 호환성 metadata와 profile reference를 결합한 논리 카탈로그
- **Component Library**: Windowing, FFT, Decision처럼 재사용 가능한 실행 구현
- **Stage**: Workflow 안에서 Component Library 또는 Model을 설정과 함께 참조하는 실행 인스턴스
- **Execution Unit**: 하나 이상의 연속 Stage를 묶어 실제 Pod/Deployment 또는 endpoint로 실행하는 배포 단위

Component Library와 Model에는 최소한 다음 메타데이터가 있어야 한다.

- 기능·버전·입출력 schema
- `arm64`/`amd64` 호환성
- CPU/GPU/NPU runtime 후보
- `stateful`/`stateless`
- `offloadable` 여부와 로컬 고정 여부
- 입력·출력 크기와 shape
- CPU/GPU/NPU·메모리 요구량
- p50/p95 실행시간과 throughput
- cold start/model load 시간
- artifact digest와 runtime/container version
- 실패 코드와 retry/idempotency 특성

upstream/downstream dependency, fallback subgraph, cut-point와 rollback은 개별 Library가 아니라 Workflow/Placement 정책에서 관리한다. Stateful Component는 state migration 정책이 검증되기 전까지 `local-pinned`을 기본값으로 둔다.

---

## 1.4 기존 기술 활용과 과제 개발 경계

### 1.4.1 기존 기술을 그대로 활용하는 영역

| 기능 | 활용 기술 | 과제에서의 사용 방식 |
|---|---|---|
| 모델 등록·버전·Catalog·Artifact metadata 관리 | **Kubeflow Hub standalone** | 모델 ID/버전/Task/Artifact 참조의 기준 metadata 저장소 |
| Registry metadata 저장 | **Hub backend RDBMS** | Hub metadata 영속화·백업·복구 |
| 모델 Artifact 저장 | **MinIO/S3** | ONNX, TensorRT engine, PyTorch weight, LightGBM model 저장 |
| ARM/x86 CPU 추론 | **ONNX Runtime** | RPi/Jetson/Server CPU 공통 runtime 후보 |
| Jetson GPU 실행 | **TensorRT / ORT CUDA** | Jetson GPU 최적화 runtime |
| Server GPU Serving | **NVIDIA Triton** | 다중 모델 loading/version/serving |
| Server GPU 성능 탐색 | **Triton Model Analyzer** | batch/concurrency/instance에 따른 latency/throughput/GPU memory 탐색 |
| GPU 상태 수집 | **DCGM/Prometheus** | GPU utilization, memory, power, temperature |
| 배포·장치 관리 | **Kubernetes/KubeEdge** | 모델 runtime Pod 배포와 Edge node 관리 |

이 기능들은 과제의 독자 핵심기술로 주장하지 않는다. **기존 공개SW 기반 인프라**로 사용한다.

Kubeflow Hub는 모델 metadata를 관리하는 passive repository로 사용하며 Placement, Kubernetes orchestration 또는 Edge 오프로딩 판단을 담당시키지 않는다. Full Kubeflow의 Pipelines, Katib, Trainer, Notebooks 등은 1차 범위에 포함하지 않는다.

### 1.4.2 과제에서 직접 개발할 영역

| 개발 모듈 | 입력 | 출력 | 차별점 |
|---|---|---|---|
| **ModelRegistryPort / KubeflowHubAdapter** | Hub model/version/artifact metadata | Registry 독립 공통 model contract | Hub API 변경·교체가 과제 모듈에 전파되지 않도록 격리 |
| **EdgeModelDescriptor Extension** | Registry model/version | HW/runtime compatibility metadata | 일반 Registry에 없는 Edge 실행환경 정보 확장 |
| **Edge Model Profiler** | model + fixed input + target device/runtime | latency/resource profile | RPi/Jetson/Server를 동일 조건으로 자동 비교 |
| **Profile Store / Aggregator** | 장치별 profiler 결과 | 상세 원시 결과 + 통합 Model Execution Profile | 반복 측정은 별도 저장하고 Hub에는 profile reference와 요약만 연결 |
| **Device State Collector 연계** | 현재 CPU/GPU/NPU/MEM/network | current state | 사전 프로파일과 실시간 상태 결합 |
| **Placement Engine** | profile + state + network + SLO | ranked placement candidates | 모델·장치·네트워크를 통합한 위치 결정 |
| **Offloading Controller** | placement decision | routing/deployment action | Local↔Server 또는 accelerator 전환 및 rollback |

핵심 연구질문은 **“모델을 어떻게 저장하는가?”가 아니라 “등록된 모델을 어떤 장치·runtime에서 실행해야 현재 서비스 요구조건을 만족하는가?”**다.

### 1.4.3 전체 시스템 구조

```text
                    [ 기존 공개SW 계층 ]

                 Kubeflow Hub standalone ─── Hub Metadata DB
                          │
                 Model/Version/Artifact Meta
                          │
                       MinIO/S3
                          │
                    [ 과제 개발 경계 ]
                          ▼
                  ModelRegistryPort
                          │
                  KubeflowHubAdapter
                          │
             ┌────────────┼────────────┐
             │            │            │
          RPi/ARM       Jetson      Edge Server
        ONNX Runtime   ORT/TRT        Triton
             │            │            │
             └────────────┼────────────┘
                          │
                 Edge Model Profiler ───── Profile Store
                          │                        │
                          └──── Profile Aggregator ┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
               Model Profile      Device State      Network State
                    └─────────────────┼─────────────────┘
                                      ▼
                               Placement Engine
                                      │
                             Offloading Controller
                                      │
                      Pre-deployed Endpoint Routing
                                      │
                           Kubernetes / KubeEdge
                                      │
                                 AI Service
```

### 1.4.4 Model Library의 재정의

본 문서에서 **Model Library**는 새로운 파일 저장소를 의미하지 않는다.

```text
Model Library
 = Kubeflow Hub Model Metadata
 + MinIO/S3 Artifact
 + EdgeModelDescriptor
 + Runtime Compatibility
 + ModelExecutionProfile Reference
```

예를 들어 하나의 TCN 모델은 다음 정보를 가진다.

```yaml
modelId: tcn-vibration-anomaly
version: 1.0.0

registry:
  type: kubeflow-hub
  registeredModel: tcn-vibration-anomaly
  modelVersion: 1.0.0
  artifactRef: TBD

task:
  type: time-series-anomaly-detection

artifacts:
  preferredFormat: onnx
  alternatives: [pytorch]

runtimeCompatibility:
  - runtime: onnxruntime-cpu
    architectures: [arm64, amd64]
  - runtime: onnxruntime-cuda
    architectures: [arm64, amd64]
  - runtime: tensorrt
    architectures: [arm64, amd64]
  - runtime: triton-tensorrt
    architectures: [amd64]

profiles:
  summaryRef: pending
  detailedProfileStore: pending
```

상세 `profiles`는 사람이 임의로 작성하지 않고 **Profiler 실행 결과로 Profile Store에 생성·갱신**한다. Hub에는 qualification 요약과 최신 profile reference만 연결한다.

---

# 2. AI 서비스 마스터 카탈로그

## 2.1 서비스 목록

| ID | 서비스명 | 현장 목적 | 데이터원 | AI/분석 Task | 결과 출력 | 초기 우선순위 |
|---|---|---|---|---|---|---|
| S01 | HOT SAW 위험구역 작업자 감지 | 톱날 가동 중 작업자 위험구역 진입 경고 | Vision + PLC | 사람 검출·추적 + PLC/ROI 규칙 결합 | 위험 여부, 대상 ID, 위치, 근거 프레임 | 중 |
| S02 | 열처리로 전·후면 고온위험구역 감지 | 고온 설비 가동 중 작업자 접근 경고 | Vision + PLC | 사람 검출·추적 + 설비 상태·구역 규칙 | 접근 경고, 대상 위치, 설비 상태 | 중 |
| S03 | 포장라인 지게차·작업자 복합재해 감지 | 지게차-작업자 근접 및 쓰러짐 감지 | Vision | 객체 검출·추적·거리 계산 + 포즈/행동 판별 | 근접위험, 쓰러짐, 궤적 | 중 |
| S04 | 버너존 화재·연소 이상전조 감지 | 열폭주·연소 불균형 조기 경고 | PLC | 다변량 시계열 이상탐지·분류 | 이상 점수, 이상 구역, 원인 변수 | 중 |
| S05 | 압출금형 번호 비전 OCR 오투입 방지 | 금형 표기와 MES 지시서 불일치 방지 | Vision + MES | 텍스트 검출·인식 + MES 교차검증 | 인식 번호, 일치 여부, 재확인 요청 | 낮음/보류 |
| S06 | 메인/AUX 유압펌프·모터 이상감지 | 구동부 고장 전조 탐지 및 예지보전 | 진동·소음 센서 + MES | 음향·진동 이상탐지, 다변량 시계열 융합 | 종합 이상점수, 부품별 점수, 근거 주파수/변수 | 최우선 |
| S07 | 와이어(풀러) 이송장력·마찰 이상감지 | 주행속도별 마찰·이송 이상 조기 감지 | 진동 + 소음 + PLC | 조건부 시계열 이상탐지 | 마찰 이상점수, 속도 조건, 근거 신호 | 중 |
| S08 | 스트레처 인장구동계 과부하 감지 | 렌치볼트 진동편차·인장유압 기반 과부하 경고 | 진동 + 소음 + PLC | 다변량 이상탐지·상태 분류 | 과부하 상태, 이상점수, 근거 변수 | 중 |
| S09 | 2D/3D 스캐너 기반 CAD 도면 품질검증 | CAD 대비 실물 형상·열변형 오차 판정 | 스캐너 + PLC | 포인트클라우드 정합·치수 오차 분석 | 정합 점수, 위치별 편차, 합격/불합격 | 낮음/장비대기 |
| S10 | 절단면 조도·치수 정밀도 검사 | 표면 결함·조도·치수 불량 자동 판정 | Vision + 스캐너 | 영상 이상검사 + 계측값 융합 | 결함 맵, 조도·치수 편차, 불량 유형 | 낮음/장비대기 |
| S11 | 센서+MES 데이터 활용 생산품질 판별 | 생산조건과 품질 결과의 상관관계 분석 및 양불 판별 | 센서 + MES + 품질/Q-Data | 표형 데이터 분류·회귀·설명 | 양품/불량 확률, 품질 예측값, 주요 영향 변수 | 최우선 |

## 2.2 제조 현장에서 이 서비스 유형을 사용하는 이유

| 서비스 유형 | 일반적인 도입 이유 | 본 PoC에서의 의미 |
|---|---|---|
| 안전 영상분석 | 기존 CCTV를 활용하면서 위험구역 접근·근접·전도를 자동 감지할 수 있음 | PLC 설비 상태와 영상 결과를 결합해 단순 사람 검출보다 오경보를 줄일 수 있음 |
| 설비 이상감지·예지보전 | 고장을 사후 발견하는 대신 진동·소음·온도 변화로 이상 전조를 확인할 수 있음 | 현장에 진동·소음 센서가 이미 설치돼 있고 구동부 손실 위험이 큼 |
| 생산품질 판별 | MES 생산조건과 품질 결과를 연결해 불량 원인·조건을 찾을 수 있음 | Q-Data와 PLC·센서 상관관계 분석 요청과 직접 연결됨 |
| OCR 오투입 방지 | 수기 입력·육안 확인 과정의 식별 오류를 줄일 수 있음 | 금형 번호와 MES 지시서를 교차검증하되, 현장 수기 페인트마카 특성 때문에 재학습이 필요함 |
| 영상·3D 품질검사 | 검사자의 주관성을 줄이고 위치별 결함·치수 편차를 반복 가능하게 측정할 수 있음 | 신규 스캐너·라인스캔 장비 도입 이후 적용 가능한 고부하 서비스임 |

## 2.3 서비스 실행 특성 분류

| Workload Class | 포함 서비스 | 실행 형태 | 시간 제약 | 예상 주 병목 |
|---|---|---|---|---|
| W1 안전 영상 스트림 | S01, S02, S03 | 지속 스트림 | 지연 민감, 로컬 실패 안전 필요 | GPU/NPU compute, 영상 decode, 메모리 대역폭 |
| W2 설비 시계열 이상탐지 | S04, S06, S07, S08 | 주기적 window 추론 | Soft real-time | CPU, 경우에 따라 GPU, 입력 정렬 |
| W3 표형 데이터 품질판별 | S11 | 제품·lot 이벤트 또는 주기적 추론 | 공정 이벤트 기반 | CPU, 데이터 join, feature 생성 |
| W4 OCR·식별 | S05 | 금형 교체 이벤트 | 이벤트 응답형 | CPU/GPU/NPU, 영상 전처리 |
| W5 영상 품질검사 | S10 | 제품 통과·검사 이벤트 | 처리량 민감 | GPU compute/VRAM, 고해상도 I/O |
| W6 3D 형상검사 | S09 | 검사 단위 batch | 완료시간·정확도 중심 | CPU/GPU, RAM/VRAM, 포인트 수 |

---


## 2.4 서비스 분할을 위한 Component Library 확장 구조

Model Library/Profiler가 구축된 이후, 서비스 분할 실험을 위해 전처리·특징추출·판단 로직을 재사용 가능한 Component Library로 확장한다. Workflow의 Stage는 Component Library 또는 Model을 참조하는 실행 인스턴스이며, 하나 이상의 연속 Stage를 Execution Unit으로 묶어 실제 배포한다. 이 계층은 **1차 Model Library 구축의 선행조건이 아니라 2차 서비스 분할 확장 계층**이다.

```text
                    ServiceDescriptor
                           │
                           ▼
                     Workflow DAG
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
      Stage A             Stage B            Stage C
 ComponentLibraryRef       ModelRef      ComponentLibraryRef
        │                  │                  │
        └──────────────────┴──────────────────┘
                           │
                           ▼
                  Execution Unit(s)
                           │
                           ▼
                   Resource Profiles
                           │
                           ▼
                    Placement Engine
                  ┌────────┴────────┐
                  ▼                 ▼
             Edge Device       Edge AI Server
```

### 2.4.1 공통 Library 계층

| 계층 | Library 예시 | 역할 | 일반적인 배치 |
|---|---|---|---|
| Input Adapter | EdgeX Reader, MQTT Reader, PLC Reader, MES Reader, Camera Reader | 물리/가상 데이터 소스 연결 | 데이터 생성 지점에 가깝게 고정 |
| Preprocessing | Time Align, Windowing, Normalize, ROI Crop, Resize | 입력 정렬·정규화·window 생성 | Local 우선 |
| Feature | FFT/RMS, Spectral Feature, Tabular Join, Track Distance | 모델 전·후의 도메인 특징 계산 | CPU Local 우선, 계산량에 따라 이동 가능 |
| Inference | LightGBM, TCN, TimesNet, RTMDet, RTMPose, PP-OCR, PatchCore | AI/ML 핵심 추론 | 모델 특성에 따라 CPU/GPU/NPU 후보 |
| Decision | Anomaly Rule, Safety ROI Rule, MES Cross-check, Quality Rule | 서비스 의미로 변환·규칙 결합 | Local 또는 결과 소비 지점 |
| Output | MQTT Publisher, Dashboard Publisher, MES Writer | 결과 전달·알람·저장 | 소비 시스템에 가깝게 배치 |

### 2.4.2 Component Library Catalog 초안

| Library ID | Library | 유형 | Stateful | Offload | 대표 실행자원 |
|---|---|---|---|---|---|
| LIB-IO-EDGEX | EdgeX Reader | Input | Y | No/Local pinned | CPU |
| LIB-IO-PLC | PLC Reader | Input | Y | No/Local pinned | CPU |
| LIB-IO-MES | MES Reader | Input | Y | 조건부 | CPU |
| LIB-IO-CAMERA | Camera Reader/Decoder | Input | Y | Local pinned | CPU/GPU decode |
| LIB-PRE-TIMEALIGN | Time Alignment | Preprocess | Y(window buffer) | Yes | CPU |
| LIB-PRE-WINDOW | Windowing | Preprocess | Y | Yes | CPU |
| LIB-PRE-NORM | Normalization | Preprocess | N | Yes | CPU |
| LIB-PRE-ROI | ROI Crop/Resize | Preprocess | N | Yes | CPU/GPU |
| LIB-FEAT-FFT | FFT/RMS/Spectral Feature | Feature | N | Yes | CPU |
| LIB-FEAT-TABJOIN | Sensor+MES Feature Join | Feature | Y | Yes | CPU |
| LIB-INF-LGBM | LightGBM Runtime | Inference | N | Yes | CPU |
| LIB-INF-TCN | 1D-CNN/TCN Runtime | Inference | N | Yes | CPU/GPU |
| LIB-INF-TIMESNET | TimesNet Runtime | Inference | N | Yes | CPU/GPU |
| LIB-INF-RTMDET | RTMDet Runtime | Inference | N | Yes | GPU/NPU, CPU fallback |
| LIB-INF-RTMPOSE | RTMPose Runtime | Inference | N | Yes | GPU/NPU |
| LIB-INF-PPOCR | PP-OCR Runtime | Inference | N | Yes | CPU/GPU/NPU |
| LIB-INF-PATCHCORE | PatchCore Runtime | Inference | Y(memory bank) | Yes | GPU/CPU |
| LIB-ALG-ICP | Open3D Registration/ICP | Algorithm | N | Yes | CPU/GPU |
| LIB-DEC-ANOMALY | Anomaly Decision | Decision | N | Yes | CPU |
| LIB-DEC-SAFETY | Safety ROI/PLC Rule | Decision | Y(track/state) | Local preferred | CPU |
| LIB-DEC-MESCHECK | MES Cross-check | Decision | N | Yes | CPU |
| LIB-OUT-DASH | Dashboard Publisher | Output | N | Yes | CPU |
| LIB-OUT-MQTT | MQTT Publisher | Output | Y(connection) | Local preferred | CPU |

Stateful Component는 상태 권한·저장·복구·이동 방식을 별도 계약으로 검증하기 전까지 `local-pinned`으로 취급한다. 따라서 위 표의 Stateful 항목에 표시된 Offload 가능 여부는 2차 후보를 뜻하며, 1차 구현의 동적 이동 허용을 뜻하지 않는다.

### 2.4.3 서비스별 기본 Workflow chain

| 서비스 | 기본 Workflow/Library chain | 주 오프로딩 후보 Stage |
|---|---|---|
| S01 | Camera → ROI/Resize → RTMDet → ByteTrack → PLC/ROI Safety Rule → Alert | RTMDet |
| S02 | Camera → ROI/Resize → RTMDet → ByteTrack → Equipment/ROI Rule → Alert | RTMDet |
| S03 | Camera → ROI/Resize → RTMDet → ByteTrack → Distance Rule → (선택) RTMPose → Alert | RTMDet, RTMPose |
| S04 | PLC Reader → Time Align → Window → Feature → LightGBM/TimesNet → Decision | TimesNet |
| S05 | Camera → ROI/Deskew → PP-OCR → MES Cross-check → Operator Review | PP-OCR |
| S06 | EdgeX Sensor → Time Align → Window → FFT/RMS → LightGBM **또는** Window → TCN/TimesNet → Decision → Dashboard | TCN, TimesNet |
| S07 | Sensor+PLC → Time Align → Window → FFT/RMS → LightGBM/TCN → Decision | TCN |
| S08 | Sensor+PLC → Time Align → Window → Feature Join → LightGBM/TCN → Decision | TCN |
| S09 | Scanner Reader → Point Filter → Initial Registration → ICP → Deviation/Tolerance → Report | Initial Registration+ICP |
| S10 | Camera/Scanner → ROI/Normalize → PatchCore/PaDiM → Measurement Fusion → Decision | PatchCore |
| S11 | Sensor+MES → Time Join → Feature Build → LightGBM → Quality Decision → MES/Dashboard | 보통 Local/CPU 유지, 대규모 feature join은 서버 후보 |

### 2.4.4 Cut-point의 의미

오프로딩 후보는 `Library A` 자체뿐 아니라 **어느 Library 사이에서 데이터를 끊어 전송할지**까지 포함한다.

예를 들어 S06 TCN 경로는 다음 두 방식이 가능하다.

```text
A) Raw/Window offload
Device: Reader → Align → Window ──────► Server: TCN → Decision

B) Feature offload
Device: Reader → Align → Window → FFT/Feature ──────► Server: Classifier → Decision
```

A는 서버에서 더 많은 계산을 수행할 수 있지만 전송 payload가 커질 수 있다. B는 디바이스 CPU를 일부 사용하지만 전송량을 줄일 수 있다. 따라서 Placement Engine은 **실행시간 + 전송시간 + queue + serialization + 결과 반환시간**을 모두 고려해야 한다.

---

# 3. Model Library 등록 후보 및 공개 대표 모델·도구 매핑

## 3.1 모델 선정 원칙

모델은 “논문 성능이 가장 높은가”만으로 고르지 않는다. 본 과제에서 모델은 AI 정확도 자체보다 **서비스 배치·오프로딩·자원 증강을 검증하기 위한 실제 workload**이기도 하다.

따라서 다음 순서로 선정한다.

1. **현장 Task 적합성**: 실제 입력과 출력이 모델 구조에 자연스럽게 맞는가
2. **현업 적용성**: 제조 현장에서 흔히 쓰는 방법 또는 설명 가능한 baseline인가
3. **배포성**: ARM64/x86_64, CPU/GPU, ONNX/TensorRT/OpenVINO 등 실제 runtime으로 배포 가능한가
4. **운영성**: cold start, 모델 크기, 관측성, 롤백, 유지보수가 가능한가
5. **공개성·재현성**: 공식 구현·가중치·평가 절차가 공개되어 있는가
6. **정확도**: 옥동 데이터 기준 성능이 서비스 요구치를 만족하는가
7. **자원 특성 차이**: 경량/중간/고부하 후보 간 계산량 차이가 있어 배치·증강 실험이 가능한가
8. **라이선스**: 코드·가중치·데이터·의존성의 내부/상용 사용 조건이 명확한가

즉, **Primary는 현업 대표성과 운영성을 우선**하고, **Comparison은 연구적 비교와 자원 특성 차이를 확인하기 위해 사용**한다.

## 3.2 서비스별 최종 후보 구조

| 서비스 | Primary: 현업 대표/운영 후보 | Industry Reference | Comparison: 연구·고부하 후보 | 선정 근거 |
|---|---|---|---|---|
| S01 HOT SAW 작업자 감지 | **RTMDet + ByteTrack + PLC/ROI rule** | YOLO 계열 + Tracker | 더 큰 RTMDet 또는 pose 결합 | 위험구역 감지는 검출·추적과 설비 상태 결합이 핵심. AI 단독보다 PLC/ROI rule을 결합하는 구조가 현장 설명성이 높음 |
| S02 고온위험구역 감지 | **RTMDet + ByteTrack + 설비상태/ROI rule** | YOLO 계열 + Tracker | RTMPose 결합 행동판별 | 사람 검출 자체보다 “설비 가동 중 특정 구역 진입” 판정이 서비스 본질이므로 규칙 결합을 Primary로 둠 |
| S03 지게차·작업자 복합재해 | **RTMDet fine-tuning + ByteTrack** | YOLO 계열 + Tracker | **RTMPose** 기반 전도 판별 | 근접위험은 Detection/Tracking이 현업적으로 단순·재현성이 높고, 전도는 pose를 별도 고부하 stage로 분리 가능 |
| S04 버너존 이상전조 | **온도 상승률/편차 rule + LightGBM** | XGBoost/GBDT 계열 | **TimesNet** 등 시계열 딥러닝 | PLC·온도 기반 안전 전조는 규칙과 GBDT가 설명성이 높음. TimesNet은 비교·고부하 후보 |
| S05 금형번호 OCR | **PP-OCRv5 fine-tuning + MES rule** | 상용 OCR/YOLO-OCR 조합 | detector/recognizer ensemble | OCR은 전용 모델을 쓰는 것이 합리적. 단, 현장 수기 페인트마카 특성상 자체 fine-tuning과 manual review가 필수 |
| S06 펌프·모터 이상감지 | **FFT/RMS feature + LightGBM** 또는 **1D-CNN/TCN** | FFT/RMS + threshold, tree-based ML, 1D-CNN 계열 | **DCASE AE**, **TimesNet** | 실제 condition monitoring에서는 주파수 특징·경량 ML·1D-CNN이 설명성과 배포성이 좋음. DCASE는 공개 benchmark baseline, TimesNet은 고부하 비교 모델로 사용 |
| S07 풀러 마찰 이상 | **속도조건별 FFT/RMS + LightGBM/TCN** | condition-based monitoring | DCASE AE / TimesNet | 속도 조건에 따라 정상 분포가 달라지는 문제이므로 조건부 특징 기반 모델이 Primary |
| S08 스트레처 과부하 | **압력·진동 feature + LightGBM/TCN** | rule + GBDT | TimesNet | 과부하 여부와 기여 변수를 설명하기 쉬운 구조를 우선하고 복잡 시계열 모델은 비교용 |
| S09 3D CAD 품질검증 | **Open3D FPFH/RANSAC + ICP + 거리오차** | 산업용 3D registration/inspection | Open3D-ML/point-cloud network | 이 문제는 먼저 기하학적 정합·공차 판정이 정석. AI를 억지로 넣지 않음 |
| S10 절단면 조도·품질검사 | **PatchCore 또는 PaDiM (Anomalib)** + 계측 rule | 산업 비전 anomaly detection | EfficientAD/segmentation 계열 | 불량 데이터가 적고 정상 데이터가 많은 제조 검사를 고려. defect map과 score를 제공해 설명 가능 |
| S11 생산품질 판별 | **LightGBM** | XGBoost/GBDT | 필요 시 TabPFN/신경망 tabular 모델 | 센서 집계값+MES 조건은 표형 데이터이므로 GBDT가 자연스러움. 작은 데이터에서도 강하고 feature importance 제공 가능 |

## 3.3 “현업에서 쓰는가?”에 대한 분류

| 모델·방법 | 현업 적용성 | 본 문서에서의 역할 | 비고 |
|---|---|---|---|
| **FFT/RMS + threshold** | 매우 높음 | S06~S08 최소 기준선 | 상태감시의 가장 단순하고 설명 가능한 기준 |
| **LightGBM / XGBoost** | 매우 높음 | S04·S06~S08·S11 Primary/기준선 | tabular·sensor feature 기반 분류/회귀에 적합 |
| **1D-CNN / TCN** | 높음 | S06~S08 Primary 딥러닝 후보 | 센서 시계열을 직접 다루면서 비교적 경량 |
| **PaddleOCR / PP-OCR** | 높음 | S05 Primary | OCR 특화, CPU/GPU 배포 경로가 명확 |
| **Open3D + ICP** | 매우 높음 | S09 Primary | 3D 정합에서 연구·산업 모두 널리 쓰는 정석적 방법 |
| **PatchCore / PaDiM / Anomalib** | 중~높음 | S10 Primary | 산업 영상 이상탐지의 대표적 baseline/구현체 |
| **RTMDet + ByteTrack** | 중~높음 | S01~S03 기본 구현 | OpenMMLab 생태계와 배포 도구가 안정적 |
| **YOLO 계열** | 매우 높음(인지도·현장 사용) | Industry Reference | 현업에서 익숙하지만 사용 버전별 라이선스 검토 필요 |
| **DCASE AutoEncoder** | 연구 benchmark 성격이 강함 | S06·S07 Comparison | 기계 이상음 공개 benchmark와 평가 절차 확보가 장점 |
| **TimesNet** | 연구 비교 성격이 강함 | S04·S06~S08 Comparison | 최신 시계열 구조 비교 및 GPU/고부하 실험에 유용 |
| **RTMPose** | 중~높음 | S03 고급 stage | 전도/행동판별에 활용하되 detector보다 계산량 증가 |

## 3.4 공개 모델·도구 및 사용 목적

| 공개 모델·도구 | 적용 Task | 공개 형태 | 라이선스/주의 | 실행 자원 | 본 과제 사용 목적 |
|---|---|---|---|---|---|
| MMDetection RTMDet | 객체 검출 | Pretrained + 학습 코드 | Apache-2.0 | CPU/GPU, NPU 변환 검증 필요 | S01~S03 Primary detector |
| ByteTrack | 다중 객체 추적 | Algorithm/code | MIT | CPU 중심, detector 결과 사용 | 안전 서비스 궤적 유지 |
| MMPose RTMPose | 사람 포즈 추정 | Pretrained + 학습 코드 | Apache-2.0 | CPU/GPU/NPU 후보 | S03 Comparison / 고급 stage |
| LightGBM | 표형·feature 기반 분류/회귀 | Library | MIT | CPU 추론 중심 | S04·S06~S08·S11 Primary |
| XGBoost | 표형 데이터 분류·회귀 | Library | Apache-2.0 | CPU/GPU 학습, CPU 추론 중심 | LightGBM 비교 기준 |
| 1D-CNN / TCN | 센서 시계열 | 자체/공개 구현 다수 | 구현별 확인 | CPU/GPU | S06~S08 Primary 딥러닝 후보 |
| DCASE Task 2 baseline AE | 기계 이상음 탐지 | Baseline code + 공개 평가 절차 | 프로젝트별 조건 확인 | CPU/GPU | 공개 benchmark 비교 |
| TimesNet / Time-Series-Library | 시계열 이상탐지·분류 | Baseline code | MIT | CPU/GPU | 연구·고부하 비교 |
| PaddleOCR PP-OCRv5 | 텍스트 검출·인식 | Pretrained + fine-tuning | Apache-2.0, 의존성·가중치 별도 검토 | CPU/GPU, NPU 후보 | S05 Primary |
| Anomalib | 영상 이상검사 | 모델 모음 + 학습·추론·export | Apache-2.0, 각 모델 조건 검토 | GPU 중심, 일부 CPU | S10 Primary/Comparison |
| Open3D | 3D 정합·처리 | Algorithm/library | MIT | CPU, 일부 CUDA 경로 | S09 Primary |

### YOLO 사용 방침

YOLO 계열은 실제 현장에서 매우 익숙한 객체검출 계열이므로 **Industry Reference**로 유지한다. 다만 본 과제의 공개 기본 구현은 라이선스·재배포 조건을 단순화하기 위해 RTMDet을 우선한다.

- 기본 구현: RTMDet + ByteTrack
- 현업 비교: YOLO 계열 + Tracker
- 실제 제품/내부 운영 플랫폼 포함 시 해당 배포판의 라이선스 재검토

## 3.5 서비스별 모델 후보의 역할

각 서비스의 모델 후보는 다음 세 역할을 가진다.

- **Baseline**: 가장 단순하고 설명 가능한 방법. “AI가 없어도 어느 정도 가능한가?”를 확인
- **Primary**: 실제 운영 후보. 정확도와 배포성의 균형이 좋아야 함
- **Comparison**: 연구 비교 또는 고부하 모델. Primary 대비 정확도·지연·자원 사용 trade-off를 확인

모델을 승격할 때는 다음을 모두 본다.

| 평가축 | 기준 |
|---|---|
| 기능 정확도 | 서비스별 Precision/Recall/F1/AUROC/MAE 등 사전 정의 |
| 지연 | 입력 준비 시점부터 결과 소비 시점까지 E2E p50/p95/p99 |
| 처리량 | 입력률 대비 완료율, backlog 증가 여부 |
| 자원 | CPU saturation·throttling, GPU utilization·VRAM, NPU utilization, RAM, 전력 |
| 배포성 | ARM64/x86_64, ONNX/TensorRT/OpenVINO/NPU SDK 변환 가능성 |
| 운영성 | 모델 크기, cold start, 업데이트, 롤백, 관측성 |
| 설명성 | feature importance, anomaly band, bounding box, defect map 등 현장 근거 제공 가능성 |
| 라이선스 | 코드·가중치·학습데이터·의존성·특허의 내부/상용화 적합성 |

Comparison 모델이 정확도에서 조금 앞서더라도 E2E 지연·자원 사용·운영성이 크게 불리하면 Primary로 자동 승격하지 않는다.


# 4. 데이터 및 서비스 계약

## 4.1 모든 서비스가 공통으로 가져야 할 정보

- `service_id`: 변경되지 않는 고유 ID
- `service_version`: 서비스 구현 버전
- `model_name`, `model_version`, `model_hash`
- `data_contract_version`
- 입력 source ID와 실제 물리 장치 ID
- 입력 timestamp와 time sync 상태
- 입력 누락·stale·out-of-order 처리 규칙
- 전처리 버전과 feature schema
- 결과 timestamp, confidence, anomaly score
- 근거 정보: bounding box, feature importance, 주파수 대역, deviation map 등
- 실패 코드: input invalid, model not ready, timeout, OOM, conversion error 등
- 실행 위치와 accelerator 정보

## 4.2 데이터 계약 초안

### 센서·PLC·MES

- 센서: X/Y/Z RMS, FFT 구간, 소음 스펙트럼, 온도
- PLC: 설비 속도·압력·가동상태·온도 등
- MES/품질: 제품·lot·공정조건·품질·경도·조도 등
- 조인 기준: 제품 ID 또는 시간 window
- 필수 검증: source ID, timestamp, 결측률, 동기 오차, 설비 상태

현재 현장자료 기준으로 진동·소음 센서는 250ms 샘플링 후 5초 단위 20개 batch 전송 구조가 제시돼 있고, PLC와 MES·품질 데이터는 1초 단위 TCP 전송 구조가 정리돼 있다. 이 수치는 최종 계약서에서 실제 장비 설정과 재확인해야 한다.

### Vision

- camera ID, frame ID, capture timestamp
- resolution, FPS, codec
- camera calibration 및 ROI version
- PLC 설비 상태와의 동기 기준
- privacy·외부 반출 권한

### 3D Scanner

- scanner ID, calibration version
- CAD/Part version
- point unit, coordinate system, point count
- 측정 당시 온도·공정 상태
- 정합 초기값과 허용 공차

## 4.3 EdgeModelDescriptor 템플릿

모델 등록의 원본 metadata는 Kubeflow Hub에 두고, 실제 artifact는 MinIO/S3에 저장한다. 과제 플랫폼은 `ModelRegistryPort`를 통해 Hub를 조회하고 Edge 실행을 위한 확장 metadata를 연결한다. 이 Descriptor는 모델의 정적 계약과 실행 후보를 나타내며, 실제 측정 결과인 `ModelExecutionProfile`과 분리한다.

```yaml
apiVersion: edgeai.etri.re.kr/v1alpha1
kind: EdgeModelDescriptor
metadata:
  modelId: tcn-vibration-anomaly
  modelVersion: 1.0.0
  registryType: kubeflow-hub
  registryRef: TBD

spec:
  task:
    type: time-series-anomaly-detection
    domain: condition-monitoring

  artifacts:
    - role: canonical
      format: onnx
      artifactUri: s3://models/tcn-vibration-anomaly/1.0.0/model.onnx
      sha256: TBD
    - role: optimized
      format: tensorrt-engine
      artifactUri: TBD
      sha256: TBD
      target:
        nodeClass: jetson
        architecture: arm64
        precision: fp16
        tensorrtVersion: TBD
        cudaVersion: TBD
        computeCapability: TBD

  io:
    inputSchema: vibration-window-v1
    outputSchema: anomaly-score-v1

  compatibility:
    architectures: [arm64, amd64]
    runtimeCandidates:
      - onnxruntime-cpu
      - onnxruntime-cuda
      - tensorrt
      - triton-tensorrt

  profileSummary:
    latestProfileRefs: []
    detailedProfileStore: edge-profile-store

  qualification:
    functional: pending
    accuracy: pending
    license: pending
```

TensorRT engine 같은 최적화 artifact는 범용 모델 파일로 간주하지 않는다. target device class, architecture, precision, TensorRT/CUDA version과 GPU compatibility 정보를 artifact 단위로 기록한다. ONNX를 우선 canonical 교환 artifact로 유지하고, 최적화 artifact는 대상 환경별 파생물로 관리한다.

### ModelRegistryPort 템플릿

Profiler, Placement Engine과 서비스 계층은 Kubeflow Hub API를 직접 호출하지 않는다. 다음 최소 기능을 공통 Port로 정의하고 `KubeflowHubAdapter`가 이를 구현한다.

```text
register_model(model)
register_version(model_id, version)
get_model(model_id, version)
get_artifact(model_id, version, target)
list_versions(model_id)
set_properties(model_id, version, properties)
update_deployment_status(model_id, version, status)
```

Hub API 또는 API version이 변경되거나 다른 Registry로 교체하더라도 이 Port 바깥의 Profiler·Placement 계약은 유지한다.

### Runtime Adapter 템플릿

```yaml
runtimeId: onnxruntime-cpu
type: edge-runtime

supports:
  formats: [onnx]
  architectures: [arm64, amd64]
  accelerators: [cpu]

adapter:
  load: true
  infer: true
  unload: true
  health: true
  metrics: true
```

### ServiceDescriptor에서 Model Library 참조

서비스는 모델 artifact를 직접 들고 있지 않고 Model Library의 `modelId/version`을 참조한다.

```yaml
serviceId: OKD-SVC-006
version: 0.1.0

modelCandidates:
  - modelId: lgbm-vibration-anomaly
    version: 1.0.0
    role: primary-light

  - modelId: tcn-vibration-anomaly
    version: 1.0.0
    role: primary-deep

  - modelId: timesnet-vibration-anomaly
    version: 1.0.0
    role: comparison

placement:
  policy: profile-aware
  inputs:
    - modelExecutionProfile
    - deviceCurrentState
    - networkState
    - serviceSLO
```

Component Library를 사용하는 Stage-level workflow는 이후 이 Model Library 참조를 inference Stage 내부에 연결한다.


## 4.4 우선 서비스 예시

### S06 메인/AUX 유압펌프·모터 이상감지

S06은 단일 AI 모델 서비스가 아니라 목적이 다른 Workflow variant를 가진 서비스로 정의한다. 운영 시 하나의 variant를 선택하며, Comparison variant를 기본 운영 경로와 동시에 실행하지 않는다.

```text
s06-light:      Sensor → Align → Window → FFT/RMS → LightGBM → Decision → Dashboard
s06-deep:       Sensor → Align → Window → TCN ───────────────→ Decision → Dashboard
s06-comparison: Sensor → Align → Window → TimesNet ──────────→ Decision → Dashboard
```

- `FFT/RMS + LightGBM`: 현업 Primary 경량 경로
- `TCN`: Primary 딥러닝 경로
- `TimesNet`: 연구·고부하 Comparison 경로
- `DCASE AE`: 소음 branch의 공개 benchmark 비교용

```yaml
metadata:
  serviceId: OKD-SVC-006
  name: pump-motor-anomaly-detection

spec:
  workflowVariants:
    - id: s06-light
      role: primary-light
      stages:
        - {id: read, libraryRef: LIB-IO-EDGEX, placementConstraint: local-pinned}
        - {id: align, libraryRef: LIB-PRE-TIMEALIGN, dependsOn: [read], placementConstraint: local-pinned}
        - {id: window, libraryRef: LIB-PRE-WINDOW, dependsOn: [align], placementConstraint: local-pinned}
        - {id: feature, libraryRef: LIB-FEAT-FFT, dependsOn: [window]}
        - {id: inference, modelRef: "lgbm-vibration-anomaly:1.0.0", dependsOn: [feature]}
        - {id: decision, libraryRef: LIB-DEC-ANOMALY, dependsOn: [inference], placementConstraint: local-pinned}
        - {id: output, libraryRef: LIB-OUT-DASH, dependsOn: [decision], placementConstraint: local-pinned}

    - id: s06-deep
      role: primary-deep
      fallbackWorkflowRef: s06-light
      stages:
        - {id: read, libraryRef: LIB-IO-EDGEX, placementConstraint: local-pinned}
        - {id: align, libraryRef: LIB-PRE-TIMEALIGN, dependsOn: [read], placementConstraint: local-pinned}
        - {id: window, libraryRef: LIB-PRE-WINDOW, dependsOn: [align], placementConstraint: local-pinned}
        - {id: inference, modelRef: "tcn-vibration-anomaly:1.0.0", dependsOn: [window]}
        - {id: decision, libraryRef: LIB-DEC-ANOMALY, dependsOn: [inference], placementConstraint: local-pinned}
        - {id: output, libraryRef: LIB-OUT-DASH, dependsOn: [decision], placementConstraint: local-pinned}

    - id: s06-comparison
      role: comparison
      productionEnabled: false
      stages:
        - {id: read, libraryRef: LIB-IO-EDGEX, placementConstraint: local-pinned}
        - {id: align, libraryRef: LIB-PRE-TIMEALIGN, dependsOn: [read], placementConstraint: local-pinned}
        - {id: window, libraryRef: LIB-PRE-WINDOW, dependsOn: [align], placementConstraint: local-pinned}
        - {id: inference, modelRef: "timesnet-vibration-anomaly:1.0.0", dependsOn: [window]}
        - {id: decision, libraryRef: LIB-DEC-ANOMALY, dependsOn: [inference], placementConstraint: local-pinned}
        - {id: output, libraryRef: LIB-OUT-DASH, dependsOn: [decision], placementConstraint: local-pinned}

  placement:
    policy: stage-aware
    allowedCutPoints:
      - window->tcn
      - window->timesnet
      - feature->lgbm
    fallbackWorkflowRef: s06-light
```

TCN 실패 시 LightGBM library 하나만 교체하지 않는다. 두 경로의 입력 schema가 다르므로 `Window → FFT/RMS → LightGBM → Decision` 전체 subgraph인 `s06-light`로 전환한다.

**우선 검증할 배치 조합**

1. 모든 Stage를 Raspberry Pi CPU에서 실행
2. 모든 Stage를 Jetson CPU에서 실행
3. `TCN` Stage만 Edge AI Server CPU로 오프로딩
4. `TCN` Stage만 Edge AI Server GPU로 오프로딩
5. `TimesNet` Stage만 Server GPU로 오프로딩
6. `Reader/Align/Window/Decision`은 로컬 고정

이렇게 해야 “CPU가 높으니 서비스 전체를 서버로 이동”이 아니라 **어떤 Stage가 병목이고 어느 Stage만 이동할 때 이득인지**를 검증할 수 있다.

### S11 센서+MES 생산품질 판별

```text
Sensor ─┐
        ├→ Time/Product Join → Feature Build → LightGBM → Quality Decision → MES/Dashboard
MES ────┘
```

```yaml
metadata:
  serviceId: OKD-SVC-011
  name: production-quality-classification

spec:
  workflow:
    stages:
      - {id: sensor, libraryRef: LIB-IO-EDGEX, placementConstraint: local-pinned}
      - {id: mes, libraryRef: LIB-IO-MES}
      - {id: join, libraryRef: LIB-FEAT-TABJOIN, dependsOn: [sensor, mes]}
      - {id: inference, modelRef: "lgbm-quality-classification:1.0.0", dependsOn: [join]}
      - {id: decision, libraryRef: LIB-DEC-QUALITY, dependsOn: [inference]}
      - {id: output, libraryRef: LIB-OUT-DASH, dependsOn: [decision]}

  placement:
    policy: stage-aware
    allowedCutPoints:
      - join->inference
    fallback: manual-quality-rule
```

S11의 LightGBM 추론 자체는 CPU에서 매우 가벼울 가능성이 높다. 따라서 실제 병목 후보는 모델보다 **Sensor+MES join, feature build, DB 조회**가 될 수 있다. 이 역시 서비스 전체가 아니라 Stage별 프로파일이 필요한 이유다.

### S03 지게차·작업자 복합재해 감지

```text
Camera → Decode/ROI → RTMDet → ByteTrack → Distance Rule ──────→ Alert
                                      └────→ RTMPose(optional) ─┘
```

- 기본 위험경보는 `RTMDet + ByteTrack + Distance Rule`
- 고부하 `RTMPose`는 선택 Stage
- 안전 Rule/Alert Stage는 네트워크 단절을 고려해 Local preferred
- detector/pose만 GPU/NPU 후보로 프로파일

---

# 5. Model Execution Profile 설계

## 5.1 프로파일 계층과 저장 구조

프로파일은 우선 모델 실행환경 기준으로 만들고, Stage-level 분할이 시작되면 Transfer/Workflow profile로 확장한다.

| 계층 | 측정 대상 | 목적 | 개발 우선순위 |
|---|---|---|---|
| **Model Execution Profile** | 동일 model/version을 특정 node/runtime에서 실행 | 모델별 배치 후보·가속기 선택 | **1차** |
| **Device Current Profile** | 현재 node의 CPU/GPU/NPU/MEM/network 상태 | 사전 profile과 실시간 상태 결합 | **1차** |
| **Transfer Profile** | Local↔Server 또는 Stage 경계의 전송비용 | 원격 실행/offloading E2E 비용 계산 | **1차/2차** |
| **Workflow Profile** | 최종 서비스 전체 E2E | 서비스 SLO 최종 검증 | **2차** |

상세 프로파일과 원시 반복 측정 결과는 별도 Profile Store에 저장한다. Kubeflow Hub에는 모델 검색과 배치 후보 선별에 필요한 qualification 요약과 최신 profile reference만 저장한다.

각 Model Execution Profile은 다음 3단계 자원 범위를 가질 수 있다.

| 프로파일 | 의미 | 사용 목적 |
|---|---|---|
| Minimum | 기능은 실행되지만 SLO 여유가 거의 없는 최소 자원 | 배치 가능 여부 하한선 |
| Nominal | 정상 입력률·동시 서비스 조건에서 안정적인 권장 자원 | 기본 스케줄링 |
| Maximum | 승인된 최대 입력률·batch·동시성에서 필요한 자원 | 증강 상한 및 admission control |

서비스의 Resource Profile은 Model/Component Profile의 단순 합이 아니다. Stage 간 queue와 network transfer가 추가되므로 Workflow 단위 E2E 검증을 별도로 수행한다.

## 5.2 측정 항목

### CPU

- CPU request/limit
- 실제 사용량과 cgroup saturation
- throttled period/time
- run queue 및 context switch
- 모델 전처리·추론·후처리 시간 분리

### GPU

- GPU model 및 runtime
- 할당 GPU core/share
- SM utilization, memory controller utilization
- VRAM used/allocated
- batch·stream 수
- kernel time, host-device copy
- 전력·온도·throttling

### NPU

- 지원 operator coverage
- 변환 성공 여부
- compiler/runtime 버전
- utilization 및 device memory
- fallback-to-CPU 발생 여부

NPU는 이름만 포함해 형식적으로 시험하지 않는다. 공개 모델 변환, operator 지원, runtime 관측이 확인된 경우에만 공식 자원 후보로 등록한다.

### Memory·Storage

- working set/RSS
- peak memory
- page fault·swap
- OOM·allocation failure
- 모델·cache·workspace 크기
- cold start 및 모델 loading 시간

### Network

- request/response payload
- serialization/deserialization
- RTT·jitter·loss·retransmission
- queueing·timeout
- upload/download 분리

### Service QoS

- E2E latency p50/p95/p99
- throughput
- backlog length와 backlog age
- deadline miss
- error·timeout·drop
- 결과 정확성 및 model version 일치

Runtime마다 측정 범위가 다르므로 `modelExecutionLatency`, `runtimeQueueLatency`, `clientObservedLatency`, `serializationLatency`, `networkLatency`, `e2eLatency`를 구분한다. 특히 Triton/원격 endpoint의 client latency와 로컬 Runtime Adapter의 순수 infer latency를 같은 지표로 직접 비교하지 않는다. Profile Aggregator가 측정 위치와 포함 구간을 공통 schema로 정규화한다.

## 5.3 Model Execution Profile 레코드 예시

```yaml
profileId: tcn-vibration-anomaly__1.0.0__jetson__tensorrt
modelId: tcn-vibration-anomaly
modelVersion: 1.0.0
artifactSha256: TBD
containerImageDigest: TBD

inputProfile:
  schema: vibration-window-v1
  datasetHash: TBD
  shape: TBD
  bytesP50: TBD
  bytesP95: TBD
  rate: TBD
  batch: 1
  concurrency: 1

measurement:
  warmupRuns: TBD
  measuredRuns: TBD
  precision: fp16
  powerMethod: TBD

target:
  nodeClass: jetson
  architecture: arm64
  cpuModel: TBD
  gpuModel: TBD
  os: TBD
  kernel: TBD
  driverVersion: TBD
  cudaVersion: TBD
  tensorrtVersion: TBD
  runtime: tensorrt
  runtimeVersion: TBD
  accelerator: gpu

constraints:
  cpuRequest: TBD
  cpuLimit: TBD
  memoryRequest: TBD
  memoryLimit: TBD

results:
  modelExecutionP50Ms: TBD
  modelExecutionP95Ms: TBD
  modelExecutionP99Ms: TBD
  runtimeQueueP95Ms: TBD
  clientObservedP95Ms: TBD
  throughputPerSec: TBD
  cpuUsedMillicoresP95: TBD
  peakMemoryMiB: TBD
  gpuUtilizationP95: TBD
  gpuMemoryMiB: TBD
  powerWattsP95: TBD
  cpuSaturation: TBD
  throttledTimeSec: TBD
  coldStartMs: TBD
  referenceOutputError: TBD

qualification:
  functional: pending
  runtimeCompatible: pending
  outputParity: pending
  serviceCandidate: pending
```

## 5.4 Transfer Profile 레코드 예시

```yaml
transferProfileId: S06-window-to-tcn__jetson-to-server__v1
fromStage: window
toStage: tcn

payload:
  schema: vibration-window-v1
  bytesP50: TBD
  bytesP95: TBD

path:
  sourceNode: jetson
  destinationNode: edge-ai-server

results:
  serializeP95Ms: TBD
  uploadP95Ms: TBD
  queueP95Ms: TBD
  downloadP95Ms: TBD
  deserializeP95Ms: TBD
  totalRoundTripOverheadP95Ms: TBD
```

## 5.5 Workflow Profile 레코드 예시

```yaml
workflowProfileId: OKD-SVC-006__tcn-server-gpu__v1
serviceId: OKD-SVC-006

placement:
  read: jetson
  align: jetson
  window: jetson
  tcn: server-gpu
  decision: jetson
  output: jetson

results:
  e2eLatencyP50Ms: TBD
  e2eLatencyP95Ms: TBD
  throughputPerSec: TBD
  backlogAgeP95Ms: TBD
  errorRate: TBD
  deadlineMissRate: TBD

qualification:
  functional: pending
  slo: pending
  placement: pending
```

## 5.6 Model-level 배치와 Stage-level 오프로딩의 관계

1차 Placement Engine은 먼저 **모델 단위 실행 위치**를 결정한다. 이후 Component Library 기반 서비스 분할이 적용되면 동일 판단 구조를 Stage 단위로 확장한다.

원격 모델 또는 Stage를 서버에서 실행하는 것이 유리하려면 단순히 `Server inference < Local inference`이면 부족하다.

다음을 함께 본다.

- 로컬 Stage 실행시간
- 입력 serialize/전송시간
- 서버 queue 대기시간
- 서버 Stage 실행시간
- 결과 반환·deserialize 시간
- upstream/downstream queue 변화
- 최종 Workflow E2E SLO

따라서 Placement Engine은 **실행시간과 데이터 이동비용을 함께** 비교한다. 1차 모델 단위 기준은 `model version + runtime + input profile + source/destination node`로 측정하고, 2차 Stage 확장에서는 `Component Library version + input profile + node pair`를 추가한다.

---

# 6. 서비스별 SLO 설계

## 6.1 모든 서비스에 같은 SLO를 적용하지 않는다

| 구분 | 서비스 | 우선 지표 | 보조 지표 |
|---|---|---|---|
| Safety local-first | S01~S03 | 경보 E2E 지연, frame 처리율, miss rate | 추적 단절, false alarm |
| Condition monitoring | S04, S06~S08 | window deadline, anomaly result age | p95, backlog, feature completeness |
| Event transaction | S05, S11 | 이벤트 완료시간, 결과 일관성 | 재시도율, manual review 비율 |
| Visual inspection | S10 | 검사 처리량, defect recall, 위치 정확도 | GPU memory, image queue |
| 3D batch inspection | S09 | 검사 완료시간, 정합 오차, 공차 판정 | peak RAM/VRAM, point 처리량 |

## 6.2 SLO 수립 절차

1. 입력 주기 또는 공정 이벤트의 실제 시간 budget 확인
2. 현재 수동·기존 시스템의 완료시간 확인
3. 업무상 허용 가능한 deadline 정의
4. 모델 정확도 목표와 서비스 오류 비용 정의
5. 정상 운영 baseline 측정
6. 최소 실용 개선폭을 실험 전에 고정
7. 해당 기준을 후보 실행 위치의 qualification gate로 사용

기존 프록시 실험에서 사용한 “지연 10% 개선·처리량 5% 비열등”은 모델 간 비교를 위한 잠정 gate다. 실제 S06·S11 및 다른 서비스의 운영 승격 기준으로 자동 상속하지 않는다.

---

# 7. 모델 프로파일 기반 초기 배치 가설

아래 내용은 **서비스 전체 배치가 아니라 Stage 기본 위치**에 대한 실험 전 가설이다.

| 서비스 | Local 고정/우선 Stage | 이동 가능한 Stage | 초기 오프로딩 후보 |
|---|---|---|---|
| S01, S02 | Camera, PLC state, Safety Rule, Alert | Detector/Tracker | RTMDet Stage → 로컬 GPU/NPU 또는 Server GPU |
| S03 | Camera, Distance/Safety Rule, Alert | RTMDet, RTMPose | Pose Stage 우선, 필요 시 Detector |
| S04 | PLC Reader, 기본 Rule | LightGBM, TimesNet | TimesNet Stage |
| S05 | Camera/ROI, Operator fallback | PP-OCR | OCR Stage |
| S06~S08 | Sensor Reader, Time Align, Window, 최종 Decision | TCN, TimesNet, 경우에 따라 Feature/LightGBM | TCN/TimesNet Stage |
| S09 | Scanner capture metadata | Registration/ICP/Deviation | Registration+ICP Stage group |
| S10 | Camera capture/ROI | PatchCore/PaDiM/EfficientAD | Visual anomaly Stage |
| S11 | Sensor/MES source access | Join, Feature Build, LightGBM | 모델보다 Join/Feature Stage가 서버 후보가 될 수 있음 |

특히 안전 서비스는 결과 판단·알람 경로를 로컬에 유지하고, 고부하 detector/pose만 원격 실행하는 구조를 우선 검토한다. 반대로 S09·S10처럼 대용량 데이터를 다루는 경우에는 원시 데이터를 원격으로 보내는 비용이 매우 크므로 **데이터 수집 지점과 cut-point**를 먼저 확인해야 한다.

---

# 8. 실험 시나리오 설계

## 8.1 전체 순서

```text
서비스 정의 및 모델 버전 고정
        ↓
데이터 계약·정확도 검증
        ↓
단일 서비스 정상 baseline 측정
        ↓
실제 입력률·동시 서비스 증가
        ↓
자원 병목과 서비스 SLO 저하의 동시 확인
        ↓
동일 입력·동일 모델의 후보 자원 E2E 비교
        ↓
로컬 유지 / 부분 오프로딩 / 전체 오프로딩 / 자원 추가 할당
        ↓
QoS 회복·오류·롤백 검증
        ↓
서비스별 프로파일 및 정책 확정
```

## 8.2 Phase A — 모델 기능 자격 시험

목적: 모델이 현장 서비스 기능을 수행할 수 있는지 먼저 확인한다.

- 입력 schema와 label 정의
- 공개 모델 baseline 실행
- accuracy·recall·AUROC·정합 오차 등 측정
- 실패 사례와 수동 검토
- 모델 버전·hash 고정

기능 정확도가 없는 모델은 자원 실험 대상으로 승격하지 않는다.

## 8.3 Phase B — 장치·runtime별 모델 실행 프로파일링

각 서비스·모델·장치에 대해 다음을 측정한다.

- cold/warm start
- 입력률 ramp-up
- steady plateau
- ramp-down과 회복
- CPU/GPU/NPU/RAM/네트워크
- E2E latency·throughput·backlog
- 최소·권장·최대 프로파일

부하는 CPU percent가 아니라 다음 축을 우선 사용한다.

- 센서 window 수 또는 입력 frame rate
- camera stream 수와 해상도
- 모델 크기
- batch 크기
- 동시 서비스 수
- point-cloud 수와 point count

## 8.4 Phase C — Workflow 조립 및 실제 서비스 동시 실행

### Scenario C1. 설비 모니터링 동시 실행 — CPU 중심

```text
S06 유압펌프·모터 이상감지 정상 실행
  + S04 버너존 이상전조 또는 S07/S08 시계열 서비스 추가
  + S11 생산품질 판별 이벤트 발생
        ↓
CPU·메모리·입력 정렬 경합 관측
        ↓
S06 window deadline 또는 backlog 저하 확인
        ↓
S06 고부하 모델 stage를 Server1로 오프로딩
        ↓
S06 QoS 회복과 S04/S11 영향 확인
```

실제 두 번째 서비스가 준비되지 않았을 때만 CPU 경쟁 부하를 대체 주입한다.

### Scenario C2. 안전 영상 서비스 동시 실행 — GPU/NPU 중심

```text
S01 또는 S02 1개 camera stream 정상 실행
  + S03 지게차·작업자 detector/tracker 추가
  + stream 수·해상도·FPS 점진 증가
        ↓
GPU compute·decode·VRAM·frame queue 관측
        ↓
안전 경보 deadline 또는 frame miss 확인
        ↓
모델 경량화 / NPU 배치 / 고급 pose stage 서버 오프로딩 비교
```

### Scenario C3. 품질검사 burst — GPU·메모리 중심

```text
상시 모니터링 서비스 실행 중
  + S10 고해상도 영상 검사 또는 S09 3D 검사 시작
        ↓
GPU core·VRAM 또는 RAM·CPU 일시 압력 발생
        ↓
안전·설비 모니터링 서비스 영향 확인
        ↓
검사 workload 우선순위 조정 / server worker 재배치 / GPU slice 증강
```

고부하 batch 서비스가 상시 안전 서비스를 방해하지 않도록 우선순위·preemption 정책을 함께 검증한다.

## 8.5 Phase D — 2차 Stage cut-point 오프로딩 및 자원 증강 시험

1차 모델 endpoint routing이 검증된 뒤 Component Library 기반의 Stage 확장을 적용한다. Stage 오프로딩 시험은 다음 순서로 수행한다.

1. 병목 Model 또는 Component를 특정한다.
2. 해당 실행 Stage 앞·뒤의 candidate cut-point를 정의한다.
3. cut-point별 payload 크기와 network overhead를 측정한다.
4. 동일 입력·동일 Model/Component version으로 Local/Server 실행시간을 비교한다.
5. 단일 Stage 이동과 연속 Stage-group 이동을 각각 비교한다.
6. 최종 판단은 Stage latency가 아니라 Workflow E2E SLO 회복 여부로 한다.
7. 효과가 없으면 원래 placement로 rollback한다.



각 후보에 대해 다음 경로를 비교한다.

1. Local baseline
2. Local optimized runtime
3. Server full offload
4. Stage-level offload
5. Server resource scale-up
6. 다른 worker로 재배치

비교 시작점과 종료점은 동일해야 한다.

- 시작: 엣지에서 서비스 입력이 준비된 시점
- 종료: 결과가 실제 소비 위치에서 사용 가능한 시점

Server 내부 추론시간만으로 판단하지 않는다.

## 8.6 Phase E — 네트워크·장애·회복 시험

- network delay·jitter·loss
- endpoint not ready
- timeout·retry storm
- server GPU OOM
- model version mismatch
- local fallback
- route rollback
- 서비스 재시작과 상태 복원

## 8.7 실험 성공 기준

| 구분 | 통과 조건 |
|---|---|
| 기능 | 서비스별 정확도 기준 충족 |
| 성능 | 사전 정의한 SLO 충족 |
| 자원 | OOM·allocation failure·과도한 throttling 없음 |
| 오프로딩 | 동일 입력·동일 모델에서 사전 정의한 실용 개선폭 충족 |
| 안정성 | 오류 시 local fallback 또는 rollback 성공 |
| 재현성 | dataset hash, model hash, runtime, node, seed 기록 |

---

# 9. CPU·GPU·NPU 실험 기준

## 9.1 CPU 실험이 필요한 서비스

- S04, S06~S08 시계열
- S11 표형 데이터
- S05 경량 OCR
- S09 기본 Open3D 정합

CPU 실험은 공통 실행 가능성과 cgroup throttling·동시 서비스 경합을 확인하는 데 적합하다. 그러나 CPU 결과를 GPU 중심 Vision 서비스에 일반화하지 않는다.

## 9.2 GPU 실험이 필요한 서비스

- S01~S03 객체 검출·추적·포즈
- S10 영상 이상검사
- S06~S08 대형 시계열 모델
- S09 대규모 포인트클라우드

GPU 실험은 단순 GPU duty 부하보다 실제 모델·stream·batch를 통해 경합을 만든다. synthetic GPU load는 계측 검증과 경계 탐색에만 사용한다.

## 9.3 NPU 실험이 필요한 서비스

- RTMDet/RTMPose 경량 모델
- PP-OCR 경량 모델
- TCN/1D-CNN 계열

다음이 확인되지 않으면 NPU 공식 실험 대상에서 제외한다.

- 모델 변환 성공
- operator fallback 없음 또는 허용 범위 정의
- NPU utilization·memory 관측 가능
- 정확도 저하 측정
- runtime·driver 재현 가능

---

# 10. 구현 및 검증 추진 순서

## Step 0. 기반 기술 범위 고정

1차 구현 스택을 다음으로 고정한다.

- Kubeflow Hub standalone
- Hub backend RDBMS와 영속 볼륨
- MinIO/S3
- 별도 Profile Store
- ONNX Runtime
- TensorRT
- Triton Inference Server
- Prometheus/DCGM
- Kubernetes `v1.31.14` / KubeEdge

Full Kubeflow와 KServe는 1차 범위에서 제외한다. Kubeflow Pipelines, Katib, Trainer, Notebooks는 현재 연구범위에 포함하지 않는다. KServe `v0.20`은 Kubernetes `1.32+` 요구사항과 KubeEdge data plane 호환성을 별도 검증한 뒤 후속 선택 도입한다.

## Step 1. Kubeflow Hub standalone 도입 자격시험

- Kubernetes `v1.31.14` standalone 설치와 재시작
- Hub metadata DB 영속성·백업·복구
- 모델·버전·artifact metadata CRUD
- MinIO/S3 artifact URI 등록·조회
- custom property 왕복 보존
- 인증·권한과 API 접근 경로
- Hub/API version 고정과 업그레이드 절차

를 작은 모델 1개로 확인한다.

자격시험을 통과하면 Kubeflow Hub를 기준 Registry로 확정한다. 실패하면 자체 Registry를 만들지 않고 동일 `ModelRegistryPort`의 다른 Adapter를 검토한다.

## Step 2. ModelRegistryPort·KubeflowHubAdapter·EdgeModelDescriptor 개발

Hub의 모델·버전·artifact metadata를 다음 공통 Port로 격리한다.

- 모델 등록과 버전 조회
- artifact 선택과 무결성 hash 조회
- custom property 갱신
- deployment status 갱신

Hub model version에는 `EdgeModelDescriptor`를 통해 다음 정보를 연결한다.

- input/output schema
- architecture: `arm64/amd64`
- runtime candidate
- CPU/GPU/NPU candidate
- license/weight provenance
- profile references
- qualification state

Profiler, Placement Engine과 Service Workflow는 Hub REST API를 직접 호출하지 않는다.

## Step 3. Runtime Adapter 구현

우선 다음 adapter를 만든다.

- `onnxruntime-cpu`
- `onnxruntime-cuda`
- `tensorrt`
- `triton-http/grpc`
- `lightgbm-native`

공통 인터페이스:

```text
load(model)
infer(input)
unload()
health()
metadata()
metrics()
```

## Step 4. Edge Model Profiler 개발

동일 모델·동일 dataset/input profile을 대상 장치에서 자동 실행한다.

대상:

- Raspberry Pi 5 / ORT CPU
- Jetson / ORT CPU
- Jetson / TensorRT 또는 ORT CUDA
- Edge AI Server / ORT CPU
- Edge AI Server / Triton GPU

측정:

- p50/p95/p99 inference
- throughput
- CPU saturation/throttling
- memory
- GPU utilization/VRAM
- NPU utilization(가능 장치)
- power
- cold start/model loading
- error/OOM

## Step 5. Server Profiler 기존 기술 연동

서버 GPU는 가능한 부분을 Triton Model Analyzer/DCGM에서 가져온다.

우리 Profiler는 결과를 자체 방식으로 다시 측정하는 것이 아니라 **Edge Profile schema에 맞게 수집·정규화**한다.

## Step 6. Profile Aggregator 개발

Key:

```text
Model ID
+ Model Version
+ Device Class
+ Runtime
+ Accelerator
+ Input Profile
+ Concurrency/Batch
```

Value:

```text
Latency
Throughput
Memory
CPU/GPU/NPU
Power
Cold start
Qualification
```

를 Profile Store에 통합 저장한다. Hub에는 상세 반복 결과를 복제하지 않고 qualification 요약과 최신 profile reference만 기록한다.

## Step 7. 첫 Model Library 등록

우선 다음 모델군을 등록하여 profiler 파이프라인을 검증한다.

- LightGBM
- TCN
- TimesNet
- RTMDet

이 시점의 목적은 서비스 정확도 확정이 아니라 **서로 다른 workload class와 CPU/GPU 특성을 가진 모델의 실행 profile pipeline 검증**이다.

## Step 8. Placement Engine v1

입력:

- Model Execution Profile
- Device Current State
- Network RTT/BW
- Input size
- Service SLO

출력:

- RPi Local CPU
- Jetson CPU
- Jetson GPU
- Server CPU
- Server GPU
- BLOCKED

v1은 rule/score 기반으로 시작하고, 정책이 안정화된 뒤 최적화 알고리즘을 검토한다.

## Step 9. Offloading Controller v1

1차는 Pod migration보다 **pre-deployed endpoint routing** 방식으로 검증한다. 이는 Kubernetes scheduler 기능이 아니라 서비스 호출 경로를 전환하는 application-level 제어다.

```text
Local endpoint
Server endpoint
      ↓
Placement decision
      ↓
Route switch
```

전환 전 대상 endpoint의 model version, readiness와 capacity를 확인하고, 진행 중 요청 drain, timeout, retry, circuit-breaker, minimum dwell time과 rollback 조건을 적용한다. 이유는 Kubernetes scheduling/model loading 시간이 순수한 offloading 효과와 섞이는 것을 막기 위해서다.

## Step 10. Service Workflow 연결

Model Library에서 모델을 선택하여 S06·S11 등 실제 서비스와 연결한다.

이 단계부터:

- model accuracy
- service E2E latency
- input rate
- backlog
- deadline

를 함께 본다.

## Step 11. Component Library / Stage-level 확장

모델 단위 Placement가 검증된 뒤:

- Windowing
- FFT/RMS
- Feature Join
- Detector
- Decision

등을 Component Library로 분리하고 Stage-level profile/cut-point offloading으로 확장한다.

## Step 12. KServe Standard Mode 선택 도입 Gate

다음 조건을 모두 만족할 때만 KServe Standard Mode를 후속 도입한다.

- 테스트베드 Kubernetes `1.32+` 업그레이드와 KubeEdge 지원 버전 정합성 확인
- KServe CRD/controller/webhook과 cert-manager 정상 동작
- Gateway API 또는 Ingress 경로 검증
- MinIO storage initializer와 credential 전달 검증
- `InferenceService`가 만든 Deployment/Service의 목표 node 배치 검증
- EdgeMesh/Service routing, HPA/metrics, cloud-edge 단절 시 동작 검증
- 기존 Runtime Adapter 대비 운영 복잡도와 효과 비교

1차에서는 KServe 없이 RPi/Jetson은 ORT/TRT Adapter, Edge AI Server는 Triton endpoint로 실행한다.

개발 순서는:

> **Hub 자격시험 → Registry Adapter → Model Profiler → Placement → Endpoint Offloading → 실제 Service → Stage-level 분할 → KServe 선택 검증**

이다.


# 11. 현재 준비도와 선행조건

| 서비스 | 데이터 준비 | 장비·접근 | 공개 모델 | 가장 큰 선행조건 | 초기 상태 |
|---|---|---|---|---|---|
| S01 | 영상 필요 | 카메라·PLC 연동 필요 | 충분 | ROI·톱날 가동 신호 확정 | 대기 |
| S02 | 영상 필요 | 카메라·PLC 연동 필요 | 충분 | 고온 구역·설비 상태 정의 | 대기 |
| S03 | XR 카메라 존재 | 영상 반출 협의 필요 | 충분 | 지게차 annotation·근접 기준 | 조건부 |
| S04 | PLC 데이터 가능 | 접근 가능성 높음 | 충분 | 이상 label·안전 규칙 | 조건부 |
| S05 | 금형 영상 필요 | 전용 카메라·표기방식 재검토 | 충분 | 수기 페인트마카 데이터셋 | 보류 |
| S06 | 센서 데이터 존재 | 진동·소음 센서 확인 | 충분 | 정상/이상 기준과 window 정의 | 우선 |
| S07 | 센서·PLC 가능 | 센서 위치 확인 | 충분 | 속도조건별 정상 분포 | 조건부 |
| S08 | 센서·PLC 가능 | 센서 위치 확인 | 충분 | 과부하 event·label | 조건부 |
| S09 | 데이터 없음 | 2D/3D scanner 도입 필요 | 충분 | CAD version·공차·scanner calibration | 장비대기 |
| S10 | 일부 장비 계획 | line scanner/조도계 연동 | 충분 | 정상·결함 영상과 조도 ground truth | 장비대기 |
| S11 | 센서·MES·품질 데이터 후보 | 실제 label join 확인 필요 | 충분 | 제품 ID·시간 정렬·불량 label 품질 | 우선 |

---

# 12. 미해결 결정사항

| 번호 | 결정사항 | 문제 | 필요한 조치 |
|---|---|---|---|
| D01 | 공식 서비스 기준선 | 협약계획서와 현장회의의 서비스 명칭·연차 우선순위 차이 | 총괄 승인된 Service Catalog v1.0 발행 |
| D02 | 실제 하드웨어 inventory | 소개자료와 최근 실험문서의 서버·GPU 정보가 다를 수 있음 | node name, CPU, GPU/NPU, memory, runtime 재수집 |
| D03 | S06 label | 실제 고장 데이터가 부족할 가능성 | 정상학습·synthetic fault·maintenance log 전략 결정 |
| D04 | S11 quality join | MES 생산조건과 Q-Data의 제품 단위 매핑 불명확 | product/lot key와 time join 검증 |
| D05 | Vision 반출 | XR CCTV 데이터 외부 송출 협의 필요 | 권한·보안·보존 정책 확정 |
| D06 | S05 표기방식 | 수기 페인트마카 OCR 난이도 높음 | 도장식 표기·재학습 주기·manual review 설계 |
| D07 | S09/S10 장비 | scanner·camera 예산·구매 일정 | 장비 확정 전 synthetic 데이터는 기능검증으로만 제한 |
| D08 | NPU 후보 | 실제 NPU와 SDK·operator 지원 미확정 | 모델 변환 spike 후 지원 여부 결정 |
| D09 | 라이선스 | 코드 라이선스와 모델 weight·dataset·dependency 조건이 다를 수 있음 | 서비스별 SBOM·license review |
| D10 | 오프로딩 gate | 기존 10%/5% gate가 실제 서비스 SLO 기반이 아님 | 서비스별 minimum practical improvement 사전 등록 |
| D11 | Kubeflow Hub 도입 | standalone/API가 비교적 초기 단계이고 운영 안정성 검증이 필요 | Step 1 자격시험 통과 후 기준 Registry로 확정 |
| D12 | Profile Store | 원시 반복 측정과 집계 profile의 저장 기술 미확정 | 조회·보존·재현 요구사항에 따라 DB/object storage 조합 결정 |
| D13 | KServe 도입 | 현재 Kubernetes `v1.31.14`와 KServe `v0.20` 요구사항 불일치, KubeEdge 호환성 미검증 | Kubernetes/KubeEdge 업그레이드와 Step 12 통합 Gate 통과 후 선택 도입 |

---

# 13. 최종 산출물 정의

본 설계가 완료되면 다음 산출물을 관리한다.

1. `contracts/models/<model-id>/<version>/edge-model-descriptor.yaml`
2. `contracts/runtimes/<runtime-id>.yaml`
3. `contracts/data/<service-id>.yaml`
4. `contracts/services/<service-id>.yaml`
5. `profiles/model-execution/<model>/<version>/<runtime>/<node>.yaml`
6. `profiles/devices/<node>.yaml`
7. `profiles/transfers/`와 `profiles/workflows/`
8. `policies/placement-policy.yaml`
9. `policies/offloading-policy.yaml`
10. `qualification/kubeflow-hub/`와 후속 `qualification/kserve/`
11. 실험 원시 결과, dataset/model/artifact/container hash
12. 서비스별 정확도·성능·장애·rollback 검증 보고서

권장 저장 구조는 다음과 같다.

```text
docs/
  service-catalog.md
  service-design/
    OKD-SVC-006.md
    OKD-SVC-011.md
contracts/
  models/
  runtimes/
  data/
  services/
profiles/
  model-execution/
  devices/
  transfers/
  workflows/
experiments/
  profiling/
  routing/
  co-location/
  recovery/
policies/
  placement-policy.yaml
  offloading-policy.yaml
qualification/
  kubeflow-hub/
  kserve/
```

---

# 14. 참고자료

## 과제·현장 자료

- 「(2차년도 협약용) 연구개발계획서 – 엣지 컴퓨팅 시스템을 위한 대규모 혼합 디바이스 제어·관리 플랫폼 개발」, 2차년도 개발 내용 및 범위, 가상 디바이스 성능 증강 기술
- 「㈜옥동 현장 방문 회의록」, 2026-07-29, PoC AI 서비스 11종, 현장 데이터·장비·우선 실증 범위
- 「AI 서비스 자원 증강 부하 실험」, 기존 프록시 기반 CPU/GPU 실험 결과와 한계

## 공개 모델·공식 구현

- Kubeflow Hub: https://www.kubeflow.org/docs/components/hub/overview/
- Kubeflow Hub Architecture: https://www.kubeflow.org/docs/components/hub/reference/architecture/
- Kubeflow Hub Installation: https://www.kubeflow.org/docs/components/hub/installation/
- KServe Kubernetes Deployment: https://kserve.github.io/website/docs/admin-guide/kubernetes-deployment
- ONNX Runtime: https://onnxruntime.ai/docs/
- NVIDIA TensorRT: https://docs.nvidia.com/deeplearning/tensorrt/
- NVIDIA Triton Inference Server: https://docs.nvidia.com/deeplearning/triton-inference-server/
- MMDetection / RTMDet: https://github.com/open-mmlab/mmdetection
- ByteTrack: https://github.com/FoundationVision/ByteTrack
- MMPose / RTMPose: https://github.com/open-mmlab/mmpose
- DCASE 2026 Task 2: https://dcase.community/challenge2026/task-first-shot-unsupervised-anomalous-sound-detection-for-machine-condition-monitoring
- DCASE baseline AE: https://github.com/nttcslab/dcase2023_task2_baseline_ae
- Time-Series-Library / TimesNet: https://github.com/thuml/Time-Series-Library
- LightGBM: https://github.com/lightgbm-org/LightGBM
- XGBoost: https://github.com/dmlc/xgboost
- PaddleOCR / PP-OCRv5: https://github.com/PaddlePaddle/PaddleOCR
- Anomalib: https://github.com/open-edge-platform/anomalib
- Open3D: https://github.com/isl-org/Open3D

---

## 결론

이 과제의 다음 작업은 CPU 임계값을 추가로 조정하는 것이 아니다.

> **자체 Model Registry를 재개발하지 않고 Kubeflow Hub standalone과 MinIO/S3를 모델 관리 기반으로 활용한다. Hub 연동은 `ModelRegistryPort`와 `KubeflowHubAdapter`로 격리하고, 상세 실행 결과는 별도 Profile Store에 저장한다. 동일 모델을 이기종 Edge 환경에서 자동 프로파일링하는 계층과 Placement/Endpoint Offloading 계층을 우선 개발한다.** Full Kubeflow와 KServe는 1차 범위에서 제외하며, 실제 서비스와 Stage-level 분할은 이 기반이 검증된 이후 연결한다.

최종 오프로딩 기준은 “CPU가 높다”가 아니라 다음 식으로 구성한다.

```text
증강 또는 오프로딩 후보
= 서비스별 병목 확인
AND 서비스 SLO 저하 지속
AND 동일 모델·동일 입력의 후보 실행 위치가 E2E 개선 제공
AND 오류·OOM·버전 불일치 없음
AND rollback 가능
```
