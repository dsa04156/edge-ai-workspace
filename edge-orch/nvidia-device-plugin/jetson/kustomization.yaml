apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - daemonset.yaml
configMapGenerator:
  - name: nvidia-jetson-timeslicing
    namespace: kube-system
    files:
      - config.yaml
