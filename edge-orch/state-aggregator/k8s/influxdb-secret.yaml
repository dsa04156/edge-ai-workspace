apiVersion: v1
kind: Secret
metadata:
  name: influxdb-token
  # Namespace should match where state-aggregator is deployed (default if not set)
  namespace: default
type: Opaque
stringData:
  # Replace the value below with your InfluxDB admin token or created API token
  token: edgeai-super-token-change-me
